\newif\ifglobal

%%%%%%%%%%%%%%%%%%%%%%%
%%% Compile to view %%%
%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Readme:                                     %%%
%%% https://www.overleaf.com/read/bwdjvfjksvjy/ %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% >>> M?: marks a module setting number or important notice

% >>> M4: Set {./relative-path} to external files for this module <<<
\def \modulefiles {./22-I2S}
% To add an external file, use:
% (Replace '---' with actual filename)
% '\input{\modulefiles/tables/---.tex}' for tables
% '\includegraphics{\modulefiles/figures/---}' for figures
% '\subfile{\modulefiles/---}' for register files

% >>> M1: This module is in global project? <<<
% 'YES' - uncomment; 'NO' - comment out
\globaltrue

\ifglobal
    \documentclass[main\_\_EN.tex]{subfiles}

\else
    \documentclass[openany, 10pt]{book}

    % >>> M2: In ./00-shared/preamble-trm-module, also find
    %         settings for visibility of todo notes, watermark <<<
    \usepackage{preamble-trm-module}

    % >>> M3: Temporary labels in English,
    %         you can add your own labels there <<<
    \myexternaldocument{./00-shared/temp-labels-en}

\fi %\ifglobal


\setcounter{secnumdepth}{4}


% >>> M5: If this module needs any updates in
% './00-shared/preamble-trm-module.sty'
% (include additional package, etc.), contact your module PM
% or create the file './00-shared/changelog.tex' make the
% necessary updates yourself and document them in this file <<<

% Make text left-aligned and allow latex to break pages naturally, comment out for full justification
\raggedright
\raggedbottom


\begin{document}


% Sets language into which pre-defined bits of text are translated
\selectlanguage{english}
\setenglishformatting

% Do not delete this block
\ifglobal
% Add the following front matter if
% this module is outside of global project
\else
    \InputIfFileExists{readme}{}{}
    \listoftodos
    {\let\clearpage\relax \listofdonetodos}
    \tableofcontents
    %\listoftables
    %\listoffigures
\fi


%%%%%%%%%%%%%%%%%%%%%%%%%
%%% TRM Module Begins %%%
%%%%%%%%%%%%%%%%%%%%%%%%%

\hypertarget{i2s}{}
\chapter{I2S Controller (I2S)}
\label{mod:i2s}

\section{Overview}

The I2S bus provides a flexible communication interface for streaming digital data in multimedia applications, especially digital audio applications. The ESP32 includes two I2S interfaces: I2S0 and I2S1.

The I2S standard bus defines three signals: a clock signal, a channel selection signal, and a serial data signal. A basic I2S data bus has one master and one slave. The roles remain unchanged throughout the communication. The I2S modules on the ESP32 provide separate transmit and receive channels for high performance.

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.9\textwidth]{./22-I2S/figures/i2s_arch}}
    \caption{I2S System Block Diagram}
    \label{Figure:i2s_arch}
\end{figure}

Figure \ref{Figure:i2s_arch} is the system block diagram of the ESP32 I2S module. In the figure above, the value of "\textcolor{red}{\it{n}}" can be either 0 or 1. There are two independent I2S modules embedded in ESP32, namely I2S0 and I2S1. Each I2S module contains a Tx (transmit) unit and a Rx (receive) unit. Both the Tx unit and the Rx unit have a three-wire interface that includes a clock line, a channel selection line and a serial data line. The serial data line of the Tx unit is fixed as output, and the serial data line of the Rx unit is fixed as input. The clock line and the channel selection line of the Tx and Rx units can be configured to both master transmitting mode and slave receiving mode. In the LCD mode, the serial data line extends to the parallel data bus. Both the Tx unit and the Rx unit have a 32-bit-wide FIFO with a depth of 64. Besides, only I2S0 supports on-chip DAC/ADC modes, as well as receiving and transmitting PDM signals.

The right side of Figure \ref{Figure:i2s_arch} shows the signal bus of the I2S module. The signal naming rule of the Rx and Tx units is I2S\textcolor{red}{\it{n}}A\_B\_C, where "\textcolor{red}{\it{n}}" stands for either I2S0 or I2S1; "A" represents the direction of I2S module's data bus signal, "I" represents input, "O" represents output; "B" represents signal function; "C" represents the signal direction, "in" means that the signal is input into the I2S module, while "out" means that the I2S module outputs the signal. For a detailed description of the I2S signal bus, please refer to Table \ref{table:I2S 信号总线描述}. Table \ref{table:I2S 信号总线描述} describes the signal bus of the I2S module. Except for the I2S\textcolor{red}{\it{n}}\_CLK signal, all other signals are mapped to the chip pin via the GPIO matrix and IO MUX. The I2S\textcolor{red}{\it{n}}\_CLK signal is mapped to the chip pin via the IO\_MUX. For details, please refer to the chapter about \hyperref[mod:iomuxgpio]{IO\_MUX and the GPIO Matrix}.

\begin{table}[H]
    \centering
    \caption{I2S Signal Bus Description}
    \label{table:I2S 信号总线描述}
    \begin{tabular}{|p{2.5cm}|p{7cm}|p{5.5cm}|}
    \hline
    \rowcolor{lightgray}
    Signal Bus & Signal Direction & Data Signal Direction\\
    \hline
    I2S\textcolor{red}{\it{n}}I\_BCK\_in & In slave mode, I2S module inputs signals. & I2S module receives data.\\
    \hline
    I2S\textcolor{red}{\it{n}}I\_BCK\_out & In master mode, I2S module outputs signals. & I2S module receives data.\\
    \hline
    I2S\textcolor{red}{\it{n}}I\_WS\_in & In slave mode, I2S module inputs signals. & I2S module receives data.\\
    \hline
    I2S\textcolor{red}{\it{n}}I\_WS\_out & In master mode, I2S module outputs signals. & I2S module receives data.\\
    \hline
    \multirow{4}*{I2S\textcolor{red}{\it{n}}I\_Data\_in$^1$} & \multirow{4}*{I2S module inputs signals.} & In I2S mode, I2S\textcolor{red}{\it{n}}I\_Data\_in[15] is the serial data bus of I2S. In LCD mode, the data bus width can be configured as needed. \\
    \hline
    \multirow{4}*{I2S\textcolor{red}{\it{n}}O\_Data\_out$^1$} & \multirow{4}*{I2S module outputs signals.} & In I2S mode, I2S\textcolor{red}{\it{n}}O\_Data\_out[23] is the serial data bus of I2S. In LCD mode, the data bus width can be configured as needed. \\
    \hline
    I2S\textcolor{red}{\it{n}}O\_BCK\_in & In slave mode, I2S module inputs signals. & I2S module sends data.\\
    \hline
    I2S\textcolor{red}{\it{n}}O\_BCK\_out & In master mode, I2S module outputs signals. & I2S module sends data.\\
    \hline
    I2S\textcolor{red}{\it{n}}O\_WS\_in & In slave mode, I2S module inputs signals.& I2S module sends data.\\
    \hline
    I2S\textcolor{red}{\it{n}}O\_WS\_out&	In master mode, I2S module outputs signals.& I2S module sends data.\\
    \hline
    \multirow{2}*{I2S\textcolor{red}{\it{n}}\_CLK$^2$}& \multirow{2}*{I2S module outputs signals.} & It is used as a clock source for peripheral chips.\\
    \hline
    I2S\textcolor{red}{\it{n}}\_H\_SYNC&\multirow{3}*{In Camera mode, I2S module inputs signals.}&\multirow{3}*{The signals are sent from the Camera. }\\
    \cline{1-1}
    I2S\textcolor{red}{\it{n}}\_V\_SYNC&&\\
    \cline{1-1}
    I2S\textcolor{red}{\it{n}}\_H\_ENABLE	&&\\
    \hline
    \end{tabular}
\end{table}

\begin{tiplisting}
\vspace{-2em}
\begin{enumerate}
\item Assume that the bit width of the input/output signal is \textcolor{red}{\it{N}}, the input signal should be configured to I2S\textcolor{red}{\it{n}}I\_Data\_in[\textcolor{red}{\it{N}}-1:0], and the output signal to I2S\textcolor{red}{\it{n}}O\_Data\_out[23:23-\textcolor{red}{\it{N}}+1]. Generally, for input signals, \textcolor{red}{\it{N}}=8 or 16; while for output signals, \textcolor{red}{\it{N}}=8, 16 or 24 (note that I2S1 does not support 24-bit width).
\item I2S\textcolor{red}{\it{n}}\_CLK can only be mapped to GPIO0, U0RXD (GPIO3) or U0TXD (GPIO1) by selecting GPIO functions CLK\_OUT1, CLK\_OUT2, or CLK\_OUT3. For more information, see Table \ref{table:iomux_pads}: \textit{\nameref{table:iomux_pads}}.
\end{enumerate}
\end{tiplisting}

\section{Features}
I2S mode
\begin{itemize}
    \item Configurable high-precision output clock
    \item Full-duplex and half-duplex data transmit and receive modes
    \item Supports multiple digital audio standards
    \item Embedded A-law compression/decompression module
    \item Configurable clock signal
    \item Supports PDM signal input and output
    \item Configurable data transmit and receive modes
\end{itemize}

LCD mode
\begin{itemize}
    \item Supports multiple LCD modes, including external LCD
    \item Supports external Camera
    \item Supports on-chip DAC/ADC modes
\end{itemize}

I2S interrupts
\begin{itemize}
    \item Standard I2S interface interrupts
    \item I2S DMA interface interrupts
\end{itemize}

\section{The Clock of I2S Module}\label{The Clock of I2S Module}
As is shown in Figure \ref{Figure:i2s_clk}, I2S\textcolor{red}{\it{n}}\_CLK, as the master clock of I2S module, is derived from the 160 MHz clock PLL\_F160M\_CLK or the configurable analog PLL output clock APLL\_CLK. The serial clock (BCK) of the I2S module is derived from I2S\textcolor{red}{\it{n}}\_CLK. The I2S\_CLKA\_ENA bit of register I2S\_CLKM\_CONF\_REG is used to select either PLL\_F160M\_CLK or APLL\_CLK as the clock source for I2S\textcolor{red}{\it{n}}. PLL\_F160\_CLK is used as the clock source for I2S\textcolor{red}{\it{n}}, by default.

{\bfseries Notice:}
\begin{itemize}
\item When using PLL\_F160M\_CLK as the clock source, it is not recommended to divide it using decimals.  For high performance audio applications, the analog PLL output clock source APLL\_CLK must be used to acquire highly accurate I2S\textcolor{red}{\it{n}}\_CLK and BCK. For further details, please refer to the chapter entitled \hyperref[mod:resclk]{Reset and Clock}.
\item When ESP32 I2S works in slave mode, the master must use I2S\textcolor{red}{\it{n}}\_CLK as the master clock and $f_{\textrm{i2s}}$ >= 8 * $f_{\textrm{BCK}}$.
\end{itemize}

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_clk}}
    \caption{I2S Clock}
    \label{Figure:i2s_clk}
\end{figure}

The relation between I2S\textcolor{red}{\it{n}}\_CLK frequency $f_{\textrm{i2s}}$ and the divider clock source frequency $f_{\textrm{pll}}$ can be seen in the equation below:
$$f_{\textrm{i2s}}=\frac{f_{\textrm{pll}}}{\textrm{N} + \frac{\textrm{b}}{\textrm{a}}}$$

"N", whose value is >=2, corresponds to the REG \_CLKM\_DIV\_NUM [7:0] bits of register I2S\_CLKM\_CONF\_REG , "b" is the I2S\_CLKM\_DIV\_B[5:0] bit and "a" is the I2S\_CLKM\_DIV\_A[5:0] bit.

In master mode, the serial clock BCK in the I2S module is derived from I2S\textcolor{red}{\it{n}}\_CLK, that is:
$$f_{\textrm{BCK}} = \frac{f_{\textrm{i2s}}}{\textrm{M}}$$

In master transmitting mode, "M", whose value is >=2, is the I2S\_TX\_BCK\_DIV\_NUM[5:0] bit of register I2S\_SAMPLE\\\_RATE\_CONF\_REG. In master receiving mode, "M" is the I2S\_RX\_BCK\_DIV\_NUM[5:0] bit of register I2S\_SAMPLE\\\_RATE\_CONF\_REG.


\section{I2S Mode}

The ESP32 I2S module integrates an A-law compression/decompression module to enable compression/decompression of the received audio data. The RX\_PCM\_BYPASS bit and the TX\_PCM\_BYPASS bit of register I2S\_CONF1\_REG should be cleared when using the A-law compression/decompression module.


\subsection{Supported Audio Standards}

In the I2S bus, BCK is the serial clock, WS is the left- /right-channel selection signal (also called word select signal), and SD is the serial data signal for transmitting/receiving digital audio data. WS and SD signals in the I2S module change on the falling edge of BCK, while the SD signal can be sampled on the rising edge of BCK. If the I2S\_RX\_RIGHT\_FIRST bit and the I2S\_TX\_RIGHT\_FIRST bit of register I2S\_CONF\_REG are set to 1, the I2S module is configured to receive and transmit right-channel data first. Otherwise, the I2S module receives and transmits left-channel data first.

\subsubsection{Philips Standard}\label{section:Philips Standard}

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_phillips_standard}}
    \caption{Philips Standard}
    \label{Figure:i2s_phillips_standard}
\end{figure}


As is shown in Figure \ref{Figure:i2s_phillips_standard}, the Philips I2S bus specifications require that the WS signal starts to change one BCK clock cycle earlier than the SD signal on BCK falling edge, which means the WS signal becomes valid one clock cycle before the first bit of data transfer on the current channel, and changes one clock cycle earlier than the end of data transfer on the current channel. The SD signal line transmits the most significant bit of audio data first. If the I2S\_RX\_MSB\_SHIFT bit and the I2S\_TX\_MSB\_SHIFT bit of register I2S\_CONF\_REG are set to 1, respectively, the I2S module will use the Philips standard when receiving and transmitting data.

\subsubsection{MSB Alignment Standard}

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_MSB_32_mode}}
    \caption{MSB Alignment Standard}
    \label{Figure:i2s_MSB_32_mode}
\end{figure}

The MSB alignment standard is shown in Figure \ref{Figure:i2s_MSB_32_mode}. WS and SD signals both change simultaneously on the falling edge of BCK under the MSB alignment standard. The WS signal continues until the end of the current channel-data transmission, and the SD signal line transmits the most significant bit of audio data first. If the I2S\_RX\_MSB\_SHIFT and I2S\_TX\_MSB\_SHIFT bits of register I2S\_CONF\_REG are cleared, the I2S module will use the MSB alignment standard when receiving and transmitting data.


\subsubsection{PCM Standard}
As is shown in Figure \ref{Figure:i2s_pcm_mode}, under the short frame synchronization mode of the PCM standard, the WS signal starts to change a BCK clock cycle earlier than the SD signal, which means that the WS signal takes effect a clock cycle earlier than the first bit of the current channel-data transmission and continues for one extra BCK clock cycle. The SD signal line transmits the most significant bit of audio data first. If the I2S\_RX\_SHORT\_SYNC and I2S\_TX\_SHORT\_SYNC bits of register I2S\_CONF\_REG are set, the I2S module will receive and transmit data in the short frame synchronization mode.
\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_pcm_mode}}
    \caption{PCM Standard}
    \label{Figure:i2s_pcm_mode}
\end{figure}

\subsection{Module Reset}
The four low-order bits in register I2S\_CONF\_REG, that is,  I2S\_TX\_RESET, I2S\_RX\_RESET, I2S\_TX\_FIFO\_RESET and I2S\_RX\_FIFO\_RESET reset the receive module, the transmit module and the corresponding FIFO buffer, respectively. In order to finish a reset operation, the corresponding bit should be set and then cleared by software.

\subsection{FIFO Operation}
The data read/write packet length for a FIFO operation is 32 bits. The data packet format for the FIFO buffer can be configured using configuration registers. As shown in Figure \ref{Figure:i2s_arch}, both sent and received data should be written into FIFO first and then read from FIFO. There are two approaches to accessing the FIFO; one is to directly access the FIFO using a CPU, the other is to access the FIFO using a DMA controller.


Generally, both the I2S\_RX\_FIFO\_MOD\_FORCE\_EN bit and I2S\_TX\_FIFO\_MOD\_FORCE\_EN bits of register I2S\_\\FIFO\_CONF\_REG should be set to 1. I2S\_TX\_DATA\_NUM[5:0] bit and I2S\_RX\_DATA\_NUM[5:0] are used to control the length of the data that have been sent, received and buffered. Hardware inspects the received-data length RX\_LEN and the transmitted-data length TX\_LEN. Both the received and the transmitted data are buffered in the FIFO method.

When RX\_LEN is greater than I2S\_RX\_DATA\_NUM[5:0], the received data, which is buffered in FIFO, has reached the set threshold and needs to be read out to prevent an overflow. When TX\_LEN is less than I2S\_TX\_DATA\_NUM[5:0], the transmitted data, which is buffered in FIFO, has not reached the set threshold and software can continue feeding data into FIFO.


\subsection{Sending Data}\label{TXCHAN}

The ESP32 I2S module carries out a data-transmit operation in three stages:
\begin{itemize}
    \item Read data from internal storage and transfer it to FIFO
    \item Read data to be sent from FIFO
    \item Clock out data serially, or in parallel, as configured by the user
\end{itemize}

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.62\textwidth]{./22-I2S/figures/i2s_tx_fifo_mode}}
    \caption{Tx FIFO Data Mode}
    \label{Figure:i2s_tx_fifo_mode}
\end{figure}

\begin{table}[H]
    \centering
    \caption{Register Configuration}
    \label{table:寄存器配置}
    \begin{tabular}{|p{5cm}|p{5cm}|p{5cm}| }
    \rowcolor{lightgray}
    \hline
    &I2S\_TX\_FIFO\_MOD[2:0]& Description\\
    \hline
    \multirow{3}*{Tx FIFO mode0} & 0 & 16-bit dual channel data\\
    \cline{2-3}
                                 & 2 & 32-bit dual channel data\\
    \cline{2-3}
                                 & 3 & 32-bit single channel data\\
    \hline
    Tx FIFO mode1                & 1 & 16-bit single channel data\\
    \hline
    \end{tabular}
\end{table}

At the first stage, there are two modes for data to be sent and written into FIFO. In Tx FIFO mode0, the Tx data-to-be-sent are written into FIFO according to the time order. In Tx FIFO mode1, the data-to-be-sent are divided into 16 high- and 16 low-order bits. Then, both the 16 high- and 16 low-order bits are recomposed and written into FIFO. The details are shown in Figure \ref{Figure:i2s_tx_fifo_mode} with the corresponding registers listed in Table \ref{table:寄存器配置}. $D^{'}_{n}$ consists of 16 high-order bits of $D_{n}$ and 16 zeros. $D^{''}_{n}$ consists of 16 low-order bits of $D_{n}$ and 16 zeros. That is to say, $D^{'}_{n} = \{D_{n}[31:16],16'h0\}$, $D^{''}_{n} = \{D_{n}[15:0],16'h0\}$.

At the second stage, the system reads data that will be sent from FIFO, according to the relevant register configuration. The mode in which the system reads data from FIFO is relevant to the configuration of I2S\_TX\_FIFO\_MOD[2.0] and I2S\_TX\_CHAN\_MOD[2:0]. I2S\_TX\_FIFO\_MOD[2.0] determines whether the data are 16-bit or 32-bit, as shown in Table \ref{table:寄存器配置}, while I2S\_TX\_CHAN\_MOD[2:0] determines the format of the data-to-be-sent, as shown in Table \ref{table:发送通道模式}.


\begin{longtable}[c]{ |p{4cm}|p{11cm}|}
    \caption{Send Channel Mode}
    \label{table:发送通道模式}\\
    \hline
    \rowcolor{lightgray}
I2S\_TX\_CHAN\_MOD[2:0]& Description \\
\hline
\endfirsthead
\hline
\rowcolor{lightgray}
I2S\_TX\_CHAN\_MOD[2:0]& Description \\
\hline
\endhead
\hline
\endfoot
    0 & Dual channel mode\\
    \hline
     \multirow{5}*{1} & Mono mode\\
        & When I2S\_TX\_MSB\_RIGHT equals 0, the left-channel data are "holding" their values and the right-channel data change into the left-channel data. \\
        & When I2S\_TX\_MSB\_RIGHT equals 1, the right-channel data are "holding" their values and the left-channel data change into the right-channel data.\\
    \hline
   \multirow{5}*{2} & Mono mode\\
        & When I2S\_TX\_MSB\_RIGHT equals 0, the right-channel data are "holding" their values and the left-channel data change into the right-channel data.\\
        & When I2S\_TX\_MSB\_RIGHT equals 1, the left-channel data are "holding" their values and the right-channel data change into the left-channel data. \\
    \hline
  \multirow{5}*{3} & Mono mode\\
        & When I2S\_TX\_MSB\_RIGHT equals 0, the left-channel data are constants in the range of REG[31:0].\\
& When I2S\_TX\_MSB\_RIGHT equals 1, the right-channel data are constants in the range of REG[31:0].\\
    \hline
  \multirow{5}*{4} & Mono mode\\
        & When I2S\_TX\_MSB\_RIGHT equals 0, the right-channel data are constants in the range of REG[31:0].\\
        & When I2S\_TX\_MSB\_RIGHT equals 1, the left-channel data are constants in the range of REG[31:0].\\
    \hline
  \end{longtable}


REG[31:0] is the value of register I2S\_CONF\_SINGLE\_DATA\_REG[31:0].

The output of the third stage is determined by the mode of the I2S and I2S\_TX\_BITS\_MOD[5:0] bits of register I2S\_SAMPLE\_RATE\_CONF\_REG.

\subsection{Receiving Data}\label{RXCHAN}

The data-receive phase of the ESP32 I2S module consists of another three stages:
\begin{itemize}
    \item The input serial-bit stream is transformed into a 64-bit parallel-data stream in I2S mode. In LCD mode, the input parallel-data stream will be extended to a 64-bit parallel-data stream.
    \item Received data are written into FIFO.
    \item Data are read from FIFO by CPU/DMA and written into the internal memory.
\end{itemize}

At the first stage of receiving data, the received-data stream is expanded to a zero-padded parallel-data stream with 32 high-order bits and 32 low-order bits, according to the level of the I2S\textcolor{red}{\it{n}}I\_WS\_out (or I2S\textcolor{red}{\it{n}}I\_WS\_in) signal. The I2S\_RX\_MSB\_RIGHT bit of register I2S\_CONF\_REG is used to determine how the data are to be expanded.


\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.7\textwidth]{./22-I2S/figures/i2s_rx_example}}
    \caption{The First Stage of Receiving Data}
    \label{Figure:i2s_rx_example}
\end{figure}

For example, as is shown in Figure \ref{Figure:i2s_rx_example}, if the width of serial data is 16 bits, when I2S\_RX\_RIGHT\_FIRST equals 1, Data0 will be discarded and I2S will start receiving data from Data1. If I2S\_RX\_MSB\_RIGHT equals 1, data of the first stage would be $\{0xFEDC0000,0x32100000\}$. If I2S\_RX\_MSB\_RIGHT equals 0, data of the first stage would be $\{0x32100000,0xFEDC0000\}$. When I2S\_RX\_RIGHT\_FIRST equals 0, I2S will start receiving data from Data0. If I2S\_RX\_MSB\_RIGHT equals 1, data of the first stage would be $\{0xFEDC0000,0x76540000\}$. If I2S\_RX\_MSB\_RIGHT equals 0, data of the first stage would be $\{0x76540000,0xFEDC0000\}$.

As is shown in Table \ref{table:接收数据写入FIFO模式和对应寄存器配置} and Figure \ref{Figure:i2s_rx_fifo_mode}, at the second stage, the received data of the Rx unit is written into FIFO. There are four modes of writing received data into FIFO. Each mode corresponds to a value of I2S\_RX\_FIFO\_MOD[2:0] bit.

\begin{table}[H]
    \centering
    \caption{Modes of Writing Received Data into FIFO and the Corresponding Register Configuration}
    \label{table:接收数据写入FIFO模式和对应寄存器配置}
    \begin{tabular}{|p{7cm}|p{8cm}|}
    \hline
    \rowcolor{lightgray}
    I2S\_RX\_FIFO\_MOD[2:0]	&Data format\\
    \hline
    0& 16-bit dual channel data\\
    \hline
    1& 16-bit single channel data\\
    \hline
    2& 32-bit dual channel data\\
    \hline
    3& 32-bit single channel data\\
    \hline
    \end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_rx_fifo_mod}}
    \caption{Modes of Writing Received Data into FIFO}
    \label{Figure:i2s_rx_fifo_mode}
\end{figure}

At the third stage, CPU or DMA will read data from FIFO and write them into the internal memory directly. The register configuration that each mode corresponds to is shown in Table \ref{table:4种模式对应寄存器配置}.

\begin{table}[H]
    \centering
    \caption{The Register Configuration to Which the Four Modes Correspond}
    \label{table:4种模式对应寄存器配置}
    \begin{tabular}{|p{3.2cm}|p{3.2cm}|p{2.1cm}|p{2.3cm}|p{2.1cm}|p{2.3cm}|}
    \hline
    \rowcolor{lightgray}
    I2S\_RX\_MSB\_RIGHT&I2S\_RX\_CHAN\_MOD&mode0&mode1&mode2&mode3\\\hline
    \multirow{6}*{0}&0&\multirow{6}*{\makecell{left channel\\+ right channel}}&-&\multirow{6}*{\makecell{left channel \\+ right channel}}&-\\
    \cline{2-2}\cline{4-4}\cline{6-6}
	&\multirow{2}*{1}&& left channel + left channel && left channel + left channel\\
	\cline{2-2}\cline{4-4}\cline{6-6}
	&\multirow{2}*{2}&& right channel + right channel && right channel + right channel \\
	\cline{2-2}\cline{4-4}\cline{6-6}
	&3&&-&&-\\
	\hline

    \multirow{6}*{1}&0&\multirow{6}*{\makecell{right channel \\+ left channel}}&-&\multirow{6}*{\makecell{right channel \\+ left channel}}&-\\
    \cline{2-2}\cline{4-4}\cline{6-6}
	&\multirow{2}*{1}&& right channel + right channel && right channel + right channel \\
	\cline{2-2}\cline{4-4}\cline{6-6}
	&\multirow{2}*{2}&&left channel + left channel && left channel + left channel\\
	\cline{2-2}\cline{4-4}\cline{6-6}
	&3&&-&&-\\
	\hline
    \end{tabular}
\end{table}

\subsection{I2S Master/Slave Mode}

The ESP32 I2S module can be configured to act as a master or slave device on the I2S bus. The module supports slave transmitter and receiver configurations in addition to master transmitter and receiver configurations. All these modes can support full-duplex and half-duplex communication over the I2S bus.

I2S\_RX\_SLAVE\_MOD bit and I2S\_TX\_SLAVE\_MOD bit of register I2S\_CONF\_REG can configure I2S to slave receiving mode and slave transmitting mode, respectively.

I2S\_TX\_START bit of register I2S\_CONF\_REG is used to enable transmission. When I2S is in master transmitting mode and this bit is set, the module will keep driving the clock signal and data of left and right channels. If FIFO sends out all the buffered data and there are no new data to shift, the last batch of data will be looped on the data line. When this bit is reset, master will stop driving clock and data lines. When I2S is configured to slave transmitting mode and this bit is set, the module will wait for the master BCK clock to enable a transmit operation.

The I2S\_RX\_START bit of register I2S\_CONF\_REG is used to enable a receive operation. When I2S is in master receiving mode and this bit is set, the module will keep driving the clock signal and sampling the input data stream until this bit is reset. If I2S is configured to slave receiving mode and this bit is set, the receiving module will wait for the master BCK clock to enable a receiving operation.


\subsection{I2S PDM}\label{I2SPDM}

As is shown in Figure \ref{Figure:i2s_arch}, ESP32 I2S0 allows for pulse density modulation (PDM), which enables fast conversion between pulse code modulation (PCM) and PDM signals.

The output clock of PDM is mapped to the I2S0*\_WS\_out signal. Its configuration is identical to I2S's BCK. Please refer to section \ref{The Clock of I2S Module}, "The Clock of I2S Module", for further details. The bit width for both received and transmitted I2S PCM signals is 16 bits.

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_pcm2pdm}}
    \caption{ PDM Transmitting Module }
    \label{Figure:i2s_pcm2pdm}
\end{figure}
The PDM transmitting module is used to convert PCM signals into PDM signals, as shown in Figure \ref{Figure:i2s_pcm2pdm}. HPF is a high-speed channel filter, and LPF is a low-speed channel filter. The PDM signal is derived from the PCM signal, after upsampling and filtering. Signal  I2S\_TX\_PDM\_HP\_BYPASS of register I2S\_PDM\_CONF\_REG can be set to bypass the HPF at the PCM input. Filter module group0 carries out the upsampling. If the frequency of the PDM signal is $f_{\textrm{pdm}}$ and the frequency of the PCM signal is $f_{\textrm{pcm}}$, the relation between $f_{\textrm{pdm}}$ and $f_{\textrm{pcm}}$ is given by:
$$f_{\textrm{pdm}} = 64×f_{\textrm{pcm}}×\frac{ I2S\_TX\_PDM\_FP }{I2S\_TX\_PDM\_FS}$$
The upsampling factor of 64 is the result of the two upsampling stages.

Table \ref{table:过采样率配置} lists the configuration rates of the I2S\_TX\_PDM\_FP bit and the I2S\_TX\_PDM\_FS bit of register I2S\_PDM\\\_FREQ\_CONF\_REG, whose output PDM signal frequency remains 48×128 KHz at different PCM signal frequencies.

\begin{table}[H]
    \centering
    \caption{Upsampling Rate Configuration}
    \label{table:过采样率配置}
 \begin{tabular}{|p{3cm}|p{4.5cm}|p{4.5cm}|p{3cm}|}
    \hline
     \rowcolor{lightgray}
    $f_{\textrm{pcm}}$ (KHz)&I2S\_TX\_PDM\_FP & I2S\_TX\_PDM\_FS&$f_{\textrm{pdm}}$ (KHz)\\\hline
    48 	&960	&480&\multirow{6}*{48×128}\\
    \cline{1-3}
    44.1	&960	&441&\\
    \cline{1-3}
    32	&960	&320&\\
    \cline{1-3}
    24&960	&240&\\
    \cline{1-3}
    16	&960	&160&\\
    \cline{1-3}
    8	&960	&80 &\\
	\hline
    \end{tabular}
\end{table}

The I2S\_TX\_PDM\_SINC\_OSR2 bit of I2S\_PDM\_CONF\_REG is the upsampling rate of the Filter group0.
$$I2S\_TX\_PDM\_SINC\_OSR2 = \left\lfloor\frac{ I2S\_TX\_PDM\_FP }{I2S\_TX\_PDM\_FS}\right\rfloor$$

As is shown in Figure \ref{Figure:i2s_pdm_tx}, the I2S\_TX\_PDM\_EN bit and the I2S\_PCM2PDM\_CONV\_EN bit of register I2S\_PDM\_\\CONF\_REG should be set to 1 to use the PDM sending module. The I2S\_TX\_PDM\_SIGMADELTA\_IN\_SHIFT bit, I2S\_TX\_PDM\_SINC\_IN\_SHIFT bit, I2S\_TX\_PDM\_LP\_IN\_SHIFT bit and I2S\_TX\_PDM\_HP\_IN\_SHIFT bit of register I2S\_PDM\_CONF\_REG are used to adjust the size of the input signal of each filter module.

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.4\textwidth]{./22-I2S/figures/i2s_pdm_tx}}
    \caption{PDM Sends Signal}
    \label{Figure:i2s_pdm_tx}
\end{figure}

As is shown in Figure \ref{Figure:i2s_pdm_rx}, the I2S\_RX\_PDM\_EN bit and the I2S\_PDM2PCM\_CONV\_EN bit of register I2S\_PDM\_\\CONF\_REG should be set to 1, in order to use the PDM receiving module. As is shown in Figure \ref{Figure:i2s_pdm2pcm}, the PDM receiving module will convert the received PDM signal into a 16-bit PCM signal. Filter group1 is used to downsample the PDM signal, and the I2S\_RX\_PDM\_SINC\_DSR\_16\_EN bit of register I2S\_PDM\_CONF\_REG is used to adjust the corresponding down-sampling rate.

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.4\textwidth]{./22-I2S/figures/i2s_pdm_rx}}
    \caption{ PDM Receives Signal }
    \label{Figure:i2s_pdm_rx}
\end{figure}

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.5\textwidth]{./22-I2S/figures/i2s_pdm2pcm}}
    \caption{PDM Receive Module }
    \label{Figure:i2s_pdm2pcm}
\end{figure}
Table \ref{table:下采样配置} shows the configuration of the I2S\_RX\_PDM\_SINC\_DSR\_16\_EN bit whose PCM signal frequency remains 48 KHz at different PDM signal frequencies.

\begin{table}[H]
    \centering
    \caption{Down-sampling Configuration}
    \label{table:下采样配置}
    \begin{tabular}{|p{4cm}|p{7cm}|p{4cm}|}
    \hline
    \rowcolor{lightgray}
    PDM freq (KHz)&I2S\_RX\_PDM\_SINC\_DSR\_16\_EN&PCM freq (KHz)\\
    \hline
     $f_{\textrm{pcm}}$×128&1&\multirow{2}*{$f_{\textrm{pcm}}$ }\\
    \cline{1-2}
    $f_{\textrm{pcm}}$×64&0&\\
    \hline
    \end{tabular}
\end{table}
\hypertarget{camlcdctrl}{}
\section{Camera-LCD Controller}
\label{LCDMODE}
There are three operational modes in the LCD mode of ESP32 I2S:
\begin{itemize}
    \item LCD master transmitting mode
    \item Camera slave receiving mode
    \item ADC/DAC mode
\end{itemize}
The clock configuration of the LCD master transmitting mode is identical to I2S' clock configuration. In the LCD mode, the frequency of WS is half of $f\mathrm{_{BCK}}$.

In the ADC/DAC mode, use PLL\_F160M\_CLK as the clock source. 

\subsection{LCD Master Transmitting Mode}
 As is shown in Figure \ref{Figure:i2s_LCD}, the WR signal of LCD connects to the WS signal of I2S. The LCD data bus width is 24 bits.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{./22-I2S/figures/i2s_LCD}
    \caption{LCD Master Transmitting Mode}
    \label{Figure:i2s_LCD}
\end{figure}

The I2S\_LCD\_EN bit of register I2S\_CONF2\_REG needs to be set and the I2S\_TX\_SLAVE\_MOD bit of register I2S\_CONF\_REG needs to be cleared, in order to configure I2S to the LCD master transmitting mode. Meanwhile, data should be sent under the correct mode, according to the I2S\_TX\_CHAN\_MOD[2:0] bit of register I2S\_CONF\_CHAN\_REG and the I2S\_TX\_FIFO\_MOD[2:0] bit of register I2S\_FIFO\_CONF\_REG. The WS signal needs to be inverted when it is routed through the GPIO Matrix. For details, please refer to the chapter about \hyperref[mod:iomuxgpio]{IO\_MUX and the GPIO Matrix}. The I2S\_LCD\_TX\_SDX2\_EN bit and the I2S\_LCD\_TX\_WRX2\_EN bit of register I2S\_CONF2\_REG should be set to the LCD master transmitting mode, so that both the data bus and WR signal work in the appropriate mode.


\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_lcd_frame_a}}
    \caption{LCD Master Transmitting Data Frame, Form 1}
    \label{Figure:i2s_lcd_frame_a}
\end{figure}
\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.6\textwidth]{./22-I2S/figures/i2s_lcd_frame_b}}
    \caption{LCD Master Transmitting Data Frame, Form 2}
    \label{Figure:i2s_lcd_frame_b}
\end{figure}

As is shown in Figure \ref{Figure:i2s_lcd_frame_a} and Figure \ref{Figure:i2s_lcd_frame_b}, the I2S\_LCD\_TX\_WRX2\_EN bit should be set to 1 and the I2S\_LCD\_TX\_SDX2\_EN bit should be set to 0 in the data frame, form 1. Both I2S\_LCD\_TX\_SDX2\_EN bit and I2S\_LCD\_TX\_WRX2\_EN bit are set to 1 in the data frame, form 2.

\subsection{Camera Slave Receiving Mode}

ESP32 I2S supports a camera slave mode for high-speed data transfer from external camera modules. As shown in Figure \ref{Figure:i2s_camera}, in this mode, I2S is set to slave receiving mode. Besides the 16-channel data signal bus I2S\textcolor{red}{\it{n}}I\_Data\_in, there are other signals, such as I2S\textcolor{red}{\it{n}}\_H\_SYNC, I2S\textcolor{red}{\it{n}}\_V\_SYNC and I2S\textcolor{red}{\it{n}}\_H\_ENABLE.

The PCLK in the Camera module connects to I2S\textcolor{red}{\it{n}}I\_WS\_in in the I2S module, as Figure \ref{Figure:i2s_camera} shows.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{./22-I2S/figures/i2s_camera}
    \caption{Camera Slave Receiving Mode}
    \label{Figure:i2s_camera}
\end{figure}

When I2S is in the camera slave receiving mode, and when I2S\textcolor{red}{\it{n}}\_H\_SYNC, I2S\_V\_SYNC and I2S\_H\_REF are held high, the master starts transmitting data, that is, $$transmission\_start= (  I2S\textcolor{red}{\it{n}}\_H\_SYNC==1) \&\& (  I2S\textcolor{red}{\it{n}}\_V\_SYNC==1 ) \&\& ( I2S\textcolor{red}{\it{n}}\_H\_ENABLE==1 ) $$
Thus, during data transmission, these three signals should be kept at a high level. For example, if the I2S\textcolor{red}{\it{n}}\_V\_SYNC signal of a camera is at low level during data transmission, it will be inverted when routed to the I2S module. ESP32 supports signal inversion through the GPIO matrix. For details, please refer to the chapter about \hyperref[mod:iomuxgpio]{IO\_MUX and the GPIO Matrix}.

In order to make I2S work in camera mode, the I2S\_LCD\_EN bit and the I2S\_CAMERA\_EN bit of register I2S\_CONF2\\\_REG are set to 1, the I2S\_RX\_SLAVE\_MOD bit of register I2S\_CONF\_REG is set to 1, the I2S\_RX\_MSB\_RIGHT bit and the I2S\_RX\_RIGHT\_FIRST bit of I2S\_CONF\_REG are set to 0. Thus, I2S works in the LCD slave receiving mode. At the same time, in order to use the correct mode to receive data, both the I2S\_RX\_CHAN\_MOD[2:0] bit of register I2S\_CONF\_CHAN\_REG and the I2S\_RX\_FIFO\_MOD[2:0] bit of register I2S\_FIFO\_CONF\_REG are set to 1.
\subsection{ADC/DAC mode}\label{sec:i2s-adc-dac}

In LCD mode, ESP32's ADC and DAC can receive data. When the I2S0 module connects to the on-chip ADC, the I2S0 module should be set to master receiving mode. Figure \ref{Figure:i2s_adc} shows the signal connection between the I2S0 module and the ADC.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{./22-I2S/figures/i2s_sar_adc}
    \caption{ADC Interface of I2S0}
    \label{Figure:i2s_adc}
\end{figure}

Firstly, the I2S\_LCD\_EN bit of register I2S\_CONF2\_REG is set to 1, and the I2S\_RX\_SLAVE\_MOD bit of register I2S\_CONF\_REG is set to 0, so that the I2S0 module works in LCD master receiving mode, and the I2S0 module clock is configured such that the WS signal of I2S0 outputs an appropriate frequency. Then, the SYSCON\_SAR\\ADC\_DATA\_TO\_I2S bit of register SYSCON\_APB\_SARADC\_CTRL\_REG is set to 1. Enable I2S to receive data after configuring the relevant registers of SARADC. For details, please refer to Chapter \hyperref[mod:sensor]{On-Chip Sensors and Analog Signal Processing}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{./22-I2S/figures/i2s_dac}
    \caption{DAC Interface of I2S}
    \label{Figure:i2s_dac}
\end{figure}

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.7\textwidth]{./22-I2S/figures/i2s_dac_data}}
    \caption{Data Input by I2S DAC Interface}
    \label{Figure:i2s_dac_data}
\end{figure}

The I2S0 module should be configured to master transmitting mode when it connects to the on-chip DAC. Figure \ref{Figure:i2s_dac} shows the signal connection between the I2S0 module and the DAC. The DAC's control module regards I2S\_CLK as the clock in this configuration.
As shown in Figure \ref{Figure:i2s_dac_data}, when the data bus inputs data to the DAC's control module, the latter will input right-channel data to DAC1 module and left-channel data to DAC2 module. When using the I2S DMA module, 8 bits of data-to-be-transmitted are shifted to the left by 8 bits of data-to-be-received into the DMA double-byte type of buffer.

The I2S\_LCD\_EN bit of register I2S\_CONF2\_REG should be set to 1, while I2S\_RX\_SHORT\_SYNC, I2S\_TX\_SHORT\\\_SYNC, I2S\_CONF\_REG , I2S\_RX\_MSB\_SHIFT and I2S\_TX\_MSB\_SHIFT should all be reset to 0. The I2S\_TX\_\\SLAVE\_MOD bit of register I2S\_CONF\_REG should be set to 0, as well, when using the DAC mode of I2S0. Select a suitable transmit mode according to the standards of transmitting a 16-bit digital data stream. Configure the I2S0 module clock to output a suitable frequency for the I2S\_CLK and the WS of I2S. Enable I2S0 to send data after configuring the relevant DAC registers.

\section{I2S Interrupts}
\subsection{FIFO Interrupts}
\begin{itemize}
\item \label{int:I2STXHUNGINT}I2S\_TX\_HUNG\_INT: Triggered when transmitting data is timed out.
\item \label{int:I2SRXHUNGINT}I2S\_RX\_HUNG\_INT: Triggered when receiving data is timed out.
\item \label{int:I2STXREMPTYINT}I2S\_TX\_REMPTY\_INT: Triggered when the transmit FIFO is empty.
\item \label{int:I2STXWFULLINT}I2S\_TX\_WFULL\_INT: Triggered when the transmit FIFO is full.
\item \label{int:I2SRXREMPTYINT}I2S\_RX\_REMPTY\_INT: Triggered when the receive FIFO is empty.
\item \label{int:I2SRXWFULLINT}I2S\_RX\_WFULL\_INT: Triggered when the receive FIFO is full.
\item \label{int:I2STXPUTDATAINT}I2S\_TX\_PUT\_DATA\_INT: Triggered when the transmit FIFO is almost empty.
\item \label{int:I2SRXTAKEDATAINT}I2S\_RX\_TAKE\_DATA\_INT: Triggered when the receive FIFO is almost full.
\end{itemize}

\subsection{DMA Interrupts}\label{DMA Interrupts}

\begin{itemize}
\item \label{int:I2SOUTTOTALEOFINT}I2S\_OUT\_TOTAL\_EOF\_INT: Triggered when all transmitting linked lists are used up.
\item \label{int:I2SINDSCREMPTYINT}I2S\_IN\_DSCR\_EMPTY\_INT: Triggered when there are no valid receiving linked lists left.
\item \label{int:I2SOUTDSCRERRINT}I2S\_OUT\_DSCR\_ERR\_INT: Triggered when invalid txlink descriptors are encountered.
\item \label{int:I2SINDSCRERRINT}I2S\_IN\_DSCR\_ERR\_INT: Triggered when invalid rxlink descriptors are encountered.
\item \label{int:I2SOUTEOFINT}I2S\_OUT\_EOF\_INT: Triggered when txlink has finished sending a packet.
\item \label{int:I2SOUTDONEINT}I2S\_OUT\_DONE\_INT: Triggered when all transmitted and buffered data have been read.
\item \label{int:I2SINSUCEOFINT}I2S\_IN\_SUC\_EOF\_INT: Triggered when all data have been received.
\item \label{int:I2SINDONEINT}I2S\_IN\_DONE\_INT: Triggered when the current rxlink descriptor is handled.
\end{itemize}

\hypertarget{i2s-reg-summ}{}
\section{Register Summary}\label{sec:i2s-reg-summ}

The addresses in this section are relative to the I2S base address provided in Table \ref{table:Peripheral_Address_Mapping} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.

The abbreviations given in Column \textbf{Access} are explained in Section \hyperref[glossary-access-types]{\textit{Access Types for Registers}}.


\subfile{\modulefiles/i2s_reg-summary_en.tex}
{\clearpage}
\section{Registers}\label{I2SRegister}

The addresses in this section are relative to the I2S base address provided in Table \ref{table:Peripheral_Address_Mapping} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}. The absolute register addresses are listed in Section \ref{sec:i2s-reg-summ} \textit{\nameref{sec:i2s-reg-summ}}.

For how to program reserved fields, please refer to Section \hyperref[programming-reserved-field]{\textit{Programming Reserved Register Field}}.


\subfile{\modulefiles/i2s_reg_en.tex}
\end{document}
