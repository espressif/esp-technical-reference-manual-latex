

\begin{register}{H}{DMABUSMODE\_REG}{0x0000}\label{regdesc:DMABUSMODEREG}
\regfield{(reserved)}{5}{27}{00000}%
\regfield{DMAMIXEDBURST}{1}{26}{0}%
\regfield{DMAADDRALIBEA}{1}{25}{0}%
\regfield{PBLX8\_MODE}{1}{24}{0}%
\regfield{USE\_SEP\_PBL}{1}{23}{0}%
\regfield{RX\_DMA\_PBL}{6}{17}{{0x01}}%
\regfield{FIXED\_BURST}{1}{16}{0}%
\regfield{PRI\_RATIO}{2}{14}{{0x0}}%
\regfield{PROG\_BURST\_LEN}{6}{8}{{0x01}}%
\regfield{ALT\_DESC\_SIZE}{1}{7}{0}%
\regfield{DESC\_SKIP\_LEN}{5}{2}{{0x00}}%
\regfield{DMA\_ARB\_SCH}{1}{1}{0}%
\regfield{SW\_RST}{1}{0}{1}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [DMAMIXEDBURST]当此位置为高电平且 FB(FIXED\_BURST) 位为低电平时，AHB 主机接口将使用 INCR（未定义 burst）开始所有长度超过 16 的突发模式，而突发长度为 16 或少于 16 的 AHB 主接口适用固定突发传输（INCRx 和 SINGLE）。（读/写）
\item [DMAADDRALIBEA]当此位置高且 FB 位为 1 时，AHB 接口将产生与起始地址 LS 位对齐的所有突发。如果 FB 位等于 0，则第一个突发（访问数据缓冲区的起始地址）未对齐，但随后的突发与地址对齐。（读/写）
\item [PBLX8\_MODE] 置为高电平时，该位将编程的 PBL(PROG\_BURST\_LEN) 值（Bit [22:17] 和 Bit [13：8]）乘以 8。因此，DMA 根据 PBL 值以 8，16，32，64，128 和 256 节拍传输数据。（读/写）
\item [USE\_SEP\_PBL]该位置位时，Rx DMA 使用 Bit [22:17] 中配置的值作为 PBL。Bit [13：8] 中的 PBL 值仅适用于 Tx DMA 操作。当复位为低电平时，Bit [13：8] 中的 PBL 值适用于两个 DMA 引擎。（读/写）
\item [RX\_DMA\_PBL] 该字段表示在一个 Rx DMA 传输中传送的最大节拍数。 这是在单次读或写的最大值。每次在主机总线上开始突发传输时，Rx DMA 总是会尝试按照 RPBL 位的值进行传输。RPBL(RX\_DMA\_PBL) 值可配置为 1，2，4，8，16 和 32。任何其他值都会导致未定义的行为。该字段仅在 USP(USE\_SEP\_PBL) 置为高时有效。（读/写）
\item [FIXED\_BURST] 该位决定 AHB 主接口是否执行固定突发传输。置 1 时，在正常突发传输开始时，AHB 接口只使用 SINGLE，INCR4，INCR8 或 INCR16 模式。复位时，AHB 接口使用 SINGLE 和 INCR 突发传输操作。（读/写）
\item [PRI\_RATIO]这些位控制 Rx DMA 和 Tx DMA 之间的加权调度中的优先级比率。只有当位 1（DA）复位时，这些位才有效。Rx 与 Tx 优先级比如下：（读/写）
\begin{itemize}
\item 2'b00 —  1：1
\item 2'b01 —  2：0
\item 2'b10 —  3：1
\item 2'b11 —  4：1
\end{itemize}
\item [PROG\_BURST\_LEN]
这些位表示在一次 DMA 传输中要传送的最大节拍数。如果要传输的节拍数大于 32，则执行以下步骤：1. 设置 PBLx8 模式；2. 配置 PBL(PROG\_BURST\_LEN) 值。（读/写）
\item [寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}

\addtocounter{Regfloat}{-1}
\begin{register}{H}{DMABUSMODE\_REG}{0x0000}
\begin{regdesc}\begin{reglist}
\item [继上一页寄存器描述。]
\item [ALT\_DESC\_SIZE]置 1 时，描述符的大小增加到 32 个字节。（读/写）
\item [DESC\_SKIP\_LEN] 该位指定在两个未链接的描述符之间跳过的字的数量。地址从当前描述符结尾跳转到下一个描述符开始。当 DSL(DESC\_SKIP\_LEN) 值等于零时，描述符表在环模式下被 DMA 视为连续的。（读/写）
\item [DMA\_ARB\_SCH] 此位指定发送路径和接收路径之间的仲裁方案。1'b0：用 RX：TX 或 TX：RX 进行循环调度，PR（Bit [15:14]）指定优先级，1'b1 固定优先级（Rx 优先于 Tx）。（读/写）
\item [SW\_RST]当该位置 1 时，MAC DMA 控制器重置 MAC 的逻辑和所有 MAC 内部寄存器。在所有 ETH\_MAC 时钟域中的复位操作完成后，该位自动清零。在对 ETH\_MAC 的任何寄存器进行重新编程之前，应该在该位读取一个零值。（读/写设置/自动清除）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMATXPOLLDEMAND\_REG}{0x0004}\label{regdesc:DMATXPOLLDEMANDREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [TRANS\_POLL\_DEMAND]当这些位被写时，DMA 将读取寄存器（当前主机发送描述符寄存器）指向的当前描述符。如果该描述符不可用（由主机拥有），则传输返回挂起状态，并且寄存器（状态寄存器）的 Bit[2]（TU）被置 1。 如果描述符可用，则传输继续。 （只读/写触发）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMARXPOLLDEMAND\_REG}{0x0008}\label{regdesc:DMARXPOLLDEMANDREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [RECV\_POLL\_DEMAND]当这些位被写入时，DMA 将读取寄存器（当前主机接收描述符寄存器）指向的当前描述符。如果该描述符不可用（由主机拥有），则接收返回到暂停状态，并且状态寄存器的 Bit[7]（RU）被置 1。如果描述符可用，则 Rx DMA 返回到激活状态。（只读/写触发）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMARXBASEADDR\_REG}{0x000C}\label{regdesc:DMARXBASEADDRREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [START\_RECV\_LIST]该字段包含接收描述符列表中第一个描述符的基地址。DMA 的 LSB Bit[1:0] 被忽略，并被内部视为全零。因此，这些 LSB 位只读。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMATXBASEADDR\_REG}{0x0010}\label{regdesc:DMATXBASEADDRREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [START\_TRANS\_LIST]该字段包含发送描述符列表中第一个描述符的基地址。LSB Bit[1:0] 被忽略，在内部被 DMA 视为全零。因此，这些 LSB 位只读。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMASTATUS\_REG}{0x0014}\label{regdesc:DMASTATUSREG}

\regfield{(reserved)}{2}{30}{00}%
\regfield{(reserved)}{1}{29}{0}%
\regfield{EMAC\_PMT\_INT}{1}{28}{0}%
\regfield{(reserved)}{2}{26}{00}%
\regfield{ERROR\_BITS}{3}{23}{{0x0}}%
\regfield{TRANS\_PROC\_STATE}{3}{20}{{0x0}}%
\regfield{RECV\_PROC\_STATE}{3}{17}{{0x0}}%
\regfield{NORM\_INT\_SUMM}{1}{16}{0}%
\regfield{ABN\_INT\_SUMM}{1}{15}{0}%
\regfield{EARLY\_RECV\_INT}{1}{14}{0}%
\regfield{FATAL\_BUS\_ERR\_INT}{1}{13}{0}%
\regfield{(reserved)}{2}{11}{00}%
\regfield{EARLY\_TRANS\_INT}{1}{10}{0}%
\regfield{RECV\_WDT\_TO}{1}{9}{0}%
\regfield{RECV\_PROC\_STOP}{1}{8}{0}%
\regfield{RECV\_BUF\_UNAVAIL}{1}{7}{0}%
\regfield{RECV\_INT}{1}{6}{0}%
\regfield{TRANS\_UNDFLOW}{1}{5}{0}%
\regfield{RECV\_OVFLOW}{1}{4}{0}%
\regfield{TRANS\_JABBER\_TO}{1}{3}{0}%
\regfield{TRANS\_BUF\_UNAVAIL}{1}{2}{0}%
\regfield{TRANS\_PROC\_STOP}{1}{1}{0}%
\regfield{TRANS\_INT}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}

\item [EMAC\_PMT\_INT]该位表示 ETH\_MAC 的 PMT 模块中的中断事件。软件必须读取 MAC 中的 PMT 控制和状态寄存器才能得到确切的中断原因并清除其来源，将该位复位为 1'b0。（只读）

\item [ERROR\_BITS] 该字段指示导致总线错误的错误类型，例如 AHB 接口上的错误响应。该字段仅在 Bit[13]（FBI）置 1 时有效。该字段不会生成中断。（只读）\begin{itemize}
    \item 3'b000：Rx DMA 写数据传输期间出错。   \item3'b011：Tx DMA 读数据传输期间出错。  \item3'b100：读 Rx DMA 描述符期间出错。  \item3'b101：写 Tx DMA 描述符期间出错。  \item3'b110：读取 Rx DMA 描述符期间出错。  \item3'b111：读取 Tx DMA 描述符期间出错。
    \end{itemize}
\item [TRANS\_PROC\_STATE]该字段表示发送 DMA FSM 状态。该字段不会生成中断。
\begin{itemize}
    \item 3'b000：停止：复位或停止发出的发送命令。 \item 3'b001：运行：获取发送传输描述符。
    \item 3'b010：保留。 \item 3'b011：正在运行：正在等待发送数据包。 \item 3'b100：暂停：发送描述符不可用。\item 3'b101：运行：关闭发送描述符。\item 3'b110：保留。 \item 3'b111：运行：将发送数据包数据从发送缓存传输到主机内存。\end{itemize}

    \item [寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}

\addtocounter{Regfloat}{-1}
\begin{register}{H}{DMASTATUS\_REG}{0x0014}
\begin{regdesc}\begin{reglist}
\item [继上一页寄存器描述。]

\item [RECV\_PROC\_STATE] 该字段表示接收 DMA FSM 状态。该字段不会生成中断。（只读）
\begin{itemize}
\item 3'b000：停止：复位或停止发出的接收命令。 \item 3'b001：运行：获取接收传输描述符。\item 3'b010：保留。 \item 3'b011：正在运行：正在等待接收数据包。 \item 3'b100：暂停：接收描述符不可用。\item 3'b101：运行：关闭接收描述符。\item 3'b110：保留。 \item 3'b111：运行：将接收数据包数据从接收缓存传输到主机内存。\end{itemize}

\item [NORM\_INT\_SUMM] 当使能中断使能寄存器中的相应中断时，正常中断集合位的值是以下位的逻辑或：

\begin{itemize}
\item Bit[0]：发送中断。\item Bit[2]：发送缓存不可用。\item Bit[6]：接收中断。\item  Bit[14]：提前接收中断。\end{itemize}只有未被屏蔽的位会影响正常中断集合位。这是一个粘滞位，必须在每次引起 NIS 置位的相应位清零（通过向该位写入 1）被清除。（读/自动设置/写清除）

\item [ABN\_INT\_SUMM] 当中断使能寄存器中的对应中断使能时，非正常中断集合位的值是以下逻辑或：

\begin{itemize}
\item Bit[1]：发送进程已停止。 \item Bit[3]：传送 Jabber 超时。 \item Bit[4]：接收 FIFO 溢出。 \item Bit[5]：传输下溢。 \item Bit[7]：接收缓冲器不可用。 \item Bit[8]：接收进程已停止。 \item Bit[9]：接收看门狗超时。 \item Bit[10]：提前发送中断。 \item Bit[13]：严重总线错误。\end{itemize}只有未被屏蔽的位会影响非正常中断集合位。这是一个粘滞位，必须在每次引起 AIS 置位的相应位清零时（向该位写入 1）清除。（读/自动设置/写清除）

\item [寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}

\addtocounter{Regfloat}{-1}
\begin{register}{H}{DMASTATUS\_REG}{0x0014}
\begin{regdesc}\begin{reglist}\item [继上一页寄存器描述。]

\item [EARLY\_RECV\_INT]该位表示 DMA 填充了数据包的第一个数据缓冲区。当软件向该位写入 1 或该寄存器的 Bit[6]（RI）置 1 时，该位清零。（读/自动设置/写清除）
\item [FATAL\_BUS\_ERR\_INT]该位表示发生 Bit[25:23] 所述的总线错误。当该位置 1 时，相应的 DMA 引擎关闭其所有的总线访问。（读/自动设置/写清除）
\item [EARLY\_TRANS\_INT]该位表示要发送的帧被完全传送到 MTL 发送 FIFO。（读/自动设置/写清除）
\item [RECV\_WDT\_TO]置 1 表示接收看门狗定时器在接收到当前帧时已经过期，当前帧在看门狗超时后被截断。（读/自动设置/写清除）
\item [RECV\_PROC\_STOP]接收进程进入停止状态时，该位置 1。（读/自动设置/写清除）
\item [RECV\_BUF\_UNAVAIL] 该位表示主机拥有接收列表中的下一个描述符，并且 DMA 无法获取该描述符。接收过程被暂停。要恢复处理接收描述符，主机应更改描述符的所有权并发出“接收轮询需求”命令。如果没有收到轮询请求，则接收进程在收到下一个识别的传入帧时恢复。只有当 DMA 拥有前一个接收描述符时，该位才被置 1。（读/自动设置/写清除）
\item [RECV\_INT] 该位表示帧接收已完成。 当接收完成时，RDES1 的 Bit[31] 在最后的描述符中被复位，并且特定的帧状态信息在描述符中被更新。接收处于运行状态。（读/自动设置/写清除）
\item [TRANS\_UNDFLOW]该位表示发送缓存在帧传输期间具有下溢。发送暂停，并将下溢错误位 TDES0[1] 置 1。（读/自动设置/写清除）
\item [RECV\_OVFLOW]该位表示接收缓存在帧接收过程中有溢出。如果部分帧被传送到应用程序，则溢出状态将在 RDES0 [11] 中置 1。（读/自动设置/写清除）
\item [TRANS\_JABBER\_TO]该位表示发送 Jabber 定时器超时，当帧大小超过 2048 字节（当 Jumbo 帧被使能时为 10,240 字节）时发生。当发生 Jabber 超时时，传输过程被中止并进入停止状态。这会导致发送 Jabber 超时 TDES0 [14] 标志置 1。（读/自动设置/写清除）
\item [TRANS\_BUF\_UNAVAIL] 该位表示主机在发送列表中拥有下一个描述符，并且 DMA 无法获取它。传输被暂停。Bit[22:20] 表示发送进程状态转换。要恢复处理传输描述符，主机应通过设置 TDES0 [31] 来更改描述符的所有权，然后发出传输轮询需求命令。（读/自动设置/写清除）
\item [TRANS\_PROC\_STOP]传输停止时该位被置 1。（读/自动设置/写清除）
\item [TRANS\_INT]该位表示帧传输已完成。传输完成后，TDES0 的 Bit[31]（OWN）复位，并在描述符中更新特定的帧状态信息。（读/自动设置/写清除）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMAOPERATION\_MODE\_REG}{0x0018}\label{regdesc:DMAOPERATION_MODEREG}
\regfield{(reserved)}{5}{27}{00000}%
\regfield{DIS\_DROP\_TCPIP\_ERR\_FRAM}{1}{26}{0}%
\regfield{RX\_STORE\_FORWARD}{1}{25}{0}%
\regfield{DIS\_FLUSH\_RECV\_FRAMES}{1}{24}{0}%
\regfield{(reserved)}{1}{23}{0}%
\regfield{(reserved)}{1}{22}{0}%
\regfield{TX\_STR\_FWD}{1}{21}{0}%
\regfield{FLUSH\_TX\_FIFO}{1}{20}{0}%
\regfield{(reserved)}{3}{17}{0}%
\regfield{TX\_THRESH\_CTRL}{3}{14}{0}%
\regfield{START\_STOP\_TRANSMISSION\_COMMAND}{1}{13}{0}%
\regfield{(reserved)}{2}{11}{0}%
\regfield{(reserved)}{2}{9}{0}%
\regfield{(reserved)}{1}{8}{0}%
\regfield{FWD\_ERR\_FRAME}{1}{7}{0}%
\regfield{FWD\_UNDER\_GF}{1}{6}{0}%
\regfield{DROP\_GFRM}{1}{5}{0}%
\regfield{RX\_THRESH\_CTRL}{2}{3}{0}%
\regfield{OPT\_SECOND\_FRAME}{1}{2}{0}%
\regfield{START\_STOP\_RX}{1}{1}{0}%
\regfield{(reserved)}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [DIS\_DROP\_TCPIP\_ERR\_FRAM] 置 1 时，ETH\_MAC 不会丢弃 checksum 错误的包。置 1 时，并且 FWD\_ERR\_FRAME 位为 0 时，所有错误的包都会被丢弃。（读/写）
\item [RX\_STORE\_FORWARD] 当此位为 1 时，MTL 层会直接把写到 RX FIFO 的包转发出去。（读/写）
\item [DIS\_FLUSH\_RECV\_FRAMES] 当此位为 1 时，RX DAM 不会冲刷掉因为链表或缓存取不可用的包。（读/写）
\item [TX\_STR\_FWD] 当此位为 1 时，并且整个发送的帧已经存储到 TX FIFO 中时，开始传送包。此时，TX\_THRESH\_CTRL 中的值被忽略。（读/写）
\item [FLUSH\_TX\_FIFO] 置 1 时，TX FIFO 中的所有值被清除为默认值。当清除完成后，此位自动清零。（读/写设置/自动清除）
\item [TX\_THRESH\_CTRL] 当 TX FIFO 中的值大于此既存器中的阈值值，开始发送帧。当整帧的大小小于这个阈值时，此帧也将被发送。仅仅 TX\_STR\_FWD 寄存器为 0 时，此仅寄存器才有效。阈值大小：3'b000：64；3'b001：128；3'b010：192；3'b011：256；3'b100：40；3'b101：32；3'b110：24；3'b111：1。（读/写）
\item [START\_STOP\_TRANSMISSION\_COMMAND] 当 TX FIFO 中的值大于此既存器中的阈值值，开始发送帧。当整帧的大小小于这个阈值时，此帧也将被发送。仅当 TX\_STR\_FWD 寄存器为 0 时，此寄存器才有效。阈值大小：000：64, 001：128, 010：192, 011：256, 100：40, 101：32, 110：24, 111：16。（读/写）
\item [FWD\_ERR\_FRAME] 当此位为 0 时，RX FIFO 会清除掉所有错误帧（CRC 错，冲突错，超大帧错，看门狗超时错，溢出错），否则会转发这些错误帧。（读/写）
\item [FWD\_UNDER\_GF] 当此位置 1 时，RX FIFO 会转发包括 PAD 和 CRC 的较小帧（长度小于 64 字节的正确帧），否则不会转发。
\item [DROP\_GFRM] 当此位置 1 时，ETH\_MAC 会丢掉 RX FIFO 中的超大帧。（读/写）

\item[寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}
\addtocounter{Regfloat}{-1}
\begin{register}{H}{DMAOPERATION\_MODE\_REG}{0x0018}
\begin{regdesc}\begin{reglist}
\item[继上一页寄存器描述。]

\item [RX\_THRESH\_CTRL] 2'b00：64，2'b01：32，2'b10：96，2'b11：128.
当 RX FIFO 中的值大于这个阈值时，会触发 DMA 开始接受此帧的数据到缓存中。2'b00：64；2'b01：32；2'b10：96；2'b11：128。
（读/写）
\item [OPT\_SECOND\_FRAME] 当此位置 1 时，在前一帧的传送状态被硬件获取之前 TX DMA 开始处理下一帧的数据。（读/写）
\item [START\_STOP\_RX] 置 1 时，RX DMA 开始接收数据，当此位为 0 时，RX DMA 接收完当前正在接收的帧之后停止接收新的帧。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{DMAIN\_EN\_REG}{0x001C}\label{regdesc:DMAINENREG}
\regfield{(reserved)}{15}{17}{000000000000000}%
\regfield{DMAIN\_NISE}{1}{16}{0}%
\regfield{DMAIN\_AISE}{1}{15}{0}%
\regfield{DMAIN\_ERIE}{1}{14}{0}%
\regfield{DMAIN\_FBEE}{1}{13}{0}%
\regfield{(reserved)}{2}{11}{00}%
\regfield{DMAIN\_ETIE}{1}{10}{0}%
\regfield{DMAIN\_RWTE}{1}{9}{0}%
\regfield{DMAIN\_RSE}{1}{8}{0}%
\regfield{DMAIN\_RBUE}{1}{7}{0}%
\regfield{DMAIN\_RIE}{1}{6}{0}%
\regfield{DMAIN\_UIE}{1}{5}{0}%
\regfield{DMAIN\_OIE}{1}{4}{0}%
\regfield{DMAIN\_TJTE}{1}{3}{0}%
\regfield{DMAIN\_TBUE}{1}{2}{0}%
\regfield{DMAIN\_TSE}{1}{1}{0}%
\regfield{DMAIN\_TIE}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [DMAIN\_NISE]该位置 1 时，使能正常中断集合。当该位复位时，关闭正常中断集合。该位使能状态寄存器中的以下中断：状态寄存器 Bit[0]：发送中断。状态寄存器 Bit[2]：发送缓存不可用。状态寄存器 Bit[6]：接收中断。状态寄存器 Bit[14]：早期接收中断。（读/写）
\item [DMAIN\_AISE]该位置 1 时，使能非正常中断汇总。 该位复位时，关闭非正常中断集合。该位使能状态寄存器中的以下中断：\begin{itemize}
\item 状态寄存器 Bit[1]：传送进程停止。
\item 状态寄存器 Bit[3]：传送 Jabber 超时。
\item 状态寄存器 Bit[4]：接收溢出。
\item 状态寄存器 Bit[5]：发送下溢。
\item 状态寄存器 Bit[7]：接收缓存区不可用。
\item 状态寄存器 Bit[8]：接收进程停止。
\item 状态寄存器 Bit[9]：接收看门狗超时。
\item 状态寄存器 Bit[10]：提前发送中断。
\item 状态寄存器 Bit[13]：严重的总线错误。（读/写）
\end{itemize}
\item [DMAIN\_ERIE] 当该位通过正常中断集合要使能位（Bit[16]）置 1 时，使能提前接收中断。当该位复位时，关闭提前接收中断。（读/写）
\item [DMAIN\_FBEE]当该位通过非正常中断集合使能（Bit[15]）置 1 时，使能严重总线错误中断。当该位复位时，关闭致命总线错误使能中断。（读/写）
\item [DMAIN\_ETIE] 当该位通过非正常中断集合使能位（Bit[15]）置 1 时，使能提前发送中断。 当该位复位时，关闭提前发送中断。（读/写）
\item [DMAIN\_RWTE] 当该位通过非正常中断集合使能位（Bit 15）置 1 时，接收看门狗超时中断使能。当该位复位时，关闭接收看门狗超时中断。（读/写）
\item [DMAIN\_RSE]当该位由非正常中断集合使能位（Bit[15]）置 1 时，使能接收停止中断。 当该位复位时，关闭接收停止中断。（读/写）
\item [DMAIN\_RBUE] 当该位通过非正常中断集合使能位（Bit[15]）置 1 时，使能接收缓存不可用中断。 当该位复位时，关闭接收缓存区不可用中断。（读/写）
\item [DMAIN\_RIE] 当该位通过正常中断集合使能位（Bit[16]）置 1 时，使能接收中断。 当该位复位时，关闭接收中断。（读/写）


\item[寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}
\addtocounter{Regfloat}{-1}
\begin{register}{H}{DMAIN\_EN\_REG}{0x001C}
\begin{regdesc}\begin{reglist}

\item[继上一页寄存器描述。]
\item [DMAIN\_UIE]当该位通过非正常中断集合使能位（Bit[15]）置 1 时，使能发送下溢中断。当该位复位时，关闭下溢中断。（读/写）
\item [DMAIN\_OIE]当此位通过非正常中断集合使能位（Bit[15]）置 1 时，使能接收溢出中断。 当该位复位时，关闭溢出中断。（读/写）
\item [DMAIN\_TJTE] 当该位通过异常中断集合使能位（Bit[15]）置 1 时，使能发送 Jabber 超时中断。当该位复位时，关闭发送 Jabber 超时中断。（读/写）
\item [DMAIN\_TBUE] 当该位通过正常中断集合使能位（Bit[16]）置 1 时，使能发送缓存不可用中断。当该位复位时，关闭发送缓存不可用中断。（读/写）
\item [DMAIN\_TSE] 当此位通过异常中断集合使能位（Bit[15]）置 1 时，使能发送停止中断。 当该位复位时，关闭发送停止中断。（读/写）
\item [DMAIN\_TIE] 当该位通过正常中断集合使能位（Bit[16]）置 1 时，使能发送中断。 当该位复位时，关闭发送中断。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMAMISSEDFR\_REG}{0x0020}\label{regdesc:DMAMISSEDFRREG}
\regfield{(reserved)}{3}{29}{00}%
\regfield{Overflow\_BFOC}{1}{28}{{0x0}}%
\regfield{Overflow\_FC}{11}{17}{{0x0}}%
\regfield{Overflow\_BMFC}{1}{16}{{0x0}}%
\regfield{Missed\_FC}{16}{0}{{0x0}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [Overflow\_BFOC] 当寄存器 Overflow\_FC (Bits[27:17]) 溢出时，此位置 1。也就是说 RX FIFO 溢 出，同时溢出帧计数器达到了最大值。在这种情况下，溢出帧计数器会复位成 0，此位表示发生溢出行为。（读/自动设置/读清除）
\item [Overflow\_FC] 此位表示溢出过后有多少个帧溢出丢失而没有上传给上层应用程序，MTL 的 FIFO 每溢出一次，这个计数器就会累加 1，每次读取该寄存器，计数器就会清除。（读/自动设置/读清除）
\item [Overflow\_BMFC] 当寄器 Missed\_FC (Bits[15:0]) 溢出时，此位置 1。也就是说此时应用端缓 存取不可用，同时丢失帧计数器达到了最大值。在这种情况下，丢失帧计数器会 复位成 0，此位表示发生计数器发生溢出行为。（读/自动设置/读清除）
\item [Missed\_FC] 此位表示接收端缓存区不可用，导致有多少个帧丢失而没有上传给上层应用程序，DMA 每丢弃一帧，这个计数器就会累加 1，每次读取该寄存器，计数器就会清除。（读/自动设置/读清除）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{DMARINTWDTIMER\_REG}{0x0024}\label{regdesc:DMARINTWDTIMERREG}
\regfield{(reserved)}{24}{8}{000000000000000000000000}%
\regfield{RIWTC}{8}{0}{{0x000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [RIWTC]该位表示系统时钟周期数乘以 256。在 Rx DMA 完成传送由于相应描述符 RDES1 [31] 中的设置而未设置 RI 状态位的帧后，看门狗定时器将以编程值触发。看门狗定时器计数停止后，RI 位置  1，定时器停止。当由于接收帧的 RDES1[31] 的 RI 设置，RI (RECV\_INT) 位被置为高电平时，看门狗定时器复位。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMATXCURRDESC\_REG}{0x0048}\label{regdesc:DMATXCURRDESCREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [TRANS\_DECR\_ADDR\_PTR]复位时清零。操作期间由 DMA 更新指针。当前发送链表的地址。（只读）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMARXCURRDESC\_REG}{0x004C}\label{regdesc:DMARXCURRDESCREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [RECV\_DECR\_ADDR\_PTR] 复位时清零。操作期间由 DMA 更新指针。当前接收链表的地址。（只读）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{DMATXCURRADDR\_BUF\_REG}{0x0050}\label{regdesc:DMATXCURRADDRBUFREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [TRANS\_BUFF\_ADDR\_PTR] 复位时清零。操作期间由 DMA 更新指针。当前发送链表的地址。（只读）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{DMARXCURRADDR\_BUF\_REG}{0x0054}\label{regdesc:DMARXCURRADDRBUFREG}
\regfield{}{32}{0}{{0x000000000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [RECV\_BUFF\_ADDR\_PTR] 复位时清零。操作期间由 DMA 更新指针。当前接收链表的地址。（只读）
\end{reglist}\end{regdesc}
\end{register}



\begin{register}{H}{EMACCONFIG\_REG}{0x1000}\label{regdesc:EMACCONFIGREG}
\regfield{(reserved)}{1}{31}{0}%
\regfield{SAIRC}{3}{28}{{0x0}}%
\regfield{ASS2KP}{1}{27}{0}%
\regfield{(reserved)}{3}{24}{000}%
\regfield{EMACWATCHDOG}{1}{23}{0}%
\regfield{EMACJABBER}{1}{22}{0}%
\regfield{(reserved)}{1}{21}{0}%
\regfield{EMACJUMBOFRAME}{1}{20}{0}%
\regfield{EMACINTERFRAMEGAP}{3}{17}{0}%
\regfield{EMACDISABLECRS}{1}{16}{0}%
\regfield{EMACMII}{1}{15}{0}%
\regfield{EMACFESPEED}{1}{14}{0}%
\regfield{EMACRXOWN}{1}{13}{0}%
\regfield{EMACLOOPBACK}{1}{12}{0}%
\regfield{EMACDUPLEX}{1}{11}{0}%
\regfield{EMACRXIPCOFFLOAD}{1}{10}{0}%
\regfield{EMACRETRY}{1}{9}{0}%
\regfield{(reserved)}{1}{8}{0}%
\regfield{EMACPADCRCSTRIP}{1}{7}{0}%
\regfield{EMACBACKOFFLIMIT}{2}{5}{{0x0}}%
\regfield{EMACDEFERRALCHECK}{1}{4}{0}%
\regfield{EMACTX}{1}{3}{0}%
\regfield{EMACRX}{1}{2}{0}%
\regfield{PLTF}{2}{0}{{0x0}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [SAIRC]该字段控制所有传输帧的源地址插入或替换。Bit[30] 根据 Bit[29:28] 的值指定将哪个 MAC 地址寄存器（0 或 1）用于源地址插入或替换。（读/写）
\begin{itemize}
\item 2'b0x：输入信号 mti\_sa\_ctrl\_i 和 ati\_sa\_ctrl\_i 控制 SA 字段的生成。
\item 2'b10：如果将 Bit[30] 置 0，则 MAC 将 MAC 地址 0 寄存器的内容插入到所有传输帧的 SA 字段中。如果将 Bit[30] 置 1，则 MAC 将 MAC 地址 1 寄存器的内容插入所有传输帧的 SA 字段。
\item 2'b11：如果将 Bit[30] 置 0，则 MAC 替换所有传输帧的 SA 字段中的 MAC 地址 0 寄存器的内容。如果将 Bit[30] 置 1，则 MAC 替换所有传输帧的 SA 字段中的 MAC 地址 1 寄存器的内容。
\end{itemize}
\item [ASS2KP]置 1 时，MAC 将所有长度不超过 2,000 个字节的帧视为普通数据包。当 Bit[20]（JE）未置 1 时，MAC 将所有接收到的大于 2K 字节的帧视为巨帧。当此位复位且 Bit[20]（JE）未置 1 时，MAC 将所有收到的大小超过 1,518 字节（标记为 1,522 字节）的帧视为巨帧。当 Bit[20] 置 1 时，该位对巨帧状态没有影响。（读/写）
\item [EMACWATCHDOG]当该位置 1 时，MAC 关闭接收器上的看门狗定时器。MAC 可以接收最多 16,383 字节的帧。当该位复位时，MAC 不接收超过 2048 字节（如果 JE 位置高，则为 10,240 字节）的接收帧或看门狗超时寄存器中设置的值。MAC 切断超出看门狗限制范围之后接收到的任何字节数。（读/写）
\item [EMACJABBER]当该位置 1 时，MAC 关闭发送器上的 Jabber 定时器。MAC 可以传输长达 16,383 字节的帧。 当此位复位时，如果应用程序在传输过程中发送了超过 2048 字节（如果 JE 设置为高电平，则为 10240 字节）的数据，MAC 将切断发送器。（读/写）
\item [EMACJUMBOFRAME]当该位置 1 时，MAC 允许 9,018 字节的巨帧（或 9,022 字节的 VLAN 标记帧），而不在接收帧状态中报告巨大帧错误。（读/写）
\item [EMACINTERFRAMEGAP]这些位控制传输时帧间的最小 IFG。（读/写）
\begin{itemize}
\item 3'b000：96 比特时间。\item 3'b001：88 比特时间。\item 3'b010：80 比特时间。\item 3'b111：40 比特时间。
在半双工模式下，最小的 IFG 为 64 比特时间（IFG = 100）。\end{itemize}
\item[寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}
\addtocounter{Regfloat}{-1}
\begin{register}{H}{EMACCONFIG\_REG}{0x1000}
\begin{regdesc}\begin{reglist}
\item[继上一页寄存器描述。]
\item [EMACDISABLECRS]当该位置高时，MAC 发送器在半双工模式下的帧发送期间忽略 MII CRS 信号。 此请求不会导致在传输过程中由于载波丢失或无载波而产生的错误。 当此位为低时，MAC 发送器由于载波侦听而产生这样的错误，甚至可以中止传输。 （读/写）
\item [EMACMII] 该位选择以太网信号线速度。对于 10 或 100 Mbps 操作，该位置 1。在 10 或 100 Mbps 操作中，此位与 FES(EMACFESPEED) 位一起选择信号线速率。（读/写）
\item [EMACFESPEED] 该位选择 MII，RMII 接口速度。0：10 Mbps；1：100 Mbps。（读/写）
\item [EMACRXOWN]该位置 1 时，在半双工模式下当 TX\_EN 触发中断时，MAC 关闭接收帧。该位复位时，MAC 将在传输时接收 PHY 发出的所有数据包。MAC 处于全双工模式时，该位不适用。（读/写）
\item [EMACLOOPBACK]该位置 1 时，MAC 使用回调模式 MII。MII 接收时钟输入信号（CLK\_RX）是环回正常工作所必需的，因为发送时钟在内部没有环回。（读/写）
\item [EMACDUPLEX]该位置 1 时，MAC 工作在全双工模式下，可以同时发送和接收数据。 该位在全双工模式下为 RO，默认值为 1'b1。（读/写）
\item [EMACRXIPCOFFLOAD]
当该位置 1 时，MAC 计算所有接收的以太网帧有效载荷的 16 位的补码和，再对相加和取补。它还检查 IPv4 报头校验和（接收到的以太网帧的字节 25/26 或 29/30（VLAN 标记）是否对接收帧是正确的，并在接收状态字中给出状态。MAC 还将 IP 报头数据报有效负载（IPv4 报头后的字节）16 位校验和附加到应用程序的以太网帧当中（当取消选择类型 2 COE 时）。该位复位时，该功能被禁止。（读/写）
\item [EMACRETRY]当该位置 1 时，MAC 只尝试一次传输。当 MII 接口上发生冲突时，MAC 将忽略当前帧传输，并在传输帧状态中报告由于出现过多冲突错误的帧终止。当该位复位时，MAC 将根据 BL 字段（Bit [6:5]）的设置尝试重试。该位仅适用于半双工模式。（读/写）
\item [EMACPADCRCSTRIP]
当该位置 1 时，只有在长度字段的值小于 1,536 字节的情况下，MAC 才去除传入帧上的 Pad 或 FCS 字段。所有接收到的长度字段大于或等于 1,536 字节的帧将被传送到应用程序，而不去除 Pad 或 FCS 字段。当该位复位时，MAC 将所有接收的帧传递给主机，而不修改它们。（读/写）
\item [EMACBACKOFFLIMIT]
退避极限决定在发生冲突后，在重试时重新安排传输之前，MAC 等待的时隙时间延迟（10/100 Mbps 的 512 比特时间）的随机整数（r）。该位仅适用于半双工模式。00：k = min（n，10）。01：k = min（n，8）。10：k = min（n，4）。11：k = min（n，1），其中 n =重传尝试次数。随机整数 r 的取值范围为 0 <r <2k。（读/写）
\item [EMACDEFERRALCHECK]延期检查使能。（读/写）
\item[寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}
\addtocounter{Regfloat}{-1}
\begin{register}{H}{EMACCONFIG\_REG}{0x1000}
\begin{regdesc}\begin{reglist}
\item[继上一页寄存器描述。]

\item [EMACTX]当该位置 1 时，MAC 的发送状态机被启用在 MII 上发送。当该位被复位时，MAC 发送状态机在完成当前帧的发送之后被关闭，并且不发送任何其他帧。（读/写）
\item [EMACRX]当该位置 1 时，使能 MAC 的接收器状态机以接收来自 MII 的帧。当该位复位时，MAC 接收状态机在完成当前帧的接收后被关闭，并且不从 MII 接收任何其他帧。（读/写）
\item [PLTF] 这些位控制添加到每个发送帧的开始处的前导字节数量。只有在 MAC 出于全双工模式下时，才会减少报头字节数量。（读/写）\begin{itemize}
\item 2'b00：7 字节的报头。\item 2'b01：5 字节的报头。\item 2'b10：3 字节的报头。\end{itemize}
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACFF\_REG}{0x1004}\label{regdesc:EMACFFREG}
\regfield{RECEIVE\_ALL}{1}{31}{0}%
\regfield{(reserved)}{21}{10}{000000000000000000000}%
\regfield{SAFE}{1}{9}{0}%
\regfield{SAIF}{1}{8}{0}%
\regfield{PCF}{2}{6}{{0x0}}%
\regfield{DBF}{1}{5}{0}%
\regfield{PAM}{1}{4}{0}%
\regfield{DAIF}{1}{3}{0}%
\regfield{(reserved)}{2}{1}{00}%
\regfield{PMODE}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [RECEIVE\_ALL]当该位置 1 时，MAC 接收器模块将所有接收到的帧传送给应用程序，而不管它们是否通过地址过滤器。SA 或 DA 过滤的结果在接收状态字的相应位中更新（通过或失败）。当该位复位时，接收器模块仅将那些帧传递给通过 SA 或 DA 地址过滤器的应用程序。（读/写）
\item [SAFE]当该位置 1 时，MAC 将接收帧的 SA 字段与使能的 SA 寄存器中编程的值进行比较。如果比较失败，则 MAC 丢弃帧。当该位复位时，MAC 根据 SA 地址比较将接收到的帧转发到具有 Rx 状态的更新的 SAF 位的应用。（读/写）
\item [SAIF]当该位置 1 时，地址检查模块将在反向滤波模式下进行 SA 地址比较。SA 与 SA 寄存器匹配的帧被标记为未通过 SA地址过滤。当该位复位时，SA 与 SA 寄存器不匹配的帧被标记为未通过 SA 地址过滤。（读/写）
\item [PCF]这些位控制所有控制帧（包括单播和多播暂停帧）的转发。（读/写）\begin{itemize}
\item 2'b00：MAC 过滤掉所有控制帧。\item 2'b01：MAC 将除暂停帧之外的所有控制帧转发给应用程序，即使这些帧未通过地址过滤。\item 2'b10：即使 MAC 地址过滤器失败，MAC 也会将所有控制帧转发给应用程序。\item 2'b11：MAC 转发通过地址过滤器的控制帧。\end{itemize}
暂停帧的处理应符合以下条件：
\begin{itemize}
\item 条件 1：MAC 处于全双工模式，通过将流控制寄存器的位 2（RFE）置 1 来使能流控制。\item 条件 2：当流控制寄存器的 Bit 3（UP）置 1 时，接收到的帧的目的地址（DA）与特殊的多播地址或 EMACADDR0 匹配。\item 条件 3：接收帧的 Type 字段为 0x8808，OPCODE 字段为 0x0001。\end{itemize}
\item [DBF]此位置 1 后，AFM (Address Filtering Module) 模块会阻止所有传入的广播帧，并且所有其他过滤设置无效。当该位复位时，AFM 模块将传送所有接收到的广播帧。（读/写）
\item [PAM]当该位置 1 时，该位表示所有接收到的具有多播目的地址（目的地址字段中的第一位为 “1”）的帧都通过过滤。复位时，多播帧的过滤取决于 HMC 位。（读/写）
\item [DAIF]当该位置 1 时，地址校验模块将对单播和多播帧的 DA 地址比较进行反向过滤。复位时，执行帧的正常过滤。（读/写）
\item [PMODE]当该位置 1 时，地址过滤器模块将传送所有传入帧，而不管目的地址或源地址如何。PR (PRI\_RATIO) 置 1 时，总是清除接收状态字的 SA 或 DA 滤波器失败状态位。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACMIIADDR\_REG}{0x1010}\label{regdesc:EMACMIIADDRREG}
\regfield{(reserved)}{16}{16}{0000000000000000}%
\regfield{MIIDEV}{5}{11}{{0x00}}%
\regfield{MIIREG}{5}{6}{{0x00}}%
\regfield{MIICSRCLK}{4}{2}{{0x00}}%
\regfield{MIIWRITE}{1}{1}{0}%
\regfield{MIIBUSY}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [MIIDEV]该字段表示 32 个 PHY 设备中的正被访问的哪一个。（读/写）
\item [MIIREG]\label{fielddesc:MIIREG} 该字段在所选 PHY 设备中选择所需的地址寄存器。（读/写）
\item  [MIICSRCLK]\label{fielddesc:MIICSRCLK} 该字段配置 CSR 时钟频率，支持以下两个取值，其他值保留。
\begin{itemize}
\item 4'b0000：APB 时钟频率为 80 MHz。MDC 时钟频率为 APB\_CLK/42。
\item 4'b0011：APB 时钟频率为 40 MHz。MDC 时钟频率为 APB\_CLK/26。（读/写）
\end{itemize}
\item [MIIWRITE] 该字段置 1 时，表示使用 \hyperref[regdesc:EMACMIIDATAREG]{MII\_DATA} 的写操作。如果未置 1，则表示这是一个读取操作，即将数据放入 \hyperref[regdesc:EMACMIIDATAREG]{MII\_DATA}。（读/写）
\item [MIIBUSY]\label{fielddesc:MIIBUSY} 该字段与 \hyperref[fielddesc:MIIREG]{MIIREG}、\hyperref[regdesc:EMACMIIDATAREG]{MII\_DATA} 配合使用。

在写入 \hyperref[fielddesc:MIIREG]{MIIREG} 和 \hyperref[regdesc:EMACMIIDATAREG]{MII\_DATA} 之前，该字段应读取逻辑 0（默认为空闲状态）。

要读写 \hyperref[fielddesc:MIIREG]{MIIREG} 或 \hyperref[regdesc:EMACMIIDATAREG]{MII\_DATA}，软件（用户）应将此字段置为 1。

在读或写访问正在进行时，\hyperref[regdesc:EMACMIIDATAREG]{MII\_DATA} 应保持有效（数据应保持不变），直到该字段被硬件 (MAC) 清零。

请注意在读写 \hyperref[fielddesc:MIIREG]{MIIREG} 或 \hyperref[regdesc:EMACMIIDATAREG]{MII\_DATA} 时，ESP32 MAC 不会收到 PHY 的 ACK 信息。（读/写设置/自动清除）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACMIIDATA\_REG}{0x1014}\label{regdesc:EMACMIIDATAREG}
\regfield{(reserved)}{16}{16}{0000000000000000}%
\regfield{MII\_DATA}{16}{0}{{0x00000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [MII\_DATA]该字段包含在管理读操作之后从 PHY 读取的 16 位数据值，或在管理写操作之前要写入 PHY 的 16 位数据值。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACFC\_REG}{0x1018}\label{regdesc:EMACFLOWCONTROLREG}
\regfield{PAUSE\_TIME}{16}{16}{{0x00000}}%
\regfield{(reserved)}{10}{6}{0000000000}%
\regfield{PLT}{2}{4}{{0x0}}%
\regfield{UPFD}{1}{3}{0}%
\regfield{RFCE}{1}{2}{0}%
\regfield{TFCE}{1}{1}{0}%
\regfield{FCBBA}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [PAUSE\_TIME] 该字段保存在发送控制帧的暂停时间字段中使用的值。如果暂停时间位配置为与 MII 时钟域双重同步，则只有在目标时钟域中至少有四个时钟周期后，才能对此寄存器进行连续写入。（读/写）
\item [PLT]该字段配置暂停帧的暂停定时器自动重传的阈值。阈值应始终小于 Bit [31:16] 中配置的暂停时间。例如，如果 PT = 100H（256 个时隙）并且 PLT = 01，则在发送第一个暂停帧之后的 228 个时隙时间（256-28）自动发送第二个暂停帧。以下表示不同值的阈值：（读/写）
\begin{itemize}
\item 2'b00：阈值是暂停时间减去 4 个时隙（PT-4 时隙时间）。 \item 2'b01：阈值是暂停时间减去 28 个时隙（PT-28 时隙次数）。 \item 2'b10：阈值是暂停时间减去144时隙时间（PT-144 时隙时间）。 \item 2'b11：阈值是暂停时间减去 256 个时隙（PT-256 个时隙）。时隙时间被定义为在 MII 接口上传输 512 位（64 个字节）所用的时间。\end{itemize}
\item [UPFD] 当一个暂停帧具有在 IEEE Std 802.3 中指定的唯一的多播地址时，处理暂停帧。当该位置 1 时，MAC 也可以检测站点的单播地址的暂停帧。 这个单播地址应该在 EMACADDR0 高寄存器和 EMACADDR0 低寄存器中指定。 当该位复位时，MAC 仅检测具有唯一多播地址的暂停帧。
\item [RFCE]当该位置 1 时，MAC 解码接收到的暂停帧，并将发送器关闭指定的（暂停）时间。 当该位复位时，关闭暂停帧的解码功能。（读/写）
\item [TFCE] 在全双工模式下，当该位置 1 时，MAC 使能流控制操作发送暂停帧。当该位复位时，MAC 中的流控制操作被禁止，MAC 不会传送任何暂停帧。 在半双工模式下，当该位置 1 时，MAC 启用背压操作。 当该位复位时，关闭背压功能。（读/写）
\item [FCBBA]
该位在全双工模式下启动暂停帧，如果 TFCE 位置 1，则在半双工模式下激活背压功能。在全双工模式下，在写入流控制寄存器之前，应将该位读为 1'b0。要启动暂停帧，应用程序必须将此位置为 1'b1。在传输控制帧的过程中，该位继续被设置为表示帧传输正在进行。 暂停帧传输完成后，MAC 将该位复位为 1'b0。 流控制寄存器不应写入，直到该位被清除。 在半双工模式下，当该位置 1（并且 TFCE 置 1）时，背压由 MAC 确定。在背压期间，当 MAC 接收到一个新的帧时，发送器开始发送一个卡纸图案，导致碰撞。当 MAC 被配置为全双工模式时，BPA 被自动关闭。 （读/写设置/自动清除）(FCB)/（读/写）(BPA/Backpressure Activate)
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACDEBUG\_REG}{0x1024}\label{regdesc:EMACDEBUGREG}
\regfield{(reserved)}{6}{26}{000000}%
\regfield{MTLTSFFS}{1}{25}{0}%
\regfield{MTLTFNES}{1}{24}{0}%
\regfield{(reserved)}{1}{23}{0}%
\regfield{MTLTFWCS}{1}{22}{0}%
\regfield{MTLTFRCS}{2}{20}{{0x0}}%
\regfield{MACTP}{1}{19}{0}%
\regfield{MACTFCS}{2}{17}{{0x0}}%
\regfield{MACTPES}{1}{16}{0}%
\regfield{(reserved)}{6}{10}{000000}%
\regfield{MTLRFFLS}{2}{8}{{0x0}}%
\regfield{(reserved)}{1}{7}{0}%
\regfield{MTLRFRCS}{2}{5}{{0x0}}%
\regfield{MTLRFWCAS}{1}{4}{0}%
\regfield{(reserved)}{1}{3}{0}%
\regfield{MACRFFCS}{2}{1}{{0x0}}%
\regfield{MACRPES}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [MTLTSFFS] 该位为高电平时，该位表示 MTL TxStatus FIFO 已满。因此，MTL 不能接受任何更多的传输帧。（只读）
\item [MTLTFNES] 该位为高电平时，表示 MTL Tx FIFO 不为空，剩余一些数据用于传输。（只读）
\item [MTLTFWCS] 高电平时，该位表示 MTL Tx FIFO 写控制器激活，并正在将数据传输到 Tx FIFO。（只读）
\item [MTLTFRCS]该字段表示 Tx FIFO 读取控制器的状态：（只读）\begin{itemize}
\item 2'b00：空闲状态。\item 2'b01：READ 状态（将数据传输到 MAC 发射器）。\item 2'b10：等待来自 MAC 发送器的 TxStatus。\item 2'b11：写入接收的 TxStatus 或清空 Tx FIFO。\end{itemize}
\item [MACTP] 当该位为高电平时，表示 MAC 发送器处于暂停状态（处于全双工模式），因此不传输任何帧。（只读）
\item [MACTFCS]该字段表示 MAC 传输帧控制器模块的状态：（只读）
\begin{itemize}
\item 2'b00：空闲状态。 \item 2'b01：等待前一帧或 IFG 或退避周期的状态结束。 \item 2'b10：生成并发送暂停帧（全双工模式）。 \item 2'b11：传输输入帧。\end{itemize}
\item [MACTPES]该位为高电平时，表示 MAC MII 传输协议引擎正在主动传输数据，不处于空闲状态。（只读）
\item [MTLRFFLS]该字段表示 Rx FIFO 的填充级别的状态：（只读） \begin{itemize}
\item 2'b00：Rx FIFO 为空。 \item 2'b01：Rx FIFO 填充水平低于流量控制关闭阈值。\item  2'b10：Rx FIFO 填充水平高于流量控制激活阈值。\item  2'b11：Rx FIFO 满。\end{itemize}

\item[寄存器描述下一页继续。]
\end{reglist}\end{regdesc}
\end{register}
\addtocounter{Regfloat}{-1}
\begin{register}{H}{EMACDEBUG\_REG}{0x1024}
\begin{regdesc}\begin{reglist}
\item[继上一页寄存器描述。]

\item [MTLRFRCS]
该字段给出了 Rx FIFO 读取控制器的状态：（只读）
\begin{itemize}
\item 2'b00：空闲状态。\item  2'b01：读取帧数据。 \item 2'b10：保留。 \item 2'b11：刷新帧数据和状态。\end{itemize}
\item [MTLRFWCAS]该位为高电平时，该位表示 MTL Rx FIFO 写控制器激活，并正在将接收到的帧传送到 FIFO。（只读）
\item [MACRFFCS]该位为高电平时，该字段表示 MAC 接收帧控制器模块的小型 FIFO 读写控制器的活动状态。RFCFCSTS[1] 表示 FIFO 读取控制器的状态。RFCFCSTS[0] 表示 FIFO 写入控制器的状态。（只读）
\item [MACRPES] 该位为高电平时，该位表示 MAC MII 接收协议引擎正在主动接收数据，不处于空闲状态。（只读）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{PMT\_RWUFFR\_REG}{0x1028}\label{regdesc:PMT_RWUFFRREG}
\regfield{PMT\_RWUFFR}{32}{0}{00000000000000000000000000000000}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [WKUPPKTFILTER]（读/写）

\begin{itemize}
\item RWKPTR 等于 0 时，此寄存器代表过滤器0 的唤醒帧的字节屏蔽位。bit[31] 必须为 0，bit[30:0] 为字节屏蔽位。如果相应的位置 1，那么对收到的帧的过滤器0 的 offset+j（j=0--30）进行屏蔽，否则不屏蔽。 \item RWKPTR 等于 1 时，此寄存器代表过滤器1 的唤醒帧的字节屏蔽位。bit[31] 必须为 0，bit[30:0] 为字节屏蔽位。如果相应的位置 1，那么对收到的帧的过滤器1 的 offset+j（j=0--30）进行屏蔽，否则不屏蔽。 \item RWKPTR 等于 2 时，此寄存器代表过滤器 2的唤醒帧的字节屏蔽位。bit[31] 必须为 0，bit[30:0] 为字节屏蔽位。如果相应的位置 1，那么对收到的帧的过滤器2 的 offset+j（j=0--30）进行屏蔽，否则不屏蔽。 \item RWKPTR 等于 3 时，此寄存器代表过滤器3 的唤醒帧的字节屏蔽位。bit[31] 必须为 0，bit[30:0] 为字节屏蔽位。如果相应的位置 1，那么对收到的帧的过滤器3 的 offset+j（j=0--30）进行屏蔽，否则不屏蔽。 \item RWKPTR 等于 4 时，bit[3]/bit[11]/bit[19]/bit[27] 代表目的地址类型，1 代表 多播包，0 代表单播包。bit[0]/bit[8]/bit[16]/bit[24] 分别代表了过滤器 0/1/2/3 的使能位。
\item RWKPTR 等于 5 时，bit [7:0]/[15:8]/[23:16]/[31:24] 分别代表了过滤器 0/1/2/3 的屏蔽字节的起始 offset 位。
\item RWKPTR 等于 6 时，bit [15:0] 为过滤器0 的 CRC 计算值，bit [31:16] 为过滤器1 的 CRC 计算 值，从 offset 字节开始计算被屏蔽的字节的 CRC16。计算公式为

G(x) = x$^\text{16}$ + x$^\text{15}$ + x$^\text{2}$ + 1

\item RWKPTR 等于 7 时，bit [15:0] 为过滤器2 的 CRC 计算值，bit [31:16] 为过滤器3 的 CRC 计算值，从 offset 字节开始计算被屏蔽的字节的 CRC16。计算公式为

G(x) = x$^\text{16}$ + x$^\text{15}$ + x$^\text{2}$ + 1

\end{itemize}
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{PMT\_CSR\_REG}{0x102C}\label{regdesc:PMT_CSRREG}
\regfield{RWKFILTRST}{1}{31}{0}%
\regfield{(reserved)}{2}{29}{00}%
\regfield{RWKPTR}{5}{24}{00000}%
\regfield{(reserved)}{14}{10}{00000000000000}%
\regfield{GLBLUCAST}{1}{9}{00}%
\regfield{(reserved)}{2}{7}{00}%
\regfield{RWKPRCVD}{1}{6}{0}%
\regfield{MGKPRCVD}{1}{5}{0}%
\regfield{(reserved)}{2}{3}{00}%
\regfield{RWKPKTEN}{1}{2}{0}%
\regfield{MGKPKTEN}{1}{1}{0}%
\regfield{PWRDWN}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [RWKFILTRST] 置 1 时，把寄存器 RWKPTR 复位为 0。(读/写设置/自动清除)
\item [RWKPTR] 最大值为 7，每个值代表寄存器 PMT\_RWUFFR 的意义不同，详见 PMT\_RWUFFR 说明。（只读）
\item [GLBLUCAST] 置 1 使能通过目的地址过滤器的单播包作为唤醒帧。（读/写）
\item [RWKPRCVD] 置 1 表明收到了远程唤醒帧的一次事件。 （读/自动设置/读清除）
\item [MGKPRCVD]  置 1 表明收到了 magic 帧的一次事件。（读/自动设置/读清除）
\item [RWKPKTEN] 置 1 表明使能远程唤醒帧的功耗管理事件。（读/写）
\item [MGKPKTEN] 置 1 表明使能 magic 帧的功耗管理事件。（读/写）
\item [PWRDWN] 置 1 表明 EMAC 接收端放弃所有的接收到的帧，知道受到期望的 magic 帧或者远程唤醒帧。只有 MGKPKTEN, GLBLUCAST 或者 RWKPKTEN 置 1 时才有效。（读/写设置/自动清除）


\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACLPI\_CSR\_REG}{0x1030}\label{regdesc:EMACLPI_CSRREG}
\regfield{(reserved)}{12}{20}{00000000000}%
\regfield{LPITXA}{1}{19}{0}%
\regfield{(reserved)}{1}{18}{0}%
\regfield{PLS}{1}{17}{0}%
\regfield{LPIEN}{1}{16}{0}%
\regfield{(reserved)}{6}{10}{000000}%
\regfield{RLPIST}{1}{9}{0}%
\regfield{TLPIST}{1}{8}{0}%
\regfield{(reserved)}{4}{4}{0}%
\regfield{RLPIEX}{1}{3}{0}%
\regfield{RLPIEN}{1}{2}{0}%
\regfield{TLPIEX}{1}{1}{0}%
\regfield{TLPIEN}{1}{0}{0}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [LPITXA] 此位控制 EMAC 进入或退出 LPI 模式，当 LPITXA 和 LPIEN 置 1，并且所有的正常帧和暂停帧全部发送之后，EMAC 进入 LPI 模式，当应用层发送任何帧时退出 LPI 模式。当置 0 时，LPIEN 直接控制 EMAC 进入或退出 LPI 模式。（读/写）
\item [PLS] 此位表示 PHY 的链接状态，1 表示链接成功，0 表示链接失败。（读/写）
\item [LPIEN] 置 1 表明 EMAC 发送端进入 LPI 状态，置 0 表明 EMAC 退出 LPI 状态回复正常状态。当 LPITXA 位置 1 并且由于 EMAC 收到新的发送帧而使得退出 LPI 状态时，此位 被置 0。（读/写/自动清除）
\item [RLPIST] 置 1 表明 EMAC 正在接收 LPI 序列。 （读/写）
\item [TLPIST] 置 1 表明 EMAC 正在发送 LPI 序列。 （读/写）
\item [RLPIEX] 置 1 表明 EMAC 接收端已经停止接收 LPI 序列，已经退出 LPI 状态，恢复到正常接受状态，该寄存器被读取时清 0。 （读/自动设置/读清除）
\item [RLPIEN] 置 1 表明 EMAC 接收端受到 LPI 序列并且进入 LPI 状态，该寄存器被读取时清 0。 （读/自动设置/读清除）
\item [TLPIEX] 置 1 表明 EMAC 发送端在寄存器 LPIEN 被清 0，并且 LPI\_TW\_TIMER 过期之后已经退出 LPI 状态，该寄存器被读取时清 0。 （读/自动设置/读清除）
\item [TLPIEN] 置 1 表明 EMAC 的寄存器 LPIEN 置 1 后发送端进入 LPI 状态，该寄存器被读取时清 0。 （读/自动设置/读清除）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACLPITIMERSCONTROL\_REG}{0x1034}\label{regdesc:EMACLPITIMERSCONTROLREG}
\regfield{(reserved)}{6}{26}{000000}%
\regfield{LPI\_LS\_TIMER}{10}{16}{0x3E8}%
\regfield{LPI\_TW\_TIMER}{16}{0}{000000000000000}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [LPI\_LS\_TIMER] 此域代表 LPI 序列发送给物理层之前物理层建立链接的最小时间（毫 秒）。根据 IEEE 标准默认值为 1 秒。（读/写）
\item [LPI\_TW\_TIMER] 此域代表 EMAC 在发送正常帧之前停止发送 LPI 帧的最小时间（毫秒）。在这个计时器到期之后，寄存器 TLPIEX 应该置 1。
（读/写）
\end{reglist}\end{regdesc}
\end{register}




\begin{register}{H}{EMACINTS\_REG}{0x1038}\label{regdesc:EMACINTSREG}
\regfield{(reserved)}{21}{11}{000000000000000000000}%
\regfield{LPIINTS}{1}{10}{0}%
\regfield{(reserved)}{1}{9}{0}%
\regfield{(reserved)}{4}{5}{0000}%
\regfield{PMTINTS}{1}{3}{0}%
\regfield{(reserved)}{3}{0}{000}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [LPIINTS] 节能以太网功能启用时，该位用于设置 MAC 发射机或接收机进入或退出 LPI 状态。该位在读取寄存器（LPI 控制和状态寄存器）的 Bit[0] 时清零。（只读）

\item [PMTINTS] 在掉电模式下收到一个魔术包或远程唤醒帧时，该位置 1（见 PMT 控制和状态寄存器的 Bit[5] 和 Bit[6]）。由于对 PMT 控制和状态寄存器的读取操作，Bit[6:5] 被清零时，该位被清零。只有在核心配置期间选择可选的 PMT 模块时，该位才有效。（只读）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACINTMASK\_REG}{0x103C}\label{regdesc:EMACINTMASKREG}
\regfield{(reserved)}{21}{11}{000000000000000000000}%
\regfield{LPIINTMASK}{1}{10}{0}%
\regfield{(reserved)}{1}{9}{0}%
\regfield{(reserved)}{5}{4}{00000}%
\regfield{PMTINTMASK}{1}{3}{0}%
\regfield{(reserved)}{3}{0}{000}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [LPIINTMASK]置 1 时，由于中断状态寄存器中 LPI 中断状态位的设置，该位禁止中断信号的触发。（读/写）
\item [PMTINTMASK]置 1 时，由于中断状态寄存器中 PMT 中断状态位的设置，该位禁止中断信号的置位。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR0HIGH\_REG}{0x1040}\label{regdesc:EMACADDR0HIGHREG}
\regfield{ADDRESS\_ENABLE0}{1}{31}{0}%
\regfield{(reserved)}{15}{16}{000000000000000}%
\regfield{MAC\_ADDRESS0\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE0] 该位始终置 1。（只读）
\item [MAC\_ADDRESS0\_HI] 该字段包含第一个 6 字节 MAC 地址的高 16 位 [47:32]。MAC 使用此字段过滤接收到的帧，并将 MAC 地址插入到传输流控制（暂停）帧中。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR0LOW\_REG}{0x1044}\label{regdesc:EMACADDR0LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR0LOW\_REG] 该字段包含第一个 6 字节 MAC 地址的低 32 位。MAC 被用于过滤接收到的帧，并将 MAC 地址插入到传输流控制（暂停）帧中。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR1HIGH\_REG}{0x1048}\label{regdesc:EMACADDR1HIGHREG}
\regfield{ADDRESS\_ENABLE1}{1}{31}{0}%
\regfield{SOURCE\_ADDRESS}{1}{30}{0}%
\regfield{MASK\_BYTE\_CONTROL}{6}{24}{{0x00}}%
\regfield{(reserved)}{8}{16}{00000000}%
\regfield{MAC\_ADDRESS1\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE1]当该位置 1 时，地址过滤器模块使用第二个 MAC 地址进行完美过滤。 当该位复位时，地址过滤器模块将忽略用于过滤的地址。（读/写）
\item [SOURCE\_ADDRESS]当该位置 1 时，EMACADDR1 [47:0] 与接收帧的 SA 字段进行比较。当该位复位时，使用 EMACADDR1 [47:0] 与接收帧的 DA 字段进行比较。（读/写）
\item [MASK\_BYTE\_CONTROL] 这些位是屏蔽控制位，用于比较每个 MAC 地址字节。当置高时，MAC 不会将接收到的 DA 或 SA 的相应字节与 EMACADDR1 寄存器的内容进行比较。 每一位控制字节的掩码如下：
\begin{itemize}
\item Bit[29]：EMACADDR1 寄存器高 [15:8]。\item Bit[28]：EMACADDR1 寄存器高 [7:0]。\item Bit[27]：EMACADDR1 寄存器低 [31:24]。\item Bit[24]：EMACADDR1 寄存器低 [7:0]。\end{itemize}
可以通过屏蔽地址的一个或多个字节来过滤一组地址（称为组地址过滤）。（读/写）
\item [MAC\_ADDRESS1\_HI] 该字段包含第二个 6 字节 MAC 地址的高 16 位 [47:32]。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR1LOW\_REG}{0x104C}\label{regdesc:EMACADDR1LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR1LOW\_REG]该字段包含第二个 6 字节 MAC 地址的低 32 位。在初始化之后，该寄存器应该被配置，因为该字段的内容是不确定的。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR2HIGH\_REG}{0x1050}\label{regdesc:EMACADDR2HIGHREG}
\regfield{ADDRESS\_ENABLE2}{1}{31}{0}%
\regfield{SOURCE\_ADDRESS2}{1}{30}{0}%
\regfield{MASK\_BYTE\_CONTROL2}{6}{24}{{0x00}}%
\regfield{(reserved)}{8}{16}{00000000}%
\regfield{MAC\_ADDRESS2\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE2]当该位置 1 时，地址过滤器模块使用第三个 MAC 地址进行完美过滤。 当该位复位时，地址过滤器模块将忽略用于过滤的地址。（读/写）
\item [SOURCE\_ADDRESS2]当该位置 1 时，EMACADDR2 [47:0] 与接收帧的 SA 字段进行比较。当该位复位时，使用 EMACADDR2 [47:0] 与接收帧的 DA 字段进行比较。（读/写）
\item [MASK\_BYTE\_CONTROL2] 这些位是屏蔽控制位，用于比较每个 MAC 地址字节。当置高时，MAC 不会将接收到的 DA 或 SA 的相应字节与 EMACADDR2 寄存器的内容进行比较。 每一位控制字节的掩码如下：
\begin{itemize}
\item Bit[29]：EMACADDR2 寄存器高 [15:8]。\item Bit[28]：EMACADDR2 寄存器高 [7:0]。\item Bit[27]：EMACADDR2 寄存器低 [31:24]。\item Bit[24]：EMACADDR2 寄存器低 [7:0]。\end{itemize}
可以通过屏蔽地址的一个或多个字节来过滤一组地址（称为组地址过滤）。（读/写）
\item [MAC\_ADDRESS2\_HI] 该字段包含第三个 6 字节 MAC 地址的高 16 位 [47:32]。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR2LOW\_REG}{0x1054}\label{regdesc:EMACADDR2LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR2LOW\_REG]该字段包含第三个 6 字节 MAC 地址的低 32 位。在初始化之后，该寄存器应该被配置，因为该字段的内容是不确定的。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACADDR3HIGH\_REG}{0x1058}\label{regdesc:EMACADDR3HIGHREG}
\regfield{ADDRESS\_ENABLE3}{1}{31}{0}%
\regfield{SOURCE\_ADDRESS3}{1}{30}{0}%
\regfield{MASK\_BYTE\_CONTROL3}{6}{24}{{0x00}}%
\regfield{(reserved)}{8}{16}{00000000}%
\regfield{MAC\_ADDRESS3\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE3]当该位置 1 时，地址过滤器模块使用第四个 MAC 地址进行完美过滤。 当该位复位时，地址过滤器模块将忽略用于过滤的地址。（读/写）
\item [SOURCE\_ADDRESS3]当该位置 1 时，EMACADDR3 [47:0] 与接收帧的 SA 字段进行比较。当该位复位时，使用 EMACADDR3 [47:0] 与接收帧的 DA 字段进行比较。（读/写）
\item [MASK\_BYTE\_CONTROL3] 这些位是屏蔽控制位，用于比较每个 MAC 地址字节。当置高时，MAC 不会将接收到的 DA 或 SA 的相应字节与 EMACADDR3 寄存器的内容进行比较。 每一位控制字节的掩码如下：
\begin{itemize}
\item Bit[29]：EMACADDR3 寄存器高 [15:8]。\item Bit[28]：EMACADDR3 寄存器高 [7:0]。\item Bit[27]：EMACADDR3 寄存器低 [31:24]。\item Bit[24]：EMACADDR3 寄存器低 [7:0]。\end{itemize}
可以通过屏蔽地址的一个或多个字节来过滤一组地址（称为组地址过滤）。（读/写）
\item [MAC\_ADDRESS3\_HI] 该字段包含第四个 6 字节 MAC 地址的高 16 位 [47:32]。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR3LOW\_REG}{0x1060}\label{regdesc:EMACADDR3LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR3LOW\_REG]该字段包含第四个 6 字节 MAC 地址的低 32 位。在初始化之后，该寄存器应该被配置，因为该字段的内容是不确定的。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACADDR4HIGH\_REG}{0x1064}\label{regdesc:EMACADDR4HIGHREG}
\regfield{ADDRESS\_ENABLE4}{1}{31}{0}%
\regfield{SOURCE\_ADDRESS4}{1}{30}{0}%
\regfield{MASK\_BYTE\_CONTROL4}{6}{24}{{0x00}}%
\regfield{(reserved)}{8}{16}{00000000}%
\regfield{MAC\_ADDRESS4\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE4]当该位置 1 时，地址过滤器模块使用第五个 MAC 地址进行完美过滤。 当该位复位时，地址过滤器模块将忽略用于过滤的地址。（读/写）
\item [SOURCE\_ADDRESS4]当该位置 1 时，EMACADDR4 [47:0] 与接收帧的 SA 字段进行比较。当该位复位时，使用 EMACADDR4 [47:0] 与接收帧的 DA 字段进行比较。（读/写）
\item [MASK\_BYTE\_CONTROL4] 这些位是屏蔽控制位，用于比较每个 MAC 地址字节。当置高时，MAC 不会将接收到的 DA 或 SA 的相应字节与 EMACADDR4 寄存器的内容进行比较。 每一位控制字节的掩码如下：
\begin{itemize}
\item Bit[29]：EMACADDR4 寄存器高 [15:8]。\item Bit[28]：EMACADDR4 寄存器高 [7:0]。\item Bit[27]：EMACADDR4 寄存器低 [31:24]。\item Bit[24]：EMACADDR4 寄存器低 [7:0]。\end{itemize}
可以通过屏蔽地址的一个或多个字节来过滤一组地址（称为组地址过滤）。（读/写）
\item [MAC\_ADDRESS4\_HI] 该字段包含第五个 6 字节 MAC 地址的高 16 位 [47:32]。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR4LOW\_REG}{0x1068}\label{regdesc:EMACADDR4LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR4LOW\_REG]该字段包含第五个 6 字节 MAC 地址的低 32 位。在初始化之后，该寄存器应该被配置，因为该字段的内容是不确定的。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACADDR5HIGH\_REG}{0x106C}\label{regdesc:EMACADDR5HIGHREG}
\regfield{ADDRESS\_ENABLE5}{1}{31}{0}%
\regfield{SOURCE\_ADDRESS5}{1}{30}{0}%
\regfield{MASK\_BYTE\_CONTROL5}{6}{24}{{0x00}}%
\regfield{(reserved)}{8}{16}{00000000}%
\regfield{MAC\_ADDRESS5\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE5]当该位置 1 时，地址过滤器模块使用第六个 MAC 地址进行完美过滤。 当该位复位时，地址过滤器模块将忽略用于过滤的地址。（读/写）
\item [SOURCE\_ADDRESS5]当该位置 1 时，EMACADDR5 [47:0] 与接收帧的 SA 字段进行比较。当该位复位时，使用 EMACADDR5 [47:0] 与接收帧的 DA 字段进行比较。（读/写）
\item [MASK\_BYTE\_CONTROL5] 这些位是屏蔽控制位，用于比较每个 MAC 地址字节。当置高时，MAC 不会将接收到的 DA 或 SA 的相应字节与 EMACADDR5 寄存器的内容进行比较。 每一位控制字节的掩码如下：
\begin{itemize}
\item Bit[29]：EMACADDR5 寄存器高 [15:8]。\item Bit[28]：EMACADDR5 寄存器高 [7:0]。\item Bit[27]：EMACADDR5 寄存器低 [31:24]。\item Bit[24]：EMACADDR5 寄存器低 [7:0]。\end{itemize}
可以通过屏蔽地址的一个或多个字节来过滤一组地址（称为组地址过滤）。（读/写）
\item [MAC\_ADDRESS5\_HI] 该字段包含第六个 6 字节 MAC 地址的高 16 位 [47:32]。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACADDR5LOW\_REG}{0x1070}\label{regdesc:EMACADDR5LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR5LOW\_REG]该字段包含第六个 6 字节 MAC 地址的低 32 位。在初始化之后，该寄存器应该被配置，因为该字段的内容是不确定的。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACADDR6HIGH\_REG}{0x1074}\label{regdesc:EMACADDR6HIGHREG}
\regfield{ADDRESS\_ENABLE6}{1}{31}{0}%
\regfield{SOURCE\_ADDRESS6}{1}{30}{0}%
\regfield{MASK\_BYTE\_CONTROL6}{6}{24}{{0x00}}%
\regfield{(reserved)}{8}{16}{00000000}%
\regfield{MAC\_ADDRESS6\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE6]当该位置 1 时，地址过滤器模块使用第七个 MAC 地址进行完美过滤。 当该位复位时，地址过滤器模块将忽略用于过滤的地址。（读/写）
\item [SOURCE\_ADDRESS6]当该位置 1 时，EMACADDR6 [47:0] 与接收帧的 SA 字段进行比较。当该位复位时，使用 EMACADDR6 [47:0] 与接收帧的 DA 字段进行比较。（读/写）
\item [MASK\_BYTE\_CONTROL6] 这些位是屏蔽控制位，用于比较每个 MAC 地址字节。当置高时，MAC 不会将接收到的 DA 或 SA 的相应字节与 EMACADDR6 寄存器的内容进行比较。 每一位控制字节的掩码如下：
\begin{itemize}
\item Bit[29]：EMACADDR6 寄存器高 [15:8]。\item Bit[28]：EMACADDR6 寄存器高 [7:0]。\item Bit[27]：EMACADDR6 寄存器低 [31:24]。\item Bit[24]：EMACADDR6 寄存器低 [7:0]。\end{itemize}
可以通过屏蔽地址的一个或多个字节来过滤一组地址（称为组地址过滤）。（读/写）
\item [MAC\_ADDRESS6\_HI] 该字段包含第七个 6 字节 MAC 地址的高 16 位 [47:32]。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACADDR6LOW\_REG}{0x1078}\label{regdesc:EMACADDR6LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR6LOW\_REG]该字段包含第七个 6 字节 MAC 地址的低 32 位。在初始化之后，该寄存器应该被配置，因为该字段的内容是不确定的。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACADDR7HIGH\_REG}{0x107C}\label{regdesc:EMACADDR7HIGHREG}
\regfield{ADDRESS\_ENABLE7}{1}{31}{0}%
\regfield{SOURCE\_ADDRESS7}{1}{30}{0}%
\regfield{MASK\_BYTE\_CONTROL7}{6}{24}{{0x00}}%
\regfield{(reserved)}{8}{16}{00000000}%
\regfield{MAC\_ADDRESS7\_HI}{16}{0}{{0x0FFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [ADDRESS\_ENABLE7]当该位置 1 时，地址过滤器模块使用第八个 MAC 地址进行完美过滤。 当该位复位时，地址过滤器模块将忽略用于过滤的地址。（读/写）
\item [SOURCE\_ADDRESS7]当该位置 1 时，EMACADDR7 [47:0] 与接收帧的 SA 字段进行比较。当该位复位时，使用 EMACADDR7 [47:0] 与接收帧的 DA 字段进行比较。（读/写）
\item [MASK\_BYTE\_CONTROL7] 这些位是屏蔽控制位，用于比较每个 MAC 地址字节。当置高时，MAC 不会将接收到的 DA 或 SA 的相应字节与 EMACADDR7 寄存器的内容进行比较。 每一位控制字节的掩码如下：
\begin{itemize}
\item Bit[29]：EMACADDR7 寄存器高 [15:8]。\item Bit[28]：EMACADDR7 寄存器高 [7:0]。\item Bit[27]：EMACADDR7 寄存器低 [31:24]。\item Bit[24]：EMACADDR7 寄存器低 [7:0]。\end{itemize}
可以通过屏蔽地址的一个或多个字节来过滤一组地址（称为组地址过滤）。（读/写）
\item [MAC\_ADDRESS7\_HI] 该字段包含第八个 6 字节 MAC 地址的高 16 位 [47:32]。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMACADDR7LOW\_REG}{0x1070}\label{regdesc:EMACADDR7LOWREG}
\regfield{}{32}{0}{{0x0FFFFFFFF}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [EMACADDR7LOW\_REG]该字段包含第八个 6 字节 MAC 地址的低 32 位。在初始化之后，该寄存器应该被配置，因为该字段的内容是不确定的。（读/写）
\end{reglist}\end{regdesc}
\end{register}


\begin{register}{H}{EMACWDOGTO\_REG}{0x10DC}\label{regdesc:EMACWDOGTOREG}
\regfield{(reserved)}{15}{17}{000000000000000}%
\regfield{PWDOGEN}{1}{16}{0}%
\regfield{(reserved)}{2}{14}{00}%
\regfield{WDOGTO}{14}{0}{{0x0000}}%
\reglabel{Reset}\regnewline%
\begin{regdesc}\begin{reglist}
\item [PWDOGEN]当该位置 1 且 EMACCONFIG\_REG Bit[23]（WD）复位时，WTO 字段（Bit [13:0]）用作接收帧的看门狗超时。当该位清零时，接收帧的 EMACCONFIG\_REG Bit[23]（WD）和 Bit[20]（JE）的设置控制。（读/写）
\item [WDOGTO]当 Bit[16]（PWE）置 1 且 MAC 配置寄存器的 Bit[23]（WD）复位时，该字段用作接收帧的看门狗超时。 如果接收到的帧的长度超过了这个字段的值，那么这个帧将被终止并被声明为错误帧。（读/写）
\end{reglist}\end{regdesc}
\end{register}

\begin{register}{H}{EMAC\_EX\_CLKOUT\_CONF\_REG}{0x0800}\label{regdesc:EMACEXCLKOUTCONFREG}
    \regfield{(reserved)}{24}{8}{000000000000000000000000}%
    \regfield{EMAC\_CLK\_OUT\_H\_DIV\_NUM}{4}{4}{{0x02}}%
    \regfield{EMAC\_CLK\_OUT\_DIV\_NUM}{4}{0}{{0x04}}%
    \reglabel{Reset}\regnewline%
    \begin{regdesc}\begin{reglist}
    \item [EMAC\_CLK\_OUT\_H\_DIV\_NUM]当使用 RMII PHY 时，RMII CLK 使用内部 APLL CLK，进行半整数分频。（读/写）
    \item [EMAC\_CLK\_OUT\_DIV\_NUM]当使用 RMII PHY 时，RMII CLK 使用内部的 APLL CLK，进行整数分频。（读/写）
    \end{reglist}\end{regdesc}
    \end{register}


    \begin{register}{H}{EMAC\_EX\_OSCCLK\_CONF\_REG}{0x0804}\label{regdesc:EMACEXOSCCLKCONFREG}
    \regfield{(reserved)}{7}{25}{0000000}%
    \regfield{EMAC\_OSC\_CLK\_SEL}{1}{24}{0}%
    \regfield{EMAC\_OSC\_H\_DIV\_NUM\_100M}{6}{18}{{0}}%
    \regfield{EMAC\_OSC\_DIV\_NUM\_100M}{6}{12}{{1}}%
    \regfield{EMAC\_OSC\_H\_DIV\_NUM\_10M}{6}{6}{{9}}%
    \regfield{EMAC\_OSC\_DIV\_NUM\_10M}{6}{0}{{19}}%
    \reglabel{Reset}\regnewline%
    \begin{regdesc}\begin{reglist}
    \item [EMAC\_OSC\_CLK\_SEL]该位决定当使用 RMII PHY 时，以太网是否使用外部 PHY 输出时钟作为 RMII CLK。该位置 1 时，使用外部晶振 CLK；该位置 0 时，使用内部 APLL CLK。（读/写）
    \item [EMAC\_OSC\_H\_DIV\_NUM\_100M]RMII/MII 半整数分频寄存器，当 EMAC\_EX\_CLKOUT\_CONF 分频寄存器配置为 100 MHz 时。（读/写）
    \item [EMAC\_OSC\_DIV\_NUM\_100M]RMII/MII 整数分频寄存器，当 EMAC\_EX\_CLKOUT\_CONF 分频寄存器配置为 100 MHz 时。（读/写）
    \item [EMAC\_OSC\_H\_DIV\_NUM\_10M]RMII/MII 半整数分频寄存器，当 EMAC\_EX\_CLKOUT\_CONF 分频寄存器配置为 10 MHz 时。 （读/写）
    \item [EMAC\_OSC\_DIV\_NUM\_10M]RMII/MII 整数分频寄存器，当 EMAC\_EX\_CLKOUT\_CONF 分频寄存器配置为 10 MHz 时。（读/写）
    \end{reglist}\end{regdesc}
    \end{register}


    \begin{register}{H}{EMAC\_EX\_CLK\_CTRL\_REG}{0x0808}\label{regdesc:EMACEXCLKCTRLREG}
    \regfield{(reserved)}{26}{6}{00000000000000000000000000}%
    \regfield{EMAC\_CLK\_EN}{1}{5}{0}%
    \regfield{EMAC\_MII\_CLK\_RX\_EN}{1}{4}{0}%
    \regfield{EMAC\_MII\_CLK\_TX\_EN}{1}{3}{0}%
    \regfield{(reserved)}{1}{2}{0}%
    \regfield{EMAC\_INT\_OSC\_EN}{1}{1}{0}%
    \regfield{EMAC\_EXT\_OSC\_EN}{1}{0}{0}%
    \reglabel{Reset}\regnewline%
    \begin{regdesc}\begin{reglist}

    \item [EMAC\_MII\_CLK\_RX\_EN]
    Ethernet RX CLK 使能位。（读/写）
    \item [EMAC\_MII\_CLK\_TX\_EN] Ethernet TX CLK使能位。（读/写）
    \item [EMAC\_INT\_OSC\_EN] 在 RMII PHY 模式下使用内部 APLL CLK。（读/写）
    \item [EMAC\_EXT\_OSC\_EN]在 RMII PHY 模式下使用外部 XTAL CLK。（读/写）
    \end{reglist}\end{regdesc}
    \end{register}


    \begin{register}{H}{EMAC\_EX\_PHYINF\_CONF\_REG}{0x080c}\label{regdesc:EMACEXPHYINFCONFREG}
    \regfield{(reserved)}{16}{16}{0000000000000000}%
    \regfield{EMAC\_PHY\_INTF\_SEL}{3}{13}{000}%
    \regfield{(reserved)}{13}{0}{0000000000000}%
    \reglabel{Reset}\regnewline%
    \begin{regdesc}\begin{reglist}
    \item [EMAC\_PHY\_INTF\_SEL] 选择的 PHY 接口类型。0x0 表示 PHY 为 MII，0x4 表示 PHY 为 RMII。（读/写）
    \end{reglist}\end{regdesc}
    \end{register}


    \begin{register}{H}{EMAC\_PD\_SEL\_REG}{0x0810}\label{regdesc:EMACPDSELREG}
    \regfield{(reserved)}{30}{2}{000000000000000000000000000000}%
    \regfield{EMAC\_RAM\_PD\_EN}{2}{0}{0}%
    \reglabel{Reset}\regnewline%
    \begin{regdesc}\begin{reglist}
    \item [EMAC\_RAM\_PD\_EN]以太网 RAM 掉电使能信号，Bit 0 用于 TX SRAM，Bit 1 用于 RX SRAM。置 1 表示掉电。（读/写）
    \end{reglist}\end{regdesc}
    \end{register}



