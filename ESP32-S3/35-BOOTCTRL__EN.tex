\newif\ifglobal

%%%%%%%%%%%%%%%%%%%%%%%
%%% Compile to view %%%
%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Readme:                                     %%%
%%% https://www.overleaf.com/read/bwdjvfjksvjy/ %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% >>> M?: marks a module setting number or important notice

% >>> M4: Set {./relative-path} to external files for this module <<<
\def \modulefiles {./35-BOOTCTRL}
% To add an external file, use:
% (Replace '---' with actual filename)
% '\input{\modulefiles/tables/---.tex}' for tables
% '\includegraphics{\modulefiles/figures/---}' for figures
% '\subfile{\modulefiles/---}' for register files

% >>> M1: This module is in global project? <<<
% 'YES' - uncomment; 'NO' - comment out
\globaltrue

\ifglobal
    \documentclass[main\_\_EN.tex]{subfiles}

\else
    % Font "FandolSong-Regular" used by ctex does not have GSUB table.
    % It causes warnings that do not affect compilation and can be ignored.

    \documentclass[openany, 10pt]{book}

    % >>> M2: In ./00-shared/preamble-trm-module, also find
    %         settings for visibility of todo notes, watermark <<<
    \usepackage{preamble-trm-module}

    % Enable Chinese typesetting

    % >>> M3: Temporary labels in English,
    %         you can add your own labels there <<<
    \myexternaldocument{./00-shared/temp-labels-en}

\fi %\ifglobal


\setcounter{secnumdepth}{4}

% >>> M5: If this module needs any updates in
% './00-shared/preamble-trm-module.sty'
% (include additional package, etc.), contact your module PM
% or create the file './00-shared/changelog.tex' make the
% necessary updates yourself and document them in this file <<<


\begin{document}


% Sets language into which pre-defined bits of text are translated
\selectlanguage{english}

% Do not delete this block
\ifglobal
% Add the following front matter if
% this module is outside of global project
\else
    \InputIfFileExists{readme}{}{}
    {\let\clearpage\relax \listoftodos}
    \tableofcontents
    %\listoftables
    %\listoffigures
\fi

%%%%%%%%%%%%%%%%%%%%%%%%%
%%% TRM Module Begins %%%
%%%%%%%%%%%%%%%%%%%%%%%%%

\hypertarget{bootctrl}{}
\chapter{Chip Boot Control}
\label{mod:bootctrl}

\section{Overview}

\chipname{} has four strapping pins:
\begin{itemize}
    \item GPIO0
    \item GPIO3
    \item GPIO45
    \item GPIO46
\end{itemize}
These strapping pins are used to control the following functions during chip power-on or hardware reset:
\begin{itemize}
    \item control chip boot mode
    \item enable or disable ROM messages printing
    \item control the voltage of VDD\_SPI
    \item control the source of JTAG signals
\end{itemize}

During Chip Reset (see Chapter \ref{mod:resclk} \textit{\nameref{mod:resclk}}), hardware captures samples and stores the voltage level of strapping pins as strapping bit of ``0" or ``1" in latches, and holds these bits until the chip is powered down or shut down. Software can read the latch status (strapping value) from the register \hyperref[fielddesc:GPIOSTRAPPING]{GPIO\_STRAPPING}.

By default, GPIO0, GPIO45, and GPIO46 are connected to the chip's internal pull-up/pull-down resistors. If these pins are not connected or connected to an external high-impedance circuit, the internal weak pull-up/pull-down determines the default input level of these strapping pins (see Table \ref{tab:bootctrl-default-config-strapping-pins}).

\begin{longtable}[c]{|p{2.5cm}|p{3.5cm}|}
\caption{Default Configuration of Strapping Pins}
\label{tab:bootctrl-default-config-strapping-pins} \\
    \hline
    \rowcolor{lightgray}
  \textbf{Strapping Pin} & \textbf{Default Configuration} \\ \hline
  \textbf{GPIO0} & Pull-up \\ \hline
  \textbf{GPIO3} & N/A \\ \hline
  \textbf{GPIO45} & Pull-down \\ \hline
  \textbf{GPIO46} & Pull-down \\ \hline
\end{longtable}

To change the strapping bit values, users can apply external pull-down/pull-up resistors, or use host MCU GPIOs to control the voltage level of these pins when powering on \chipname{}. After the reset is released, the strapping pins work as normal-function pins.

\begin{tiplisting}
The following section provides description of the chip functions and the pattern of the strapping pins values to invoke each function. Only documented patterns should be used. If some pattern is not documented, it may trigger unexpected behavior.
\end{tiplisting}

\section{Boot Mode Control}\label{sec:bootModeControl}

The values of GPIO0, GPIO1, GPIO2, and GPIO46 at reset determine the boot mode after the reset is released. Table \ref{tab:bootctrl-boot-mode-control} shows the strapping pin values of GPIO0, GPIO1, GPIO2, and GPIO46, and the associated boot modes.

\begin{ThreePartTable}
\begin{TableNotes}
    \item[1] Joint Download Boot mode: This mode supports the following download methods:
    \begin{itemize}
        \item USB Download Boot
        \begin{itemize}
            \item USB-Serial-JTAG Download Boot
            \item USB-OTG Download Boot
        \end{itemize}
        \item UART Download Boot
    \end{itemize}
    \item[2] SPI Download Boot mode: GPIO1 and GPIO2 are not strapping pins. But you need to reserve them when using SPI Download Boot mode. GPIO1 and GPIO2 are floating by default and are in a high-impedance state at reset.
\end{TableNotes}
\begin{longtable}[c]{|p{5cm}|c|c|c|c|}
\caption{Boot Mode Control}
\label{tab:bootctrl-boot-mode-control} \\
    \hline
    \rowcolor{lightgray}
  \textbf{Boot Mode} & \textbf{GPIO0} & \textbf{GPIO46}& \textbf{GPIO1} & \textbf{GPIO2} \\
    \hline
    \endfirsthead
    \insertTableNotes
    \endlastfoot
  SPI Boot mode & 1 & Ignored & Ignored & Ignored \\ \hline
  Joint Download Boot mode\tnote{1} & 0 & 0 & Ignored & Ignored \\ \hline
  SPI Download Boot mode\tnote{2} & 0 & 1 & 1 & 0 \\ \hline
\end{longtable}
\end{ThreePartTable}

In SPI Boot mode, the ROM bootloader loads and executes the program from SPI flash to boot the system. SPI Boot mode can be further classified as follows:
\begin{itemize}
    \item Normal Flash Boot: supports Security Boot. The ROM bootloader loads the program from flash into SRAM and executes it. In most practical scenarios, this program is the 2nd stage bootloader, which later boots the target application.
    \item Direct Boot: does not support Security Boot and programs run directly from flash. To enable this mode, make sure that the first two words of the bin file downloaded to flash (address: 0x{}42000000) are 0x{}aedb041d.
\end{itemize}

In Joint Download Boot mode, users can download binary files into flash using UART0 or USB interface. It is also possible to download binary files into SRAM and execute it in this mode.

In SPI Download Boot mode, users can download binary files into flash using SPI interface. It is also possible to download binary files into SRAM and execute it from SRAM.

The following eFuses control boot mode behaviors:

\begin{itemize}
    \item \hyperref[fielddesc:EFUSEDISFORCEDOWNLOAD]{EFUSE\_DIS\_FORCE\_DOWNLOAD}
    \begin{itemize}
    \item If this eFuse is 0 (default), software can force switch the chip from SPI Boot mode to Joint Download Boot mode by setting register \hyperref[fielddesc:RTCCNTLFORCEDOWNLOADBOOT]{RTC\_CNTL\_FORCE\_DOWNLOAD\_BOOT} and triggering a CPU reset. In this case, hardware overwrites \hyperref[fielddesc:GPIOSTRAPPING]{GPIO\_STRAPPING}[3:2] from ``1x" to ``00".
    \item If this eFuse is 1, RTC\_CNTL\_FORCE\_DOWNLOAD\_BOOT is disabled. \hyperref[fielddesc:GPIOSTRAPPING]{GPIO\_STRAPPING} can not be overwritten.
   \end{itemize}
    \item \hyperref[fielddesc:EFUSEDISDOWNLOADMODE]{EFUSE\_DIS\_DOWNLOAD\_MODE}

    If this eFuse is 1, Joint Download Boot mode is disabled. \hyperref[fielddesc:GPIOSTRAPPING]{GPIO\_STRAPPING} will not be overwritten by \hyperref[fielddesc:RTCCNTLFORCEDOWNLOADBOOT]{RTC\_CNTL\_FORCE\_DOWNLOAD\_BOOT}
    \item \hyperref[fielddesc:EFUSEENABLESECURITYDOWNLOAD]{EFUSE\_ENABLE\_SECURITY\_DOWNLOAD}

    If this eFuse is set to 1, the secure download mode is enabled, allowing reading, writing, and erasing \textbf{plaintext} flash. However, it does not permit downloading code to flash for direct execution via UART, USB, or SPI interfaces, and register operations are not supported. Note that in this mode, the supported \href{https://docs.espressif.com/projects/esptool/en/latest/esp32s3/esptool/index.html}{esptool} commands are limited. For example, writing to flash is allowed, but reading is not. To read flash, please switch to SPI Boot mode and enable the bootloader.

    Ignore this eFuse if Joint Download Boot mode is disabled.

    \item EFUSE\_DIS\_DIRECT\_BOOT

    If this eFuse is 1, Direct Boot mode is disabled.
\end{itemize}

USB Serial/JTAG Controller can also force the chip into Joint Download Boot mode from SPI Boot mode, as well as force the chip into SPI Boot mode from Joint Download Boot mode. For detailed information, please refer to Chapter \ref{mod:usbserialjtag} \textit{\nameref{mod:usbserialjtag}}.

\section{ROM Messages Printing Control}

During the boot process, ROM messages are printed to both UART0 and USB Serial/JTAG controller by default. The printing to UART0 or USB Serial/JTAG controller can be disabled in various boot modes or configuration.

Printing to UART0 is controlled as described in the table below.

\begin{table}[H]
    \centering
    \caption{Control of ROM Messages Printing to UART0}
    \label{table:ROM_CODE_CONTROL_UART0}
    \begin{threeparttable}
    \begin{tabular}{ |p{3.5cm}|p{1.5cm}<{\centering}|p{1.3cm}<{\centering}|p{1.3cm}<{\centering}|p{3cm}|}
    \hline
        \rowcolor{lightgray}
    \textbf{Boot Mode} & \textbf{Register\tnote{1}} & \textbf{eFuse\tnote{2}} &   \textbf{GPIO46}      & \textbf{ROM Messages Printing to UART0} \\\hline
    Download Boot Mode           & Ignored       & Ignored               & Ignored & Enabled \\\hline
    \multirow{7}*{SPI Boot Mode} & \multirow{6}*{0}  & 0                & Ignored & Enabled \\\cline{3-5}
                                 &                   & \multirow{2}*{1} & 0 & Enabled \\\cline{4-5}
                                 &                   &                  & 1 & Disabled \\\cline{3-5}
                                 &                   & \multirow{2}*{2} & 0 & Disabled \\\cline{4-5}
                                 &                   &                  & 1 & Enabled \\\cline{3-5}
                                 &                   & 3                & Ignored & Disabled \\\cline{2-5}
                                 & 1                 & Ignored               & Ignored & Disabled \\\hline
        \end{tabular}

        \begin{tablenotes}
            \item[1] Register: \hyperref[regdesc:RTCCNTLRTCSTORE4REG]{RTC\_CNTL\_RTC\_STORE4\_REG}[0]
            \item[2] eFuse: \hyperref[fielddesc:EFUSEUARTPRINTCONTROL]{EFUSE\_UART\_PRINT\_CONTROL}
        \end{tablenotes}
    \end{threeparttable}
\end{table}

Printing to USB Serial/JTAG controller is controlled as described in the table below.

\begin{table}[H]
    \centering
    \caption{Control of ROM Messages Printing to USB Serial/JTAG controller}
    \label{table:ROM_CODE_CONTROL_USB}
    \begin{threeparttable}
    \begin{tabular}{ |p{3.5cm}|p{1.5cm}<{\centering}|p{1.4cm}<{\centering}|p{1.4cm}<{\centering}|p{1.4cm}<{\centering}|p{4.5cm}|}
    \hline
        \rowcolor{lightgray}
    \textbf{Boot Mode} & \textbf{Register\tnote{a}} & \textbf{eFuse\_1\tnote{b}} & \textbf{eFuse\_2\tnote{c}} & \textbf{eFuse\_3\tnote{d}} & \textbf{ROM Messages Printing to USB Serial/JTAG controller} \\\hline
    \multirow{2}*{Download Boot Mode} & \multirow{2}*{Ignored} & 0                             & 0  & 0  & Enabled \\\cline{3-6}
                                      &                           & \multicolumn{3}{|c|}{Others}            & Disabled \\\hline
    \multirow{4}*{SPI Boot Mode}      & \multirow{3}*{0}          & 1                             & 1  & Ignored  & Enabled \\\cline{3-6}
                                      &                           & \multicolumn{2}{|c|}{00/10/01}     & 0  & Enabled  \\\cline{3-6}
                                      &                           & \multicolumn{2}{|c|}{00/10/01}     & 1  & Disabled  \\\cline{2-6}
                                      & 1                         & Ignored                            & Ignored  & Ignored  & Disabled  \\\hline
        \end{tabular}

        \begin{tablenotes}
            \item[a] Register: \hyperref[regdesc:RTCCNTLRTCSTORE4REG]{RTC\_CNTL\_RTC\_STORE4\_REG}[0]
            \item[b] eFuse\_1: \hyperref[fielddesc:EFUSEDISUSBSERIALJTAG]{EFUSE\_DIS\_USB\_SERIAL\_JTAG}
            \item[c] eFuse\_2: In Download Boot mode, eFuse\_2 is \hyperref[fielddesc:EFUSEDISUSBSERIALJTAGDOWNLOADMODE]{EFUSE\_DIS\_USB\_SERIAL\_JTAG\_DOWNLOAD\_MODE}; In SPI Boot mode, eFuse\_2 is \hyperref[fielddesc:EFUSEDISUSBOTG]{EFUSE\_DIS\_USB\_OTG}
            \item[d] eFuse\_3: In Download Boot mode, eFuse\_3 is \hyperref[fielddesc:EFUSEDISDOWNLOADMODE]{EFUSE\_DIS\_DOWNLOAD\_MODE}; In SPI Boot mode, eFuse\_3 is \hyperref[fielddesc:EFUSEDISUSBPRINT]{EFUSE\_DIS\_USB\_PRINT}
        \end{tablenotes}
    \end{threeparttable}
\end{table}

\vspace{-1em}
\begin{tiplisting}
The value of \hyperref[regdesc:RTCCNTLRTCSTORE4REG]{RTC\_CNTL\_RTC\_STORE4\_REG}[0] can be read and written any number of times, whereas eFuse can only be burned once. Therefore, \hyperref[regdesc:RTCCNTLRTCSTORE4REG]{RTC\_CNTL\_RTC\_STORE4\_REG}[0] can be used to temporarily disable ROM messages printing, while eFuse can be used to permanently disable ROM messages printing.
\end{tiplisting}

\section{VDD\_SPI Voltage Control}

GPIO45 is used to select the VDD\_SPI power supply voltage at reset:

\begin{itemize}
    \item GPIO45 = 0, VDD\_SPI pin is powered directly from VDD3P3\_RTC via resistor R$_{SPI}$. Typically this voltage is 3.3 V. For more information, see Figure: \chipname{} Power Scheme in \href{https://www.espressif.com/sites/default/files/documentation/esp32-s3_datasheet_en.pdf}{\chipname{} Datasheet}.
    \item GPIO45 = 1, VDD\_SPI pin is powered from internal 1.8 V LDO.
\end{itemize}

This functionality can be overridden by setting eFuse bit \hyperref[fielddesc:EFUSEVDDSPIFORCE]{EFUSE\_VDD\_SPI\_FORCE} to 1, in which case the \hyperref[fielddesc:EFUSEVDDSPITIEH]{EFUSE\_VDD\_SPI\_TIEH} determines the VDD\_SPI voltage:

\begin{itemize}
    \item \hyperref[fielddesc:EFUSEVDDSPITIEH]{EFUSE\_VDD\_SPI\_TIEH} = 0, VDD\_SPI connects to 1.8 V LDO.
    \item \hyperref[fielddesc:EFUSEVDDSPITIEH]{EFUSE\_VDD\_SPI\_TIEH} = 1, VDD\_SPI connects to VDD3P3\_RTC.
\end{itemize}

\section{JTAG Signal Source Control}

GPIO3 controls the source of JTAG signals during the early boot process. This GPIO is used together with \hyperref[fielddesc:EFUSEDISPADJTAG]{EFUSE\_DIS\_PAD\_JTAG}, \hyperref[fielddesc:EFUSEDISUSBJTAG]{EFUSE\_DIS\_USB\_JTAG}, and \hyperref[fielddesc:EFUSESTRAPJTAGSEL]{EFUSE\_STRAP\_JTAG\_SEL}, see Table \ref{tab:bootctrl-jtag-signal-src-control}.

\begin{table}[H]
    \centering
    \caption{JTAG Signal Source Control}
    \label{tab:bootctrl-jtag-signal-src-control}
    \begin{threeparttable}
        \begin{tabular}{ | l | C{2.3cm} | C{2.3cm} | C{2.3cm} | C{2cm} | }
        \hline

        \rowcolor{lightgray}
        \textbf{JTAG Signal Source} & \textbf{\hyperref[fielddesc:EFUSEDISPADJTAG]{EFUSE\_DIS\newline\_PAD\_JTAG}} & \textbf{\hyperref[fielddesc:EFUSEDISUSBJTAG]{EFUSE\_DIS\newline\_USB\_JTAG}} & \textbf{\hyperref[fielddesc:EFUSESTRAPJTAGSEL]{EFUSE\_STRAP\newline\_JTAG\_SEL}} &   \textbf{GPIO3}       \\\hline

        \multirow{3}*{\textbf{USB Serial/JTAG Controller}} & \textbf{0}\tnote{a}& \textbf{0} & \textbf{0} & Ignored \\\cline{2-5}
         & 0 & 0 & 1 & 1 \\\cline{2-5}
         & 1 & 0 & Ignored & Ignored \\\hline
        \multirow{2}*{JTAG pins MTDI, MTCK, MTMS, and MTDO} & 0 & 0 & 1 & 0 \\\cline{2-5}
         & 0 & 1 & Ignored & Ignored \\\hline
        JTAG disabled & 1 & 1 & Ignored & Ignored \\\hline

        \end{tabular}

            \begin{tablenotes}
                \item[a] \textbf{Bold} marks the default value and configuration
            \end{tablenotes}
        \end{threeparttable}
    \end{table}

    \end{document}
