\newif\ifglobal

%%%%%%%%%%%%%%%%%%%%%%%
%%% Compile to view %%%
%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Readme:                                     %%%
%%% https://www.overleaf.com/read/bwdjvfjksvjy/ %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% >>> M?: marks a module setting number or important notice

% >>> M4: Set {./relative-path} to external files for this module <<<
\def \modulefiles {./17-MCPWM}
% To add an external file, use:
% (Replace '---' with actual filename)
% '\input{\modulefiles/tables/---.tex}' for tables
% '\includegraphics{\modulefiles/figures/---}' for figures
% '\subfile{\modulefiles/---}' for register files

% >>> M1: This module is in global project? <<<
% 'YES' - uncomment; 'NO' - comment out
\globaltrue

\ifglobal
    \documentclass[main\_\_EN.tex]{subfiles}

\else
    \documentclass[openany, 10pt]{book}

    % >>> M2: In ./00-shared/preamble-trm-module, also find
    %         settings for visibility of todo notes, watermark <<<
    \usepackage{preamble-trm-module}

    % >>> M3: Temporary labels in English,
    %         you can add your own labels there <<<
    \myexternaldocument{./00-shared/temp-labels-en}

\fi %\ifglobal


\setcounter{secnumdepth}{4}

% >>> M5: If this module needs any updates in
% './00-shared/preamble-trm-module.sty'
% (include additional package, etc.), contact your module PM
% or create the file './00-shared/changelog.tex' make the
% necessary updates yourself and document them in this file <<<


\begin{document}


% Sets language into which pre-defined bits of text are translated
\selectlanguage{english}

% Do not delete this block
\ifglobal
% Add the following front matter if
% this module is outside of global project
\else
    \InputIfFileExists{readme}{}{}
    {\let\clearpage\relax \listoftodos}
    \tableofcontents
    %\listoftables
    %\listoffigures
\fi


%%%%%%%%%%%%%%%%%%%%%%%%%
%%% TRM Module Begins %%%
%%%%%%%%%%%%%%%%%%%%%%%%%
\hypertarget{mcpwm}{}
\chapter{Motor Control PWM (MCPWM)}
\label{mod:mcpwm}


\section{Overview}

The \textbf{M}otor \textbf{C}ontrol \textbf{P}ulse \textbf{W}idth \textbf{M}odulator (MCPWM) peripheral is intended for motor and power control. It provides six PWM outputs that can be set up to operate in several topologies. One common topology uses a pair of PWM outputs driving an H-bridge to control motor rotation speed and rotation direction.

The timing and control resources inside are allocated into two major types of submodules: PWM timers and PWM operators. Each PWM timer provides timing references that can either run freely or be synced to other timers or external sources. Each PWM operator has all necessary control resources to generate waveform pairs for one PWM channel. The MCPWM peripheral also contains a dedicated capture submodule that is used in systems where accurate timing of external events is important.

\chipname{} contains two MCPWM peripherals: MCPWM0 and MCPWM1.

\section{Features}
Each MCPWM peripheral has one clock divider (prescaler), three PWM timers, three PWM operators, and a capture module. Figure \ref{fig:mcpwm-overview} shows the submodules inside and the signals on the interface. PWM timers are used for generating timing references. The PWM operators generate desired waveform based on the timing references. Any PWM operator can be configured to use the timing references of any PWM timers. Different PWM operators can use the same PWM timer's timing references to produce related PWM signals. PWM operators can also use different PWM timers' values to produce the PWM signals that work alone. Different PWM timers can also be synchronized together.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-overview.eps}
    \caption{MCPWM Module Overview}
    \label{fig:mcpwm-overview}
\end{figure}

An overview of the submodules' function in Figure \ref{fig:mcpwm-overview} is shown below:

\begin{itemize}
    \item PWM Timers 0, 1 and 2
        \begin{itemize}
            \item Every PWM timer has a dedicated 8-bit clock prescaler.
            \item The 16-bit counter in the PWM timer can work in count-up mode, count-down mode or count-up-down mode.
            \item A hardware sync or software sync can trigger a reload on the PWM timer with a phase register. It will also trigger the prescaler's restart, so that the timer's clock can also be synced. The source of the hard sync can come from any GPIO or any other PWM timer's sync\_out. The source of the soft sync comes from writing toggle value to the \hyperref[fielddesc:MCPWMTIMER0SYNCSW]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_SYNC\_SW} bit.
        \end{itemize}
    \item PWM Operators 0, 1 and 2
        \begin{itemize}
            \item Every PWM operator has two PWM outputs: PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B. They can work independently, in symmetric and asymmetric configuration.
            \item Software, asynchronously override control of PWM signals.
            \item Configurable dead-time on rising and falling edges; each set up independently.
            \item All events can trigger CPU interrupts.
            \item Modulating of PWM output by high-frequency carrier signals, useful when gate drivers are insulated with a transformer.
            \item Period, time stamps and important control registers have shadow registers with flexible updating methods.
        \end{itemize}
    \item Fault Detection Module
        \begin{itemize}
            \item Programmable fault handling allocated on fault condition in both cycle-by-cycle mode and one-shot mode.
            \item A fault condition can force the PWM output to either high or low logic levels.
        \end{itemize}
    \item Capture Module
        \begin{itemize}
            \item Speed measurement of rotating machinery (for example, toothed sprockets sensed with Hall sensors)
            \item Measurement of elapsed time between position sensor pulses
            \item Period and duty-cycle measurement of pulse train signals
            \item Decoding current or voltage amplitude derived from duty-cycle-encoded signals of current/voltage sensors
            \item Three individual capture channels, each of which has a time-stamp register (32 bits)
            \item Selection of edge polarity and prescaling of input capture signal
            \item The capture timer can sync with a PWM timer or external signals.
            \item Interrupt on each of the three capture channels
        \end{itemize}
\end{itemize}
\clearpage

\section{Submodules}
\subsection{Overview}

This section lists the configuration parameters of key submodules. For information on adjusting a specific parameter, e.g., synchronization source of PWM timer, please refer to Section \ref{sec:mcpwm-timer-sub} for details.

\subsubsection{Prescaler Submodule}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.4\textwidth]{./17-MCPWM/figures/17-MCPWM-subm-presc.eps}
    \caption{Prescaler Submodule}
    \label{fig:mcpwm-subm-presc}
\end{figure}

Configuration option:
\begin{itemize}
    \item Scale the CRYPTO\_PWM\_CLK.
\end{itemize}

\subsubsection{Timer Submodule}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-subm-timer.eps}
    \caption{Timer Submodule}
    \label{fig:mcpwm-subm-timer}
\end{figure}

Configuration options:
\begin{itemize}
    \item Set the PWM timer frequency or period.
    \item Configure the working mode for the timer:
        \begin{itemize}
            \item Count-Up Mode: for asymmetric PWM outputs
            \item Count-Down Mode: for asymmetric PWM outputs
            \item Count-Up-Down Mode: for symmetric PWM outputs
        \end{itemize}
    \item Configure the the reloading phase (including the value and the direction) used during software and hardware synchronization.
    \item Synchronize the PWM timers with each other. Either hardware or software synchronization may be used.
    \item Configure the source of the PWM timer's the synchronization input to one of the seven sources below:
        \begin{itemize}
            \item The three PWM timer's synchronization outputs.
            \item Three synchronization signals from the GPIO matrix: PWMn\_SYNC0\_IN, PWMn\_SYNC1\_IN, PWMn\_SYNC2\_IN.
            \item No synchronization input signal selected
        \end{itemize}
    \item Configure the source of the PWM timer's synchronization output to one of the four sources below:
        \begin{itemize}
            \item Synchronization input signal
            \item Event generated when value of the PWM timer is equal to zero
            \item Event generated when value of the PWM timer is equal to period
            \item Event generated when writing toggling value to \hyperref[fielddesc:MCPWMTIMER0SYNCSW]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_SYNC\_SW} bit
        \end{itemize}
    \item Configure the method of period updating.
\end{itemize}

\subsubsection{Operator Submodule}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-subm-operator.eps}
    \caption{Operator Submodule}
    \label{fig:mcpwm-subm-operator}
\end{figure}

The configuration parameters of the operator submodule are shown in Table \ref{tab:mcpwm-param-operator}.

\input{./17-MCPWM/tables/17-MCPWM-para-operator__EN}

\subsubsection{Fault Detection Submodule}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.45\textwidth]{./17-MCPWM/figures/17-MCPWM-subm-fault-detect.eps}
    \caption{Fault Detection Submodule}
    \label{fig:mcpwm-fault-detect}
\end{figure}

Configuration options:
\begin{itemize}
    \item Enable fault event generation and configure the polarity of fault event generation for every fault signal
    \item Generate fault event interrupts
\end{itemize}

\subsubsection{Capture Submodule}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.45\textwidth]{./17-MCPWM/figures/17-MCPWM-subm-capture.eps}
    \caption{Capture Submodule}
    \label{fig:mcpwm-fault-capture}
\end{figure}

Configuration options:
\begin{itemize}
    \item Select the edge polarity and prescaling of the capture input.
    \item Set up a software-triggered capture.
    \item Configure the capture timer's sync trigger and sync phase.
    \item Software syncs the capture timer.
\end{itemize}

\subsection{PWM Timer Submodule}
\label{sec:mcpwm-timer-sub}

Each MCPWM module has three PWM timer submodules. Any of them can determine the necessary event timing for any of the three PWM operator submodules. Built-in synchronization logic allows multiple PWM timer submodules, in one or more MCPWM modules, to work together as a system, when using synchronization signals from the GPIO matrix.

\subsubsection{Configurations of the PWM Timer Submodule}

Users can configure the following functions of the PWM timer submodule:

\begin{itemize}
    \item Control how often events occur by specifying the PWM timer frequency or period.
    \item Configure a particular PWM timer to synchronize with other PWM timers or modules.
    \item Get a PWM timer in phase with other PWM timers or modules.
    \item Set one of the following timer counting modes: count-up, count-down, count-up-down.
    \item Change the rate of the PWM timer clock (PT\_clk) with a prescaler. Each timer has its own prescaler configured with \hyperref[fielddesc:MCPWMTIMER0PRESCALE]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_PRESCALE} of the register \hyperref[regdesc:MCPWMTIMER0CFG0REG]{MCPWM\_TIMER0\_CFG0\_REG}. The PWM timer increments or decrements at a slower pace, depending on the setting of this field.
\end{itemize}

\subsubsection{PWM Timer's Working Modes and Timing Event Generation}
\label{sec:mcpwm-modes-and-event-generation}

The PWM timer has three working modes, selected by the PWM\textcolor{red}{\it{x}} timer mode field:
\begin{itemize}
    \item Count-Up Mode: \\
    In this mode, the PWM timer increments from zero until reaching the value configured in the period field. Once done, the PWM timer returns to zero and starts increasing again. PWM period is equal to the value of the period field + 1.\\
    Note: The period field is \hyperref[fielddesc:MCPWMTIMER0PERIOD]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD} (\textcolor{red}{\it{x}} = 0, 1, 2), i.e., \hyperref[fielddesc:MCPWMTIMER0PERIOD]{MCPWM\_TIMER0\_PERIOD},\\ \hyperref[fielddesc:MCPWMTIMER1PERIOD]{MCPWM\_TIMER1\_PERIOD}, \hyperref[fielddesc:MCPWMTIMER2PERIOD]{MCPWM\_TIMER2\_PERIOD}.
    \item Count-Down Mode: \\
    The PWM timer decrements to zero, starting from the value configured in the period field. After reaching zero, it is set back to the period value. Then it starts to decrement again. In this case, the PWM period is also equal to the value of period field + 1.
    \item Count-Up-Down Mode: \\
    This is a combination of the two modes mentioned above. The PWM timer starts increasing from zero until the period value is reached. Then, the timer decreases back to zero. This pattern is then repeated. The PWM period is the result of (the value of the period field × 2 + 1).
\end{itemize}

Figures \ref{fig:mcpwm-up-wf} to \ref{fig:mcpwm-ud-su-wf} show PWM timer waveforms in different modes, including timer behavior during synchronization events. In Count-Up mode, the counting direction after synchronization is always counting up. while in Count-Down mode, the counting direction after synchronization is always counting down. In Count-Up-Down Mode, the counting direction after synchronization can be chosen by setting the \hyperref[fielddesc:MCPWMTIMER0PHASEDIRECTION]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_PHASE\_DIRECTION}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{./17-MCPWM/figures/17-MCPWM-up-wf.eps}
    \caption{Count-Up Mode Waveform}
    \label{fig:mcpwm-up-wf}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{./17-MCPWM/figures/17-MCPWM-down-wf.eps}
    \caption{Count-Down Mode Waveforms}
    \label{fig:mcpwm-d-wf}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{./17-MCPWM/figures/17-MCPWM-ud-sd-wf.eps}
    \caption{Count-Up-Down Mode Waveforms, Count-Down at  Synchronization Event}
    \label{fig:mcpwm-ud-sd-wf}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{./17-MCPWM/figures/17-MCPWM-ud-su-wf.eps}
    \caption{Count-Up-Down Mode Waveforms, Count-Up at Synchronization Event}
    \label{fig:mcpwm-ud-su-wf}
\end{figure}

\clearpage
When the PWM timer is running, it generates the following timing events periodically and automatically:
\begin{itemize}
    \item UTEP\\ The timing event generated when the PWM timer's value equals to the value of the period field\\(\hyperref[fielddesc:MCPWMTIMER0PERIOD]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD}) and when the PWM timer is increasing.
    \item UTEZ\\ The timing event generated when the PWM timer's value equals to zero and when the PWM timer is increasing.
    \item DTEP\\ The timing event generated when the PWM timer's value equals to the value of the period field\\(\hyperref[fielddesc:MCPWMTIMER0PERIOD]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD}) and when the PWM timer is decreasing.
    \item DTEZ\\ The timing event generated when the PWM timer's value equals to zero and when the PWM timer is decreasing.
\end{itemize}

Figures \ref{fig:mcpwm-te-u} to \ref{fig:mcpwm-te-ud} show the timing waveforms of U/DTEP and U/DTEZ.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-tep-tez-up.eps}
    \caption{UTEP and UTEZ Generation in Count-Up Mode}
    \label{fig:mcpwm-te-u}
\end{figure}

\clearpage
\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-tep-tez-down.eps}
    \caption{DTEP and DTEZ Generation in Count-Down Mode}
    \label{fig:mcpwm-te-d}
\end{figure}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-tep-tez-ud.eps}
    \caption{DTEP and UTEZ Generation in Count-Up-Down Mode}
    \label{fig:mcpwm-te-ud}
\end{figure}

Please note that in the Count-Up-Down Mode, when the counting direction is increasing, the timer range is [0, period value - 1], and when the counting direction is decreasing, the timer range is [period value, 1]. That is, in this mode, when synchronizing the timer to 0, decreasing counting direction will be illegal, namely, \hyperref[fielddesc:MCPWMTIMER0PHASEDIRECTION]{MCPWM\_TIMER\textcolor{red}{\it{n}}\_PHASE\_DIRECTION} cannot be set to 1. Similarly, when synchronizing the timer to period value, increasing counting direction will be illegal, namely, \hyperref[fielddesc:MCPWMTIMER0PHASEDIRECTION]{MCPWM\_TIMER\textcolor{red}{\it{n}}\_PHASE\_DIRECTION} cannot be set to 0. Therefore, when the timer is synchronized to 0, the counting direction can only be increasing, and \hyperref[fielddesc:MCPWMTIMER0PHASEDIRECTION]{MCPWM\_TIMER\textcolor{red}{\it{n}}\_PHASE\_DIRECTION} will be 0. When the timer is synchronized to the period value, the counting direction can only be decreasing, and \hyperref[fielddesc:MCPWMTIMER0PHASEDIRECTION]{MCPWM\_TIMER\textcolor{red}{\it{n}}\_PHASE\_DIRECTION} will be 1.


\clearpage
\subsubsection{PWM Timer Shadow Register}
\label{sec:mcpwm-shdw_reg}

The PWM timer's period register and the PWM timer's clock prescaler register have shadow registers. The purpose of a shadow register is to save a copy of the value to be written into the active register at a specific moment synchronized with the hardware. Both register types are defined as follows:
\begin{itemize}
    \item Active Register \\
    This register is directly responsible for controlling all actions performed by hardware.
    \item Shadow Register \\
    It acts as a temporary buffer for a value to be written to the active register. At a specific, user-configured point in time, the value saved in the shadow register is copied to the active register. Before this happens, the content of the shadow register has no direct effect on the controlled hardware. This helps to prevent spurious operation of the hardware, which may happen when a register is asynchronously modified by software. Both the shadow register and the active register have the same memory address. The software always writes into, or reads from the shadow register.

    The moment of updating the clock prescaler's active register is at the time when the timer starts operating. When \hyperref[fielddesc:MCPWMGLOBALUPEN]{MCPWM\_GLOBAL\_UP\_EN} is set to 1, the moment of updating the period active register can be selected by the following ways. By setting the update method register of \hyperref[fielddesc:MCPWMTIMER0PERIODUPMETHOD]{MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD\_UPMETHOD}, the update can start when the PWM timer is equal to zero, when the PWM timer is equal to period, at a synchronization moment, or immediately. Software can also trigger a globally forced update bit\\ \hyperref[fielddesc:MCPWMGLOBALFORCEUP]{MCPWM\_GLOBAL\_FORCE\_UP} which will prompt all registers in the module to be updated according to shadow registers.
\end{itemize}

\subsubsection{PWM Timer Synchronization and Phase Locking}
The PWM modules adopt a flexible synchronization method. Each PWM timer has a synchronization input and a synchronization output. The synchronization input can be selected from three synchronization outputs and three synchronization signals from the GPIO matrix. The synchronization output can be generated from the synchronization input signal, when the PWM timer's value is equal to period or zero, or software synchronization. Thus, the PWM timers can be chained together with their phase locked. During synchronization, the PWM timer clock prescaler will reset its counter in order to synchronize the PWM timer clock.

\subsection{PWM Operator Submodule}
The PWM Operator submodule has the following functions:
\begin{itemize}
    \item Generates a PWM signal pair, based on timing references obtained from the corresponding PWM timer.
    \item Each signal out of the PWM signal pair includes a specific pattern of dead time.
    \item Superimposes a carrier on the PWM signal, if configured to do so.
    \item Handles response under fault conditions.
\end{itemize}

Figure \ref{fig:mcpwm-operator} shows the block diagram of a PWM operator.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-operator}
    \caption{Submodules Inside the PWM Operator}
    \label{fig:mcpwm-operator}
\end{figure}

\clearpage
\subsubsection{PWM Generator Submodule}
\label{sec:mcpwm-gen-submodule}

\subparagraph{Purpose of the PWM Generator Submodule}
In this submodule, important timing events are generated or imported. The events are then converted into specific actions to generate the desired waveforms at the PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B outputs.

The PWM generator submodule performs the following actions:
\begin{itemize}
    \item Generation of timing events based on time stamps configured using the A and B registers. Events happen when the following conditions are satisfied:
    \begin{itemize}
        \item UTEA: the PWM timer is counting up and its value is equal to register A.
        \item UTEB: the PWM timer is counting up and its value is equal to register B.
        \item DTEA: the PWM timer is counting down and its value is equal to register A.
        \item DTEB: the PWM timer is counting down and its value is equal to register B.
    \end{itemize}
    \item Generation of U/DT1, U/DT2 timing events based on fault or synchronization events.
    \item Management of priority when these timing events occur concurrently.
    \item Qualification and generation of set, clear and toggle actions, based on the timing events.
    \item Controlling of the PWM duty cycle, depending on configuration of the PWM generator submodule.
    \item Handling of new time stamp values, using shadow registers to prevent glitches in the PWM cycle.
\end{itemize}

\subparagraph{PWM Operator Shadow Registers}
The time stamp registers A and B, as well as action configuration registers  \hyperref[regdesc:MCPWMGEN0AREG]{MCPWM\_GEN\textcolor{red}{\it{x}}\_A\_REG} and\\ \hyperref[regdesc:MCPWMGEN0BREG]{MCPWM\_GEN\textcolor{red}{\it{x}}\_B\_REG} are shadowed. Shadowing provides a way of updating registers in sync with the hardware.

When \hyperref[fielddesc:MCPWMGLOBALUPEN]{MCPWM\_GLOBAL\_UP\_EN} is set to 1, the shadow registers can be written to the active register at a specified time. The update method fields for time stamp registers A and B are \hyperref[fielddesc:MCPWMGEN0AUPMETHOD]{MCPWM\_GEN\_A\_UPMETHOD} and \hyperref[fielddesc:MCPWMGEN0BUPMETHOD]{MCPWM\_GEN\_B\_UPMETHOD}. The update method field for \hyperref[regdesc:MCPWMGEN0AREG]{MCPWM\_GEN\textcolor{red}{\it{x}}\_A\_REG} and \hyperref[regdesc:MCPWMGEN0BREG]{MCPWM\_GEN\textcolor{red}{\it{x}}\_B\_REG} is \hyperref[fielddesc:MCPWMGEN0CFGUPMETHOD]{MCPWM\_GEN\_CFG\_UPMETHOD}. Software can also trigger a globally forced update bit \\\hyperref[fielddesc:MCPWMGLOBALFORCEUP]{MCPWM\_GLOBAL\_FORCE\_UP} which will prompt all registers in the module to be updated according to shadow registers. For a description of the shadow registers, please see \ref{sec:mcpwm-shdw_reg}.

\subparagraph{Timing Events}
For convenience, all timing signals and events are summarized in Table \ref{tab:mcpwm-gen-in}.
\input{./17-MCPWM/tables/17-MCPWM-gen-in-evt__EN}

The purpose of a software-force event is to impose non-continuous or continuous changes on the PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B outputs. The change is done asynchronously. Software-force control is handled by the\\ \hyperref[regdesc:MCPWMGEN0FORCEREG]{MCPWM\_GEN\textcolor{red}{\it{x}}\_FORCE\_REG} registers.

The selection and configuration of T0/T1 in the PWM generator submodule is independent of the configuration of fault events in the fault handler submodule. A particular trip event may or may not be configured to cause trip action in the fault handler submodule, but the same event can be used by the PWM generator to trigger T0/T1 for controlling PWM waveforms.

It is important to know that when the PWM timer is in count-up-down mode, it will always decrement after a TEP event, and will always increment after a TEZ event. So when the PWM timer is in count-up-down mode, DTEP and UTEZ events will occur, while the events UTEP and DTEZ will never occur.

The PWM generator can handle multiple events at the same time. Events are prioritized by the hardware and relevant details are provided in Table \ref{tab:mcpwm-evt-prio-u} and Table \ref{tab:mcpwm-evt-prio-d}. Priority levels range from 1 (the highest) to 7 (the lowest). Please note that the priority of TEP and TEZ events depends on the PWM timer's direction.

If the value of A or B is set to be greater than the period, then U/DTEA and U/DTEB will never occur.

\input{./17-MCPWM/tables/17-MCPWM-evt-prio-u__EN}
\input{./17-MCPWM/tables/17-MCPWM-evt-prio-d__EN}

Notes:
\begin{enumerate}
   \item UTEP and UTEZ do not happen simultaneously. When the PWM timer is in count-up mode, UTEP will always happen one cycle earlier than UTEZ, as demonstrated in Figure \ref{fig:mcpwm-te-u}, so their action on PWM signals will not interrupt each other. When the PWM timer is in count-up-down mode, UTEP will not occur.
    \item DTEP and DTEZ do not happen simultaneously. When the PWM timer is in count-down mode, DTEZ will always happen one cycle earlier than DTEP, as demonstrated in Figure \ref{fig:mcpwm-te-d}, so their action on PWM signals will not interrupt each other. When the PWM timer is in count-up-down mode, DTEZ will not occur.
\end{enumerate}

\subparagraph{PWM Signal Generation}
The PWM generator submodule controls the behavior of outputs PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B when a particular timing event occurs. The timing events are further qualified by the PWM timer's counting direction (up or down). Knowing the counting direction, the submodule may then perform an independent action at each stage of the PWM timer counting up or down.

The following actions may be configured on outputs PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B:
\begin{itemize}
    \item Set High:\\
    Set the output of PWM\textcolor{red}{\it{x}}A or PWM\textcolor{red}{\it{x}}B to a high level.
    \item Clear Low:\\
    Clear the output of PWM\textcolor{red}{\it{x}}A or PWM\textcolor{red}{\it{x}}B by setting it to a low level.
    \item Toggle:\\
    Change the current output level of PWM\textcolor{red}{\it{x}}A or PWM\textcolor{red}{\it{x}}B to the opposite value. If it is currently pulled high, pull it low, or vice versa.
    \item Do Nothing:\\
    Keep both outputs PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B unchanged. In this state, interrupts can still be triggered.
\end{itemize}

The configuration of actions on outputs is done by using registers \hyperref[regdesc:MCPWMGEN0AREG]{MCPWN\_GEN\textcolor{red}{\it{x}}\_A\_REG} and \\\hyperref[regdesc:MCPWMGEN0BREG]{MCPWN\_GEN\textcolor{red}{\it{x}}\_B\_REG}. So, the action to be taken on each output is set independently. Also there is great flexibility in selecting actions to be taken on a given output based on events. More specifically, any event listed in Table \ref{tab:mcpwm-gen-in} can operate on either output PWM\textcolor{red}{\it{x}}A or PWM\textcolor{red}{\it{x}}B. To check out registers for particular generator 0, 1 or 2, please refer to register description in Section \ref{sec:mcpwm-reg-summ}.

\subparagraph{Waveforms for Common Configurations}
Figure \ref{fig:mcpwm-ud-gen-dt} presents the symmetric PWM waveform generated when the PWM timer is counting up and down. DC 0\%–100\% modulation can be calculated via the formula below:

\begin{center}
$Duty =  (Period - A) \div Period$
\end{center}

If A matches the PWM timer value and the PWM timer is incrementing, then the PWM output is pulled up. If A matches the PWM timer value while the PWM timer is decrementing, then the PWM output is pulled low.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-ud-gen-dt.eps}
    \caption{Symmetrical Waveform in Count-Up-Down Mode}
    \label{fig:mcpwm-ud-gen-dt}
\end{figure}

The PWM waveforms in Figures \ref{fig:mcpwm-u-gen-0} to \ref{fig:mcpwm-ud-gen-1} show some common PWM operator configurations. The following conventions are used in the figures:
\begin{itemize}
    \item Period A and B refer to the values written in the corresponding registers.
    \item PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B are the output signals of PWM Operator \textcolor{red}{\it{x}}.
\end{itemize}
\clearpage
\clearpage
\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-gen-up0.eps}
    \caption{Count-Up, Single Edge Asymmetric Waveform, with Independent Modulation on PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B — Active High}
    \label{fig:mcpwm-u-gen-0}
\end{figure}

The duty modulation for PWM\textcolor{red}{\it{x}}A is set by B, active high and proportional to B. \\
The duty modulation for PWM\textcolor{red}{\it{x}}B is set by A, active high and proportional to A.
\begin{center}
    $Period = (MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD + 1 ) \times T_{PT\_clk}$\\
\end{center}

\clearpage
\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\textwidth]{./17-MCPWM/figures/17-MCPWM-gen-up1.eps}
    \caption{Count-Up, Pulse Placement Asymmetric Waveform with Independent Modulation on PWM\textcolor{red}{\it{x}}A}
    \label{fig:mcpwm-u-gen-1}
\end{figure}

Pulses may be generated anywhere within the PWM cycle (zero – period). \\
PWM\textcolor{red}{\it{x}}A's high time duty is proportional to (B – A).

\begin{center}
    $Period = (MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD + 1 ) \times T_{PT\_clk}$\\
\end{center}
\clearpage
\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-gen-ud0.eps}
    \caption{Count-Up-Down, Dual Edge Symmetric Waveform, with Independent Modulation on PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B — Active High}
    \label{fig:mcpwm-ud-gen-0}
\end{figure}

The duty modulation for PWM\textcolor{red}{\it{x}}A is set by A, active high and proportional to A. \\
The duty modulation for PWM\textcolor{red}{\it{x}}B is set by B, active high and proportional to B. \\
Outputs PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B can drive independent switches.

\begin{center}
    $Period = (2 \times MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD ) \times T_{PT\_clk}$\\
\end{center}

\clearpage

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-gen-ud1.eps}
    \caption{Count-Up-Down, Dual Edge Symmetric Waveform, with Independent Modulation on PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B — Complementary}
    \label{fig:mcpwm-ud-gen-1}
\end{figure}

The duty modulation of PWM\textcolor{red}{\it{x}}A is set by A, is active high and proportional to A. \\
The duty modulation of PWM\textcolor{red}{\it{x}}B is set by B, is active low and proportional to B. \\
Outputs PWM\textcolor{red}{\it{x}} can drive upper/lower (complementary) switches. \\
Dead-time = B – A; Edge placement is fully programmable by software. Use the dead-time generator module if another edge delay method is required.

\begin{center}
    $Period = (2 \times MCPWM\_TIMER\textcolor{red}{\it{x}}\_PERIOD ) \times T_{PT\_clk}$\\
\end{center}

\subparagraph{Software-Force Events}
There are two types of software-force events inside the PWM generator:
\begin{itemize}
    \item Non-continuous-immediate (NCI) software-force events\\
    Such types of events are immediately effective on PWM outputs when triggered by software. The forcing is non-continuous, meaning the next active timing events will be able to alter the PWM outputs.
    \item Continuous (CNTU) software-force events\\
    Such types of events are continuous. The forced PWM outputs will continue until they are released by software. The events' triggers are configurable. They can be timing events or immediate events.
\end{itemize}

Figure \ref{fig:mcpwm-force-nci} shows a waveform of NCI software-force events. NCI events are used to force PWM\textcolor{red}{\it{x}}A output low. Forcing on PWM\textcolor{red}{\it{x}}B is disabled in this case.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-gen-force-nci.eps}
    \caption{Example of an NCI Software-Force Event on PWM\textcolor{red}{\it{x}}A}
    \label{fig:mcpwm-force-nci}
\end{figure}
\clearpage

Figure \ref{fig:mcpwm-force-cn} shows a waveform of CNTU software-force events. UTEZ events are selected as triggers for CNTU software-force events. CNTU is used to force the PWM\textcolor{red}{\it{x}}B output low. Forcing on PWM\textcolor{red}{\it{x}}A is disabled.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-gen-force-cn.eps}
    \caption{Example of a CNTU Software-Force Event on PWM\textcolor{red}{\it{x}}B}
    \label{fig:mcpwm-force-cn}
\end{figure}
\clearpage

\subsubsection{Dead Time Generator Submodule}
\label{sec:mcpwm-dead-time-gen}
\subparagraph{Purpose of the Dead Time Generator Submodule}
Several options to generate signals on PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B outputs, with a specific placement of signal edges, have been discussed in section \ref{sec:mcpwm-gen-submodule}. The required dead time is obtained by altering the edge placement between signals and by setting the signal's duty cycle. Another option is to control the dead time using a specialized submodule – the Dead Time Generator.

The key functions of the dead time generator submodule are as follows:
\begin{itemize}
    \item Generating signal pairs (PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B) with a dead time from a single PWM\textcolor{red}{\it{x}}A input
    \item Creating a dead time by adding delay to signal edges:
    \begin{itemize}
        \item Rising edge delay (RED)
        \item Falling edge delay (FED)
    \end{itemize}
    \item Configuring the signal pairs to be:
    \begin{itemize}
        \item Active high complementary (AHC)
        \item Active low complementary (ALC)
        \item Active high (AH)
        \item Active low (AL)
    \end{itemize}
    \item This submodule may also be bypassed, if the dead time is configured directly in the generator submodule.
\end{itemize}

\subparagraph{Dead Time Generator's Shadow Registers}
Delay registers RED and FED are shadowed with registers \hyperref[regdesc:MCPWMDT0REDCFGREG]{MCPWM\_DT\textcolor{red}{\it{x}}\_RED\_CFG\_REG} and \\\hyperref[regdesc:MCPWMDT0FEDCFGREG]{MCPWM\_DT\textcolor{red}{\it{x}}\_FED\_CFG\_REG}. When \hyperref[fielddesc:MCPWMGLOBALUPEN]{MCPWM\_GLOBAL\_UP\_EN} is set to 1, the shadow registers can be written to the active register at specified time. The update method register for \hyperref[regdesc:MCPWMDT0REDCFGREG]{MCPWM\_DT\textcolor{red}{\it{x}}\_RED\_CFG\_REG} is \\\hyperref[fielddesc:MCPWMDT0REDUPMETHOD]{MCPWM\_DT\_RED\_UPMETHOD}. The update method register for \hyperref[regdesc:MCPWMDT0FEDCFGREG]{MCPWM\_DT\textcolor{red}{\it{x}}\_FED\_CFG\_REG} is \\\hyperref[fielddesc:MCPWMDT0FEDUPMETHOD]{MCPWM\_DT\_FED\_UPMETHOD}. The Software can also trigger a globally forced update bit \hyperref[fielddesc:MCPWMGLOBALFORCEUP]{MCPWM\_GLOBAL\_FORCE\_UP} which will prompt all registers in the module to be updated according to shadow registers.For the description of shadow registers, please see section \ref{sec:mcpwm-shdw_reg}.

\subparagraph{Highlights for Operation of the Dead Time Generator}
Options for setting up the dead-time submodule are shown in Figure \ref{fig:mcpwm-conf-dt}.


\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.8\textwidth]{./17-MCPWM/figures/17-MCPWM-dt.eps}
    \caption{Options for Setting up the Dead Time Generator Submodule}
    \label{fig:mcpwm-conf-dt}
\end{figure}


\clearpage
S0-S8 in the figure above are switches controlled by fields in register \hyperref[regdesc:MCPWMDT0CFGREG]{MCPWM\_DT\textcolor{red}{\it{x}}\_CFG\_REG} shown in Table \ref{tab:mcpwm-s-dt}.

\input{./17-MCPWM/tables/17-MCPWM-s-dt__EN}


All switch combinations are supported, but not all of them represent the typical modes of use. Table \ref{tab:mcpwm-mod-dt} documents some typical dead time configurations. In these configurations the position of S4 and S5 sets PWM\textcolor{red}{\it{x}}A as the common source of both falling-edge and rising-edge delay. The modes presented in table \ref{tab:mcpwm-mod-dt} may be categorized as follows:

\input{./17-MCPWM/tables/17-MCPWM-mod-dt__EN}


\clearpage

\begin{tiplisting}
For all the modes above, the position of the binary switches S4 to S8 is set to 0.
\end{tiplisting}

\begin{itemize}
    \item {\bfseries Mode 1: Bypass delays on both falling (FED) as well as raising edge (RED)}\\
    In this mode the dead time submodule is disabled. Signals PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B pass through without any modifications.
    \item {\bfseries Mode 2-5: Classical Dead Time Polarity Settings}\\
    These modes represent typical configurations of polarity and should cover the active-high/low modes in available industry power switch gate drivers. The typical waveforms are shown in Figures \ref{fig:mcpwm-wf-dt-ahc} to \ref{fig:mcpwm-wf-dt-al}.
    \item {\bfseries Modes 6 and 7: Bypass delay on falling edge (FED) or rising edge (RED)}\\
    In these modes, either RED (Rising Edge Delay) or FED (Falling Edge Delay) is bypassed. As a result, the corresponding delay is not applied.
\end{itemize}



\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.8\textwidth]{./17-MCPWM/figures/17-MCPWM-dt-ahc.eps}
    \caption{Active High Complementary (AHC) Dead Time Waveforms}
    \label{fig:mcpwm-wf-dt-ahc}
\end{figure}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.8\textwidth]{./17-MCPWM/figures/17-MCPWM-dt-alc.eps}
    \caption{Active Low Complementary (ALC) Dead Time Waveforms}
    \label{fig:mcpwm-wf-dt-alc}
\end{figure}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.8\textwidth]{./17-MCPWM/figures/17-MCPWM-dt-ah.eps}
    \caption{Active High (AH) Dead Time Waveforms}
    \label{fig:mcpwm-wf-dt-ah}
\end{figure}

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.8\textwidth]{./17-MCPWM/figures/17-MCPWM-dt-al.eps}
    \caption{Active Low (AL) Dead Time Waveforms}
    \label{fig:mcpwm-wf-dt-al}
\end{figure}

\clearpage
Rising edge (RED) and falling edge (FED) delays may be set up independently. The delay value is programmed using the 16-bit registers \hyperref[fielddesc:MCPWMDT0RED]{MCPWM\_DT\textcolor{red}{\it{x}}\_RED} and MCPWM\_DT\textcolor{red}{\it{x}}\_FED. The register value represents the number of clock (DT\_clk) periods by which a signal edge is delayed. DT\_CLK can be selected from PWM\_clk or PT\_clk through register \hyperref[fielddesc:MCPWMDT0CLKSEL]{MCPWM\_DT\textcolor{red}{\it{x}}\_CLK\_SEL}.

To calculate the delay on falling edge (FED) and rising edge (RED), use the following formulas:
\begin{center}
    \item $FED = MCPWM\_DT\textcolor{red}{\it{x}}\_FED \times T_{DT\_clk}$
    \item $RED = MCPWM\_DT\textcolor{red}{\it{x}}\_RED \times T_{DT\_clk}$
\end{center}

\clearpage

\subsubsection{PWM Carrier Submodule}
The coupling of PWM output to a motor driver may need isolation with a transformer. Transformers deliver only AC signals, while the duty cycle of a PWM signal may range anywhere from 0\% to 100\%. The PWM carrier submodule passes such a PWM signal through a transformer by using a high frequency carrier to modulate the signal.

\subparagraph{Function Overview}
The following key characteristics of this submodule are configurable:
\begin{itemize}
    \item Carrier frequency
    \item Pulse width of the first pulse
    \item Duty cycle of the second and the subsequent pulses
    \item Enabling/disabling the carrier function
\end{itemize}

\subparagraph{Operational Highlights}
The PWM carrier clock (PC\_clk) is derived from PWM\_clk. The frequency and duty cycle are configured by the \hyperref[fielddesc:MCPWMCARRIER0PRESCALE]{MCPWM\_CARRIER\textcolor{red}{\it{x}}\_PRESCALE} and \hyperref[fielddesc:MCPWMCARRIER0DUTY]{MCPWM\_CARRIER\textcolor{red}{\it{x}}\_DUTY} bits in the \hyperref[regdesc:MCPWMCARRIER0CFGREG]{MCPWM\_CARRIER\textcolor{red}{\it{x}}\_CFG\_REG} register. The purpose of one-shot pulses is to provide high-energy impulse to reliably turn on the power switch. Subsequent pulses sustain the power-on status. The width of a one-shot pulse is configurable with the\\ \hyperref[fielddesc:MCPWMCARRIER0OSHTWTH]{MCPWM\_CARRIER\textcolor{red}{\it{x}}\_OSHTWTH} bits. Enabling/disabling of the carrier submodule is done with the\\ \hyperref[fielddesc:MCPWMCARRIER0EN]{MCPWM\_CARRIER\textcolor{red}{\it{x}}\_EN} bit.

\subparagraph{Waveform Examples}
Figure \ref{fig:mcpwm-carrier-0} shows an example of waveforms, where a carrier is superimposed on original PWM pulses. This figure do not show the first one-shot pulse and the duty-cycle control. Related details are covered in the following two sections.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.7\textwidth]{./17-MCPWM/figures/17-MCPWM-carrier-0.eps}
    \caption{Example of Waveforms Showing PWM Carrier Action}
    \label{fig:mcpwm-carrier-0}
\end{figure}

\subparagraph{One-Shot Pulse}
The width of the first pulse is configurable. It may assume one of 16 possible values and is described by the formula below:

\begin{center}
$T_{1stpulse} = T_{PWM\_clk} \times 8 \times (MCPWM\_CARRIER\textcolor{red}{\it{x}}\_PRESCALE+1) \times (MCPWM\_CARRIER\textcolor{red}{\it{x}}\_OSHTWTH+1)$ \\
\end{center}

Where:
\begin{itemize}
    \item $T_{PMW\_clk}$ is the period of the PWM clock (PWM\_clk).
    \item $(MCPWM\_CARRIER\textcolor{red}{\it{x}}\_OSHTWTH+1)$ is the width of the first pulse (whose value ranges from 1 to 16).
    \item $(MCPWM\_CARRIER\textcolor{red}{\it{x}}\_PRESCALE+1)$ is the PWM carrier clock's (PC\_clk) prescaler value.
\end{itemize}

The first one-shot pulse and subsequent sustaining pulses are shown in Figure \ref{fig:mcpwm-carrier-1}.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-carrier-1.eps}
    \caption{Example of the First Pulse and the Subsequent Sustaining Pulses of the PWM Carrier Submodule}
    \label{fig:mcpwm-carrier-1}
\end{figure}

\subparagraph{Duty Cycle Control}
After issuing the first one-shot pulse, the remaining PWM signal is modulated according to the carrier frequency. Users can configure the duty cycle of this signal. Tuning of duty may be required, so that the signal passes through the isolating transformer and can still operate (turn on/off) the motor drive, changing rotation speed and direction.

The duty cycle may be set to one of seven values, using  \hyperref[fielddesc:MCPWMCARRIER0DUTY]{MCPWM\_CARRIER\textcolor{red}{\it{x}}\_DUTY}, or bits [7:5] of register\\ \hyperref[regdesc:MCPWMCARRIER0CFGREG]{MCPWM\_CARRIER\textcolor{red}{\it{x}}\_CFG\_REG}.

Below is the formula for calculating the duty cycle:

\begin{center}
$Duty =  MCPWM\_CARRIER\textcolor{red}{\it{x}}\_DUTY \div 8$
\end{center}

All seven settings of the duty cycle are shown in Figure \ref{fig:mcpwm-carrier-2}.

\begin{figure}[ht!]
    \centering
    \includegraphics[width=0.9\textwidth]{./17-MCPWM/figures/17-MCPWM-carrier-2.eps}
    \caption{Possible Duty Cycle Settings for Sustaining Pulses in the PWM Carrier Submodule}
    \label{fig:mcpwm-carrier-2}
\end{figure}

\subsubsection{Fault Handler Submodule}
Each MCPWM peripheral is connected to three fault signals (FAULT0, FAULT1 and FAULT2) which are sourced from the GPIO matrix. These signals are intended to indicate external fault conditions, and may be preprocessed by the fault detection submodule to generate fault events. Fault events can then execute the user code to control MCPWM outputs in response to specific faults.

\subparagraph{Function of Fault Handler Submodule}
The key actions performed by the fault handler submodule are:
\begin{itemize}
    \item Forcing outputs PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B, upon detected fault, to one of the following states:
    \begin{itemize}
        \item High
        \item Low
        \item Toggle
        \item No action taken
    \end{itemize}
    \item Execution of one-shot trip (OST) upon detection of over-current conditions/short circuits.
    \item Cycle-by-cycle tripping (CBC) to provide current-limiting operation.
    \item Allocation of either one-shot or cycle-by-cycle operation for each fault signal.
    \item Generation of interrupts for each fault input.
    \item Support for software-force tripping.
    \item Enabling or disabling of submodule function as required.
\end{itemize}

\subparagraph{Operation and Configuration Tips}
This section provides the operational tips and set-up options for the fault handler submodule.

Fault signals coming from pins are sampled and synced in the GPIO matrix. In order to guarantee the successful sampling of fault pulses, each pulse duration must be at least two APB clock cycles. The fault detection submodule will then sample fault signals by using PWM\_clk. So, the duration of fault pulses coming from GPIO matrix must be at least one PWM\_clk cycle. Differently put, regardless of the period relation between APB clock and PWM\_clk, the width of fault signal pulses on pins must be at least equal to the sum of two APB clock cycles and one PWM\_clk cycle.

Each level of fault signals, FAULT0 to FAULT2, can be used by the fault handler submodule to generate fault events (fault\_event0 to fault\_event2). Every fault event can be configured individually to provide CBC action, OST action, or none.

\begin{itemize}
    \item {\bfseries Cycle-by-Cycle (CBC) action:}\\
    When CBC action is triggered, the state of PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B will be changed immediately according to the configuration of fields \hyperref[fielddesc:MCPWMFH0ACBCU]{MCPWM\_FH\textcolor{red}{\it{x}}\_A\_CBC\_U/D} and \hyperref[fielddesc:MCPWMFH0BCBCU]{MCPWM\_FH\textcolor{red}{\it{x}}\_B\_CBC\_U/D}. Different actions can be indicted when the PWM timer is incrementing or decrementing. Different CBC action interrupts can be triggered for different fault events. Status field \hyperref[fielddesc:MCPWMFH0CBCON]{MCPWM\_FH\textcolor{red}{\it{x}}\_CBC\_ON} indicates whether a CBC action is on or off.
   When the fault event is no longer present, CBC actions on PWM\textcolor{red}{\it{x}}A/B will be cleared at a specified point, which is either a D/UTEP or D/UTEZ event. Field \hyperref[fielddesc:MCPWMFH0CBCPULSE]{MCPWM\_FH\textcolor{red}{\it{x}}\_CBCPULSE} determines at which event PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B will be able to resume normal actions. Therefore, in this mode, the CBC action is cleared or refreshed upon every PWM cycle.
    \item {\bfseries One-Shot (OST) action:}\\
	When OST action is triggered, the state of PWM\textcolor{red}{\it{x}}A and PWM\textcolor{red}{\it{x}}B will be changed immediately, depending on the setting of fields \hyperref[fielddesc:MCPWMFH0AOSTU]{MCPWM\_FH\textcolor{red}{\it{x}}\_A\_OST\_U/D} and \hyperref[fielddesc:MCPWMFH0BOSTU]{MCPWM\_FH\textcolor{red}{\it{x}}\_B\_OST\_U/D}. Different actions can be configured when PWM timer is incrementing or decrementing. Different OST action interrupts can be triggered form different fault events. Status field \hyperref[fielddesc:MCPWMFH0OSTON]{MCPWM\_FH\textcolor{red}{\it{x}}\_OST\_ON} indicates whether an OST action is on or off.
    The OST actions on PWM\textcolor{red}{\it{x}}A/B are not automatically cleared when the fault event is no longer present. One-shot actions must be cleared manually by setting the rising edge of the \hyperref[fielddesc:MCPWMFH0CLROST]{MCPWM\_FH\textcolor{red}{\it{x}}\_CLR\_OST} bit.
\end{itemize}

\subsection{Capture Submodule}
\subsubsection{Introduction}
The capture submodule contains three complete capture channels. Channel inputs CAP0, CAP1 and CAP2 are sourced from the GPIO matrix. Thanks to the flexibility of the GPIO matrix, CAP0, CAP1 and CAP2 can be configured from any pin input. Multiple capture channels can be sourced from the same pin input, while prescaling for each channel can be set differently. Also, capture channels are sourced from different pins. This provides several options for handling capture signals by hardware in the background, instead of having them processed directly by the CPU.
A capture submodule has the following independent key resources:
\begin{itemize}
    \item One 32-bit timer (counter) which can be synchronized with the PWM timer, another submodule or software.
    \item Three capture channels, each equipped with a 32-bit time-stamp and a capture prescaler.
    \item Independent edge polarity (rising/falling edge) selection for any capture channel.
    \item Input capture signal prescaling (from 1 to 256).
    \item Interrupt capabilities on any of the three capture events.
\end{itemize}

\subsubsection{Capture Timer}
The capture timer is a 32-bit counter incrementing continuously. It is enabled by setting \hyperref[fielddesc:MCPWMCAPTIMEREN]{MCPWM\_CAP\_TIMER\_EN} to 1. Its operating clock source is APB\_CLK. When \hyperref[fielddesc:MCPWMCAPSYNCIEN]{MCPWM\_CAP\_SYNCI\_EN} is configured, the counter will be loaded with phase stored in register\\\hyperref[regdesc:MCPWMCAPTIMERPHASEREG]{MCPWM\_CAP\_TIMER\_PHASE\_REG} at the time of a sync event. Sync events can select from PWM timers sync-out, PWM module sync-in by configuring \hyperref[fielddesc:MCPWMCAPSYNCISEL]{MCPWM\_CAP\_SYNCI\_SEL}. Sync event can also generate by setting \hyperref[fielddesc:MCPWMCAPSYNCSW]{MCPWM\_CAP\_SYNC\_SW}. The capture timer provides timing references for all three capture channels.

\subsubsection{Capture Channel}
The capture signal coming to a capture channel will be inverted first, if needed, and then prescaled. Each capture channel has a prescaler register of \hyperref[fielddesc:MCPWMCAP0PRESCALE]{MCPWM\_CAP\textcolor{red}{\it{x}}\_PRESCALE}. Finally, specified edges of preprocessed capture signal will trigger capture events. Setting \hyperref[fielddesc:MCPWMCAP0EN]{MCPWM\_CAP\textcolor{red}{\it{x}}\_EN} to enable a capture channel. The capture event occurs at the time selected by the \hyperref[fielddesc:MCPWMCAP0MODE]{MCPWM\_CAP\textcolor{red}{\it{x}}\_MODE}. When a capture event occurs, the capture timer's value is stored in time-stamp register \hyperref[regdesc:MCPWMCAPCH0REG]{MCPWM\_CAP\_CH\textcolor{red}{\it{x}}\_REG}. Different interrupts can be generated for different capture channels at capture events. The edge that triggers a capture event is recorded in register \hyperref[fielddesc:MCPWMCAP0EDGE]{MCPWM\_CAP\textcolor{red}{\it{x}}\_EDGE}. The capture event can be also forced by software setting \hyperref[fielddesc:MCPWMCAP0SW]{MCPWM\_CAP\textcolor{red}{\it{x}}\_SW}.


% >>> If your module has no registers, delete sample text
%       for Sections Register Summary and Registers below <<<

%%%%%%%%%%%%%%%%%%%%%%%%
%%% Register Summary %%%
%%%%%%%%%%%%%%%%%%%%%%%%

% >>> Update the sample text below and insert
%     the info on register summary into the subfile <<<

\clearpage
\hypertarget{mcpwm-reg-summ}{}
\section{Register Summary}
\label{sec:mcpwm-reg-summ}

The addresses in this section are relative to Motor Control PWM0 and Motor Control PWM1 base address provided in Table \ref{tab:sysmem-base-address} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.

The abbreviations given in Column \textbf{Access} are explained in Section \hyperref[glossary-access-types]{\textit{Access Types for Registers}}.

\subfile{\modulefiles/17-MCPWM-reg-summ__EN}


%%%%%%%%%%%%%%%%%
%%% Registers %%%
%%%%%%%%%%%%%%%%%

% >>> Update the sample text below and insert
%      the info on registers into the subfile <<<

\clearpage
\section{Registers}

The addresses in this section are relative to Motor Control PWM0 and Motor Control PWM1 base address provided in Table \ref{tab:sysmem-base-address} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.

\subfile{\modulefiles/17-MCPWM-reg__EN}


\end{document}
