\begin{longtable}[c]{|l|m{10cm}|}
    \caption{移位指令}
    \label{tab:pie-shift-instructions}\\
    \hline
    \rowcolor{lightgray}
    \textbf{指令} & \textbf{定义} \\\hline
    \endfirsthead
    \rowcolor{lightgray}
    \textbf{指令} & \textbf{定义} \\\hline
    \endhead
    \endfoot
    
    \hyperref[instruction:EESRCQ]{EE.SRC.Q} & 16-字节拼接后逻辑右移，移位数值由 SAR\_BYTE 寄存器决定。
    \\\hline
    \hyperref[instruction:EESRCQQUP]{EE.SRC.Q.QUP} & 16-字节拼接后逻辑右移，移位数值由 SAR\_BYTE 寄存器决定。同时保存高 8-字节数据。
    \\\hline
    \hyperref[instruction:EESRCQLDXP]{EE.SRC.Q.LD.XP} & 16-字节拼接后逻辑右移，移位数值由 SAR\_BYTE 寄存器决定。同时从内存读取 16-字节数据，并对读地址进行寄存器加法操作。
    \\\hline
    \hyperref[instruction:EESRCQLDIP]{EE.SRC.Q.LD.IP} & 16-字节拼接后逻辑右移，移位数值由 SAR\_BYTE 寄存器决定。同时从内存读取 16-字节数据，并对读地址进行立即数加法操作。
    \\\hline
    \hyperref[instruction:EESLCI2Q]{EE.SLCI.2Q} & 16-字节拼接后逻辑左移，移位数值由立即数决定。
    \\\hline
    \hyperref[instruction:EESLCXXP2Q]{EE.SLCXXP.2Q} & 16-字节拼接后逻辑左移，移位数值由 AR 寄存器内数值决定。
    \\\hline
    \hyperref[instruction:EESRCI2Q]{EE.SRCI.2Q} & 16-字节拼接后逻辑右移，移位数值由立即数决定。
    \\\hline
    \hyperref[instruction:EESRCXXP2Q]{EE.SRCXXP.2Q} & 16-字节拼接后逻辑右移，移位数值由 AR 寄存器内数值决定。
    \\\hline
    \hyperref[instruction:EESRCQ128STINCP]{EE.SRCQ.128.ST.INCP} & 16-字节拼接后逻辑右移，并将移位后得到的 16-字节写入内存。
    \\\hline
    \hyperref[instruction:EEVSR32]{EE.VSR.32} & 4-字节向量算术右移。
    \\\hline
    \hyperref[instruction:EEVSL32]{EE.VSL.32} & 4-字节向量算术左移。
    \\\hline
\end{longtable}