\begin{longtable}[c]{|l|m{12cm}|}
    \caption{读内存指令}
    \label{tab:pie-load-instructions}\\
    \hline
    \rowcolor{lightgray}
    \textbf{指令} & \textbf{定义} \\\hline
    \endfirsthead
    \rowcolor{lightgray}
    \textbf{指令} & \textbf{定义} \\\hline
    \endhead
    \endfoot
    
    \hyperref[instruction:LDQR]{LD.QR} & 将 16-字节数据加载至 QR 寄存器。
    \\\hline
    \hyperref[instruction:EEVLD128XP]{EE.VLD.128.XP} & 读取 16-字节数据，随后访存地址加上 AR 寄存器内数值。
    \\\hline
    \hyperref[instruction:EEVLD128IP]{EE.VLD.128.IP} & 读取 16-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EEVLDH64XP]{EE.VLD.[H/L].64.XP} & 读取 8-字节数据，随后访存地址加上 AR 寄存器内数值。
    \\\hline
    \hyperref[instruction:EEVLDH64IP]{EE.VLD.[H/L].64.IP} & 读取 8-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EEVLDBC8]{EE.VLDBC.[8/16/32]} & 读取 1-字节/2-字节/4-字节数据。
    \\\hline
    \hyperref[instruction:EEVLDBC8XP]{EE.VLDBC.[8/16/32].XP} & 读取 1-字节/2-字节/4-字节数据，随后访存地址加上 AR 寄存器内数据。
    \\\hline
    \hyperref[instruction:EEVLDBC8IP]{EE.VLDBC.[8/16/32].IP} & 读取 1-字节/2-字节/4-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EEVLDHBC16INCP]{EE.VLDHBC.16.INCP} & 读取 16-字节数据，随后访存地址加上 16。
    \\\hline
    \hyperref[instruction:EELDF64XP]{EE.LDF.[64/128].XP} & 读取 8-字节/16-字节数据，随后访存地址加上 AR 寄存器内数值。
    \\\hline
    \hyperref[instruction:EELDF64IP]{EE.LDF.[64/128].IP} & 读取 8-字节/16-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EELD128USARXP]{EE.LD.128.USAR.XP} & 读取 16-字节数据，随后访存地址加上 AR 寄存器内数值。
    \\\hline
    \hyperref[instruction:EELD128USARIP]{EE.LD.128.USAR.IP} & 读取 16-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EELDQAU8128XP]{EE.LDQA.U8.128.[XP/IP]} & 读取 16-字节数据，按照 1-字节大小分片，随后 0 扩展为 20-bit 数据写入 QACC\_H 和 QACC\_L 寄存器中，最后在访存地址上加上 AR 寄存器内数值/立即数。
    \\\hline
    \hyperref[instruction:EELDQAU16128XP]{EE.LDQA.U16.128.XP} & 读取 16-字节数据，按照 2-字节大小分片，随后 0 扩展为 40-bit 数据写入 QACC\_H 和 QACC\_L 寄存器中，最后在访存地址上加上 AR 寄存器内数值/立即数。
    \\\hline
    \hyperref[instruction:EELDQAS8128XP]{EE.LDQA.S8.128.XP} & 读取 16-字节数据，按照 1-字节大小分片，随后符号位扩展为 20-bit 数据写入 QACC\_H 和 QACC\_L 寄存器中，最后在访存地址上加上 AR 寄存器内数值/立即数。
    \\\hline
    \hyperref[instruction:EELDQAS16128XP]{EE.LDQA.S16.128.XP} & 读取 16-字节数据，按照 1-字节大小分片，随后符号位扩展为 40-bit 数据写入 QACC\_H 和 QACC\_L 寄存器中，最后在访存地址上加上 AR 寄存器内数值/立即数。
    \\\hline
    \hyperref[instruction:EELDQACCHH32IP]{EE.LD.QACC\_[H/L].H.32.IP} & 读取 4-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EELDQACCHL128IP]{EE.LD.QACC\_[H/L].L.128.IP} & 读取 16-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EELDACCXIP]{EE.LD.ACCX.IP} & 读取 8-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EELDUASTATEIP]{EE.LD.UA\_STATE.IP} & 读取 16-字节数据，随后访存地址加上立即数。
    \\\hline
    \hyperref[instruction:EELDXQ32]{EE.LDXQ.32} & 先更新访存地址，随后读取 4-字节数据。
    \\\hline
\end{longtable}