% Doc: LaTeX Template for TRMs by Espressif
% Author: Kirill
% Version: 2025-04-07 v3.1
% See Changelog down below

% P?: marks a preamble setting (or important notice) number

\ProvidesPackage{preamble-trm-module}[LaTeX package defining a TRM chapter preamble]


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Enable chip-spec-settings %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{chip-spec-settings}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Import Shared Preamble %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\RequirePackage{preamble-trm-repo}


%%%%%%%%%%%%
%%% Text %%%
%%%%%%%%%%%%

\usepackage{listings}
% Code highlighting style definition based on
% https://tex.stackexchange.com/questions/57121/latex-source-code-listing
\lstset{%
    basicstyle=\small\ttfamily,
    language={[LaTeX]TeX}, numbers=left, numberstyle=\tiny, % number style
    breaklines=true,
    xleftmargin=8mm,
    backgroundcolor=\color{gray!5},
    frameround=fttt,escapeinside=??,
    morekeywords={% Give key words here keywords
        maketitle,
        subsection, subsubsection, paragraph,
        multirow, multicolumn, makecell,
        rowcolor,
        endhead, endfirsthead,
        endfoot, endlastfoot,
        tnote,
        TopicLine, Topic, TopicSetVPos, TopicSetContinuationCode,
        hyperref, hypertarget,
        DeclareFloatingEnvironment, AtBeginDocument
    },
    keywordstyle=\color[rgb]{0,0,1},                    % keywords
    commentstyle=\color[rgb]{0.133,0.545,0.133},    % comments
    stringstyle=\color[rgb]{0.627,0.126,0.941}  % strings
}%

\usepackage[hashEnumerators = true, % if true, enables use of hash symbols (#) as ordered item list markers
            smartEllipses = false, % if true, converts three dots into a proper ellipsis
            pipeTables = true, % if true, enables the php Markdown table syntax extension
            %hybrid = true, % if true, you can use LaTeX commands along with markdown
                            % not recommended as the aim is to allow engineers to
                            % include existing markdown content and not mix both
            %html = true, % if true, html tags are stripped, otherwise tags are preserved
                          % in any case, html tags are ignored by compiler
            fencedCode = true, % if true, turns code inside ``` ... ``` into a code block
                               % you can also use indents to achieve the same effect
            underscores = false, % if true, underscores interpreted as italics marker
                                 % keep false and use asterisks for italics marker instead
            codeSpans = true, % if true, backticks interpreted as monospace font marker
           ]{markdown}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Figure and table numbering %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage[bookmarksopen=true,bookmarksnumbered=true]{hyperref}


%%%%%%%%%%%%%%%%%
%%% Changelog %%%
%%%%%%%%%%%%%%%%%

% 2025-04-07 v3.1 (Maintainer: Wang Ziyan)
% - Merged with preamble-trm-repo.sty to avoid maintaining duplicate configurations

% 2024-11-15 v3.0 (Maintainer: Mo Feifei)
% - Added the Parts and Chapters section
% - Added the Acrobat Menu section
% - Added the Enable chip-spec-settings section
% - Moved module project-specific configurations to TRM-Module-Template > chip-spec-settings.sty
% (note: in esp-technical-reference-manual-latex, the chip-spec-settings.sty package for compiling TRM main files are reused)

% 2024-04-24 v2.3 (Maintainer: Mo Feifei)
% - Updated and enabled the "DRADT/COFNIDENTIAL" watermark and footer

% 2024-01-12 v2.2 (Maintainer: Mo Feifei)
% - Redefined the formatting of table of contents to adapt to the document class change
% - Updated font from Helvetica Neue to Maison Neue

% 2022-12-08 v2.1
% - Added support for \doclink commands
% - Fixed issue of text justification compared to TRM-Repo (see DOC-3479)
% - Added links to Section "Abbreviations for Registers"
% - Added new lines at the end of multiple .tex files for better compatibility with git

% 2022-05-25 v2.0.1
% - Added the variable docprojtype
% - Included package tagging and links to related documentation
% - Adding initial checks not required at this point

% 2021-10-28 v2.0.0
% - Removed support for ShareLaTeX compatibility
% - Removed unused package topiclongtable as it breaks compatibility with ShareLatex
% - Added package tagging
% - Made body text left-aligned, not justified
% - Added extended todo functionality
% - Added support for markdown
% - Redesigned and improved outline-template.tex and draft-template.tex

% 2021-10-28
% Created ESP8684-TRM-Module-Template by forking ESP32-C3-TRM-Module-Template v0.6
% Below are notes for ESP32-C3-TRM-Module-Template

% 2021-10-28 v1.1.3
% - Moved \hypertarget above \section for more precise navigation
% - Added instructions in root files
% - Standardized watermark settings across projects
% - Updated workflow for archiving the project
% - Fixed issues with wrong reference text

% 2021-04-01 v1.1.2
% - In preamble, added code for archiving the project
% - In preamble, updated watermark code

% 2021-03-10 v1.1.1
% - Added the package extdash to define non-breakable hyphens (RISC-V)

% 2020-11-11 v1.1.0
% - Preamble is expanded and improved using the ideas developed for module datasheet repo preamble
% - Both language versions are now stored in the same TRM module project
% - All instructions are moved into a separate project

% 2020-07-16 v1.0.1
% - Minor updates, added draft-template-cn.tex

% 2020-07-16 v1.0.0
% - Drastically improved file organization and introduction of naming conventions
% - Structure of all files is now much more concise, transparent, and intuitive
% - All files that users work with can be compiled separately
% - Project documentation is considerably improved, added guidelines for LaTeX usage
% - Automated handling of labels for missing modules, virtually no oversight required

% 2013-??-?? v0.1
% Initial Template
% % XeLaTeX can use any Mac OS X font. See the setromanfont command below.
% Input to XeLaTeX is full Unicode, so Unicode characters can be typed directly into the source.
% Apple Documentation Style - Latex Template
% https://github.com/wnagrodzki/AppleDocumentationStyleLatexTemplate
% Wojciech Nagrodzki 2013
