\newif\ifglobal

%%%%%%%%%%%%%%%%%%%%%%%
%%% Compile to view %%%
%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Readme:                                     %%%
%%% https://www.overleaf.com/read/bwdjvfjksvjy/ %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% >>> M?: marks a module setting number or important notice

% >>> M4: Set {./relative-path} to external files for this module <<<
\def \modulefiles {./06-RESCLK}
% To add an external file, use:
% (Replace '---' with actual filename)
% '\input{\modulefiles/tables/---.tex}' for tables
% '\includegraphics{\modulefiles/figures/---}' for figures
% '\subfile{\modulefiles/---}' for register files

% >>> M1: This module is in global project? <<<
% 'YES' - uncomment; 'NO' - comment out
\globaltrue

\ifglobal
    \documentclass[main\_\_EN.tex]{subfiles}
   
\else
    \documentclass[openany, 10pt]{book}

    % >>> M2: In ./00-shared/preamble-trm-module, also find
    %         settings for visibility of todo notes, watermark <<<
    \usepackage{preamble-trm-module}

    % >>> M3: Temporary labels in English,
    %         you can add your own labels there <<<
    \myexternaldocument{./00-shared/temp-labels-en}

\fi %\ifglobal


\setcounter{secnumdepth}{4}


% >>> M5: If this module needs any updates in
% './00-shared/preamble-trm-module.sty'
% (include additional package, etc.), contact your module PM
% or create the file './00-shared/changelog.tex' make the
% necessary updates yourself and document them in this file <<<

% Make text left-aligned and allow latex to break pages naturally, comment out for full justification
\raggedright
\raggedbottom


\begin{document}


% Sets language into which pre-defined bits of text are translated
\selectlanguage{english}
\setenglishformatting

% Do not delete this block
\ifglobal
% Add the following front matter if
% this module is outside of global project
\else
    \InputIfFileExists{readme}{}{}
    \listoftodos
    {\let\clearpage\relax \listofdonetodos}
    \tableofcontents
    %\listoftables
    %\listoffigures
\fi



%%%%%%%%%%%%%%%%%%%%%%%%%
%%% TRM Module Begins %%%
%%%%%%%%%%%%%%%%%%%%%%%%%

\hypertarget{resclk}{}
\chapter{Reset and Clock}
\label{mod:resclk}


\section{Reset}
\subsection{Overview}
ESP32-S2 provides four types of reset that occur at different levels, namely CPU Reset, Core Reset, System Reset, and Chip Reset.

All reset types mentioned above (except Chip Reset) maintain the data stored in internal memory.
Figure \ref{figure:Reset} shows the scopes of affected subsystems when different types of reset occur.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{\modulefiles/figures/Reset.eps}
    \caption{Reset Level}
    \label{figure:Reset}
\end{figure}

\begin{itemize}
    \item CPU Reset: Only resets CPU core. Once such reset is released, programs will be executed from CPU reset vector.
    \item Core Reset: Resets the whole digital system except RTC, including CPU, peripherals, Wi-Fi, and digital GPIOs.
    \item System Reset: Resets the whole digital system, including RTC.
    \item Chip Reset: Resets the whole chip.

\end{itemize}

\subsection{Reset Source}
CPU will be reset immediately when any of the reset above occurs. Users can get reset source codes by reading register \hyperref[fielddesc:RTCCNTLRESETCAUSEPROCPU]{RTC\_CNTL\_RESET\_CAUSE\_PROCPU} after the reset is released.

Table \ref{table:resetreasons} lists different reset sources and the types of reset they trigger.


\begin{longtable}[c]{ |p{0.8cm}|p{3.8cm}|p{2.7cm}|p{8.7cm}| }
\caption{Reset Source}
    \label{table:resetreasons} \\
        \hline
        \rowcolor{lightgray}
        Code & Source & Reset Type & Comments\\
        \hline
        \endfirsthead
        \hline
       \rowcolor{lightgray}
        PRO  & Source Type& Reset Type & Comments \\
        \hline
        \endhead
        \endfoot
        \hexprefix{}01  & Chip reset & Chip Reset & See the note below \\
        \hline
        \hexprefix{}0F & Brown-out system reset & System Reset & Triggered by brown-out detector\\
        \hline
        \hexprefix{}10  & RWDT system reset & System Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}}  \\
        \hline
        \hexprefix{}12 & Super watchdog reset & System Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}} \\
        \hline
        \hexprefix{}13 & GLITCH reset & System Reset & - \\
        \hline
        \hexprefix{}03 & Software system reset & Core Reset & Triggered by configuring \hyperref[fielddesc:RTCCNTLSWSYSRST]{RTC\_CNTL\_SW\_SYS\_RST} \\
        \hline
        \hexprefix{}05 & Deep-sleep reset & Core Reset & See Chapter \ref{mod:lowpowm} \textit{\nameref{mod:lowpowm}} \\
        \hline
        \hexprefix{}07 & MWDT0 global reset & Core Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}} \\
        \hline
        \hexprefix{}08 & MWDT1 global reset & Core Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}} \\
        \hline
        \hexprefix{}09 & RWDT core reset & Core Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}} \\
        \hline
        \hexprefix{}14 & eFuse reset & Core Reset & Triggered by eFuse CRC error \\
        \hline
        \hexprefix{}0B & MWDT0 CPU reset & CPU Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}} \\
        \hline
        \hexprefix{}0C & Software CPU reset & CPU Reset & Triggered by configuring \hyperref[fielddesc:RTCCNTLSWPROCPURST]{RTC\_CNTL\_SW\_PROCPU\_RST}\\
        \hline
        \hexprefix{}0D & RWDT CPU reset & CPU Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}}\\
        \hline
        \hexprefix{}11 & MWDT1 CPU reset & CPU Reset & See Chapter \ref{mod:wdt} \textit{\nameref{mod:wdt}} \\
        \hline

    \end{longtable}
    Note:
    \begin{itemize}
        \item Chip Reset can be triggered by the following three sources:
            \begin{itemize}
                \item Triggered by chip power-on;
                \item Triggered by brown-out detector;
                \item Triggered by Super Watchdog (SWD).
            \end{itemize}
        \item Once brown-out status is detected, the detector will trigger System Reset or Chip Reset, depending on register configuration. For more information, please see Chapter \ref{mod:lowpowm} \textit{\nameref{mod:lowpowm}}.
    \end{itemize}

\section{Clock}


\subsection{Overview}

ESP32-S2 provides multiple clock sources, which allow CPU, peripherals and RTC to work at different frequencies, thus providing more flexibility in meeting the requirements of various application scenarios. Figure \ref{figure:Clock} shows the system clock structure.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{\modulefiles/figures/sys_clk_arch.eps}
    \caption{System Clock}
    \label{figure:Clock}
\end{figure}

\subsection{Clock Source}
ESP32-S2 uses external crystal, internal PLL, or internal oscillator working as clock sources to generate different kinds of clocks, which can be classified in three types depending on their clock speed.
\begin{itemize}
    \item High speed clock for devices working at a higher frequency, such as CPU and digital peripherals
        \begin{itemize}
            \item PLL\_CLK (320 MHz or 480 MHz): internal PLL clock
            \item XTAL\_CLK (40 MHz): external crystal clock
        \end{itemize}
    \item Slow speed clock for low-power devices, such as power management unit and low-power peripherals
        \begin{itemize}
            \item XTAL32K\_CLK (32 kHz): external crystal clock
	        \item RC\_FAST\_CLK (8 MHz by default): internal divide-by-N oscillator of 8 MHz, with adjustable frequency
	        \item RC\_FAST\_DIV\_CLK (31.250 kHz by default): internal clock derived from RC\_FAST\_CLK divided by 256
	        \item RC\_SLOW\_CLK (90 kHz by default): internal oscillator with adjustable frequency
	    \end{itemize}
    \item Audio clock for audio-related devices
        \begin{itemize}
            \item APLL\_CLK (16 MHz \verb+~+ 128 MHz): internal Audio PLL clock
	    \end{itemize}
\end{itemize}

\subsection{CPU Clock}
\label{sec:cpu-clock}
As Figure \ref{figure:Clock} shows, CPU\_CLK is the master clock for CPU and it can be as high as 240 MHz when CPU works in high performance mode. Alternatively, CPU can run at lower frequencies, such as at 2 MHz, to lower power consumption.

Users can set PLL\_CLK, APLL\_CLK, RC\_FAST\_CLK or XTAL\_CLK as CPU\_CLK clock source by configuring register \hyperref[fielddesc:SYSTEMSOCCLKSEL]{SYSTEM\_SOC\_CLK\_SEL}, see Table \ref{table:cpu clksrc} and Table \ref{table:cpu clk}.

\begin{tiplisting}
ESP32-S2 is unable to operate without an external crystal clock.
\end{tiplisting}

\begin{longtable}[c]{|p{8.5cm}|p{7.3cm}| }
       \caption{CPU\_CLK Source}
		\label{table:cpu clksrc} \\
	    \hline
        \rowcolor{lightgray}
        \hyperref[fielddesc:SYSTEMSOCCLKSEL]{SYSTEM\_SOC\_CLK\_SEL} Value & Clock Source \\
        \hline
        0 & XTAL\_CLK \\
        \hline
        1 & PLL\_CLK \\
        \hline
        2 & RC\_FAST\_CLK \\
        \hline
        3 & APLL\_CLK \\
        \hline
\end{longtable}

\begin{table}[hb]
	\caption{CPU\_CLK Selection}
    \label{table:cpu clk}
     \begin{tabular}[h]{ |p{3.3cm}|c|c|c|p{9.7cm}|}
        \hline
        \rowcolor{lightgray}
        Clock Source & SEL\_0* & SEL\_1* & SEL\_2* & CPU Clock Frequency\\
        \hline
		\multirow{2}{*} {XTAL\_CLK} & \multirow{2}{*} {0} & \multirow{2}{*} {-} & \multirow{2}{*} {-} & CPU\_CLK = XTAL\_CLK / (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} + 1) \newline
         		\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} ranges from 0 \verb+~+ 1023. Default is 1\\
        \hline
       \multirow{2}{*} {PLL\_CLK (480 MHz)} & \multirow{2}{*} {1} & \multirow{2}{*} {1} & \multirow{2}{*} {0} & CPU\_CLK = PLL\_CLK / 6 \newline
         		CPU\_CLK frequency is 80 MHz \\
        \hline
       \multirow{2}{*} {PLL\_CLK (480 MHz)} & \multirow{2}{*} {1} &\multirow{2}{*} {1} & \multirow{2}{*} {1}  & CPU\_CLK = PLL\_CLK / 3 \newline
       			CPU\_CLK frequency is 160 MHz \\
        \hline
       \multirow{2}{*} {PLL\_CLK (480 MHz)} & \multirow{2}{*} {1} & \multirow{2}{*} {1} & \multirow{2}{*} {2}  & CPU\_CLK = PLL\_CLK / 2 \newline
       			CPU\_CLK frequency is 240 MHz \\
        \hline
        \multirow{2}{*} {PLL\_CLK (320 MHz)}  & \multirow{2}{*} {1}  & \multirow{2}{*} {0} & \multirow{2}{*} {0}  & CPU\_CLK = PLL\_CLK / 4 \newline
       			CPU\_CLK frequency is 80 MHz \\
        \hline
        \multirow{2}{*} {PLL\_CLK (320 MHz)}  & \multirow{2}{*} {1}  & \multirow{2}{*} {0} & \multirow{2}{*} {1}  & CPU\_CLK = PLL\_CLK / 2 \newline
       			CPU\_CLK frequency is 160 MHz \\
        \hline
       \multirow{2}{*} {RC\_FAST\_CLK}  & \multirow{2}{*} {2}  & \multirow{2}{*} {-}  & \multirow{2}{*} {-} & CPU\_CLK = RC\_FAST\_CLK / (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} + 1) \newline
        		\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} ranges from 0 \verb+~+ 1023. Default is 1\\
        \hline
        APLL\_CLK & 3 & 0 & 0 & CPU\_CLK = APLL\_CLK / 4 \\
        \hline
        APLL\_CLK & 3 & 0 & 1 & CPU\_CLK = APLL\_CLK / 2 \\
        \hline
    \end{tabular}
    *SEL\_0: The value of register \hyperref[fielddesc:SYSTEMSOCCLKSEL]{SYSTEM\_SOC\_CLK\_SEL}. \\
    *SEL\_1: The value of register \hyperref[fielddesc:SYSTEMPLLFREQSEL]{SYSTEM\_PLL\_FREQ\_SEL}. \\
    *SEL\_2: The value of register \hyperref[fielddesc:SYSTEMCPUPERIODSEL]{SYSTEM\_CPUPERIOD\_SEL}.
    \end{table}

Note:
    \begin{itemize}
        \item When users select XTAL\_CLK as CPU clock source and adjust the divider value by configuring register \hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT}, the rules below should be followed.
        \begin{itemize}
            \item If current divider value is 2 (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} = 1) and the target is x (x \verb+≠+ 1), users should set the divider first to 1 (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} = 0) and then to x (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} = x - 1).
            \item If current divider value is x (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} = x - 1) and the target is 2, users should set the divider first to 1 (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} = 0) and then to 2 (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} = 1).
            \item For other target divider value x, users can adjust the register directly (\hyperref[fielddesc:SYSTEMPREDIVCNT]{SYSTEM\_PRE\_DIV\_CNT} = x - 1).
        \end{itemize}
    \end{itemize}

\subsection{Peripheral Clock}
Peripheral clocks include APB\_CLK, REF\_TICK, LEDC\_PWM\_CLK, APLL\_CLK and PLL\_F160M\_CLK. Table \ref{table:peri clk owner} shows which clock can be used by which peripheral.

 \begin{longtable}[c]{|p{2.5cm}|c|c|c|c|c| }
 \caption{Peripheral Clock Usage}
    \label{table:peri clk owner} \\
  \hline
        \rowcolor{lightgray}
        Peripheral & APB\_CLK & REF\_TICK & LEDC\_PWM\_CLK & APLL\_CLK & PLL\_F160M\_CLK\\
        \hline
        TIMG & Y & Y &  &  &  \\
        \hline
        I²S & Y &  &  & Y & Y \\
        \hline
        UHCI & Y &  &  &  &  \\
        \hline
        UART & Y & Y &  &  &  \\
        \hline
        RMT & Y & Y &  &  &  \\
        \hline
        LED\_PWM & Y & Y & Y &  &  \\
        \hline
        I²C & Y & Y &  &  &  \\
        \hline
        SPI & Y &  &  &  &  \\
        \hline
        PCNT & Y &  &  &  &  \\
        \hline
        eFuse Controller & Y &  &  &  &  \\
        \hline
        SARADC/DAC & Y &  &  & Y &  \\
        \hline
        USB & Y &  &  &  &  \\
        \hline
        CRYPTO &  &  &  &  & Y \\
        \hline
        TWAI Controller & Y &  &  &  &  \\
        \hline
        System Timer & Y &  &  &  &  \\
        \hline
\end{longtable}

\subsubsection{APB\_CLK Source}\label{apbclkcource}

APB\_CLK is determined by the clock source of CPU\_CLK as shown in Table \ref{table:apb_clk_src}.

 \begin{longtable}[c]{ |p{7cm}|p{7cm}| }
      \caption{APB\_CLK Source}
    \label{table:apb_clk_src} \\
        \hline
        \rowcolor{lightgray}
        CPU\_CLK Source & APB\_CLK \\
        \hline
        \endfirsthead
        \hline
        \rowcolor{lightgray}
        CPU\_CLK source & APB\_CLK \\
        \hline
        \endhead
        \endfoot
        PLL\_CLK & 80 MHz \\
        \hline
        APLL\_CLK & CPU\_CLK / 2 \\
        \hline
        XTAL\_CLK & CPU\_CLK \\
        \hline
        RC\_FAST\_CLK & CPU\_CLK \\
        \hline
\end{longtable}

\subsubsection{REF\_TICK}\label{sec:ref_tick}

REF\_TICK is derived from APB\_CLK. The APB\_CLK frequency is determined by CPU\_CLK source. The REF\_TICK frequency should be fixed. When CPU\_CLK source changes, users need to make sure the REF\_TICK frequency remains unchanged by setting a correct divider value.

Clock divider registers are shown in Table \ref{table:ref_tick}.

\begin{longtable}[c]{|l|l|l|}
      \caption{REF\_TICK}
    \label{table:ref_tick}
        \\\hline
        \rowcolor{lightgray}
        CPU\_CLK Source & APB\_CLK Frequency & REF\_TICK Frequency\\
        \hline
        \endfirsthead
        \hline
        \rowcolor{lightgray}
        CPU\_CLK Source & APB\_CLK Frequency & REF\_TICK Frequency\\
        \hline
        \endhead
        \endfoot
        PLL\_CLK & 80 MHz & APB\_CLK/(\hyperref[regdesc:SYSCONTICKCONFREG]{SYSCON\_XTAL\_TICK\_NUM} + 1)\\
        \hline
        APLL\_CLK & CPU\_CLK/2 & APB\_CLK/(\hyperref[regdesc:SYSCONTICKCONFREG]{SYSCON\_XTAL\_TICK\_NUM} + 1)\\
        \hline
         XTAL\_CLK & CPU\_CLK & APB\_CLK/ (\hyperref[regdesc:SYSCONTICKCONFREG]{SYSCON\_XTAL\_TICK\_NUM} + 1)\\
        \hline
        RC\_FAST\_CLK & CPU\_CLK & APB\_CLK/(\hyperref[regdesc:SYSCONTICKCONFREG]{SYSCON\_CK8M\_TICK\_NUM} + 1)\\
        \hline
\end{longtable}

For example, when CPU\_CLK source is PLL\_CLK and users need to keep the REF\_TICK frequency at 1 MHz, then they should set \hyperref[regdesc:SYSCONTICKCONFREG]{SYSCON\_XTAL\_TICK\_NUM} to 79 (\hexprefix{}4F) so that the REF\_TICK frequency = 80 MHz/(79 + 1) = 1 MHz.



\subsubsection{LEDC\_PWM\_CLK Source}\label{ledcsclksource}

LEDC\_PWM\_CLK clock source is selected by configuring register \hyperref[fielddesc:LEDCAPBCLKSEL]{LEDC\_APB\_CLK\_SEL}, as shown in Table \ref{table:led_sclk}.

    \begin{longtable}[c]{ |p{7.5cm}|p{7.5cm}| } \caption{LEDC\_PWM\_CLK Source}
    \label{table:led_sclk} \\
        \hline
        \rowcolor{lightgray}
        \hyperref[fielddesc:LEDCAPBCLKSEL]{LEDC\_APB\_CLK\_SEL} Value & LEDC\_PWM\_CLK Source \\
        \hline
        0 (Default) & - \\
        \hline
        1 & APB\_CLK \\
        \hline
        2 & RC\_FAST\_CLK \\
        \hline
        3 & XTAL\_CLK \\
        \hline
\end{longtable}

\subsubsection{APLL\_SCLK Source}

APLL\_CLK is sourced from PLL\_CLK, and its output frequency is configured using APLL configuration registers. See Section \ref{audiopll} for more information.

\subsubsection{PLL\_F160M\_CLK Source}

PLL\_F160M\_CLK is divided from PLL\_CLK according to current PLL frequency.


\subsubsection{Clock Source Considerations}\label{ClockSourceConsiderations}

Peripherals that need to work with other clocks, such as RMT and I2C, generally operate using PLL\_CLK frequency as a reference. When this frequency changes, peripherals should update their clock configuration to operate at the same frequency after the change. Peripherals accessing REF\_TICK can continue operating normally without changing their clock configuration when switching clock sources. Please see Table \ref{table:peri clk owner}.

LED module uses RC\_FAST\_CLK as clock source when APB\_CLK is disabled. In other words, when the system is in low-power mode, 
most peripherals will be halted (APB\_CLK is turned off), but LED can work normally via RC\_FAST\_CLK.



\subsection{Wi-Fi Clock}
Wi-Fi can work only when APB\_CLK uses PLL\_CLK as its clock source. Suspending PLL\_CLK requires that Wi-Fi has entered low-power mode first.

LOW\_POWER\_CLK uses XTAL32K\_CLK, XTAL\_CLK, RC\_FAST\_CLK or RTC\_SLOW\_CLK (the low clock selected by RTC) as its clock source for Wi-Fi in low-power mode.


\subsection{RTC Clock}

The clock sources for \hypertarget{SLOW_CLK}{RTC\_SLOW\_CLK} and RTC\_FAST\_CLK are low-frequency clocks. RTC module can operate when most other clocks are stopped.


RTC\_SLOW\_CLK derived from RC\_SLOW\_CLK, XTAL32K\_CLK or RC\_FAST\_DIV\_CLK is used to clock Power Management module.

RTC\_FAST\_CLK is used to clock On-chip Sensor module. It can be sourced from a divided XTAL\_CLK or from RC\_FAST\_CLK.

\subsection{Audio PLL Clock}\label{audiopll}
The operation of audio and other time-critical data-transfer applications requires highly-configurable, low-jitter and accurate clock sources. The clock sources derived from system clocks that serve digital peripherals may carry jitter and, therefore, are not suitable for a high-precision clock frequency setting.

Providing an integrated precision clock source can minimize system cost. To this end, ESP32-S2 integrates an audio PLL to clock I²S module.

Audio PLL formula is as follows:
$$f_{\rm{out}} = \frac{f_{\rm{xtal}}(\rm{sdm2}+\frac{\rm{sdm1}}{2^8}+\frac{\rm{sdm0}}{2^{16}}+4)}{2(odiv+2)}$$

Parameters are defined below:
\begin{itemize}
    \item \begin{math}f_{\rm{xtal}}\end{math}: the frequency of crystal oscillator, usually 40 MHz;
    \item sdm0: the value is 0 \verb+~+ 255;
    \item sdm1: the value is 0 \verb+~+ 255;
    \item sdm2: the value is 0 \verb+~+ 63;
    \item odiv: the value is 0 \verb+~+ 31;
    \item The operating frequency range of the numerator is 350 MHz \verb+~+ 500 MHz.
$$ 350 MHz<{f_{\rm{xtal}}(\rm{sdm2}+\frac{\rm{sdm1}}{2^8}+\frac{\rm{sdm0}}{2^{16}}+4)}<500 MHz $$
\end{itemize}

Audio PLL can be manually enabled or disabled via registers \hyperref[fielddesc:RTCCNTLPLLAFORCEPU]{RTC\_CNTL\_PLLA\_FORCE\_PU} and \hyperref[fielddesc:RTCCNTLPLLAFORCEPD]{RTC\_CNTL\_PLLA\_FORCE\_PD}, respectively. Disabling it takes priority over enabling it. When \hyperref[fielddesc:RTCCNTLPLLAFORCEPU]{RTC\_CNTL\_PLLA\_FORCE\_PU} and \hyperref[fielddesc:RTCCNTLPLLAFORCEPD]{RTC\_CNTL\_PLLA\_FORCE\_PD} are 0, PLL follows the state of the system. When the system enters sleep mode, PLL will be disabled automatically; when the system wakes up, PLL will be enabled automatically.

\section{Register Summary}\label{sec:resclk-reg-summ}

The addresses in this section are relative to SYSCON base address provided in Table \ref{table:Peripheral_Address_Mapping} \textit{Peripheral Address Mapping} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.

\subfile{\modulefiles/apb-ctrl-summary_en.tex}

\section{Registers}

The addresses in this section are relative to SYSCON base address provided in Table \ref{table:Peripheral_Address_Mapping} \textit{Peripheral Address Mapping} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.

\subfile{\modulefiles/apb-ctrl-reg_en.tex}

\end{document}
