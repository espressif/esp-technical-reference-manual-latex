\begin{longtable}[c]{|l|m{12cm}|}
    \caption{Read Instructions}
    \label{tab:pie-load-instructions}\\
    \hline
    \rowcolor{lightgray}
    \textbf{Instructions} & \textbf{Description} \\\hline
    \endfirsthead
    \rowcolor{lightgray}
    \textbf{Instructions} & \textbf{Description} \\\hline
    \endhead
    \endfoot
    
    \hyperref[instruction:ESPVLD128XP]{ESP.VLD.128.XP} & Read the 16-byte data, then add the value stored in the AR register to the access address.
    \\\hline
    \hyperref[instruction:ESPVLD128IP]{ESP.VLD.128.IP} & Read the 16-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPVLDH64XP]{ESP.VLD.[H/L].64.XP} & Read the 8-byte data, then add the value stored in the AR register to the access address.
    \\\hline
    \hyperref[instruction:ESPVLDH64IP]{ESP.VLD.[H/L].64.IP} & Read the 8-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPVLDBC8XP]{ESP.VLDBC.[8/16/32].XP} & Read the 1-byte/2-byte/4-byte data, then add the value stored in the AR register to the access address.
    \\\hline
    \hyperref[instruction:ESPVLDBC8IP]{ESP.VLDBC.[8/16/32].IP} & Read the 1-byte/2-byte/4-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPVLDHBC16INCP]{ESP.VLDHBC.16.INCP} & Read the 16-byte data, then add 16 to the access address.
    \\\hline
    \hyperref[instruction:ESPLD128USARXP]{ESP.LD.128.USAR.XP} & Read the 16-byte data, then add the value stored in the AR register to the access address.
    \\\hline
    \hyperref[instruction:ESPLD128USARIP]{ESP.LD.128.USAR.IP} & Read the 16-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDQAU8128XP]{ESP.LDQA.U8.128.[XP/IP]} & Read the 16-byte data and slice it by 1-byte, and zero-extend it to 24-bit data, which then will be written to register QACC\_H and QACC\_L, and finally add the value stored in the AR register or the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDQAU16128XP]{ESP.LDQA.U16.128.[XP/IP]} & Read the 16-byte data and slice it by 2-byte, and zero-extend it to 48-bit data, which then will be written to register QACC\_H and QACC\_L, and finally add the value stored in the AR register or the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDQAS8128XP]{ESP.LDQA.S8.128.[XP/IP]} & Read the 16-byte data and slice it by 1-byte, then sign-extend it to 24-bit data, which then will be written to register QACC\_H and QACC\_L, and finally add the value stored in the AR register or the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDQAS16128XP]{ESP.LDQA.S16.128.[XP/IP]} & Read the 16-byte data and slice it by 1-byte, then sign-extend it to 48-bit data, which then will be written to register QACC\_H and QACC\_L, and finally add the value stored in the AR register or the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDQACC_HH128IP]{ESP.LD.QACC.[H/L].H.128.IP} & Read the 16-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDQACC_HL128IP]{ESP.LD.QACC.[H/L].L.128.IP} & Read the 16-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDXACCIP]{ESP.LD.XACC.IP} & Read the 8-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDUA_STATEIP]{ESP.LD.UA.STATE.IP} & Read the 16-byte data, then add the immediate value to the access address.
    \\\hline
    \hyperref[instruction:ESPLDXQ32]{ESP.LDXQ.32} & Update access address first, then read the 4-byte data.
    \\\hline

    \hyperref[instruction:ESPVLDEXTU8IP]{ESP.VLDEXT.[U/S][8/16].[IP/XP]} & Vector signed-expand the load data as 8-bit segment to 16-bit segment.
    \\\hline
    
\end{longtable}
