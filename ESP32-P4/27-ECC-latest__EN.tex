\newif\ifglobal

%%%%%%%%%%%%%%%%%%%%%%%
%%% Compile to view %%%
%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Readme:                                     %%%
%%% https://www.overleaf.com/read/bwdjvfjksvjy/ %%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% >>> M?: marks a module setting number or important notice

% >>> M4: Set {./relative-path} to external files for this module <<<
\def \modulefiles {./27-ECC}
% To add an external file, use:
% (Replace '---' with actual filename)
% '\input{\modulefiles/tables/---.tex}' for tables
% '\includegraphics{\modulefiles/figures/---}' for figures
% '\subfile{\modulefiles/---}' for register files

% >>> M1: This module is in global project? <<<
% 'YES' - uncomment; 'NO' - comment out
\globaltrue

\ifglobal
    \documentclass[main\_\_EN.tex]{subfiles}

\else
    \documentclass[openany, 10pt]{book}

    % >>> M2: In ./chip-spec-settings/preamble-trm-module, also find
    %         settings for visibility of todo notes, watermark <<<
    \usepackage{preamble-trm-module}

    % >>> M3: Temporary labels in English,
    %         you can add your own labels there <<<
    \myexternaldocument{temp-labels-en}

\fi %\ifglobal


\setcounter{secnumdepth}{4}

% >>> M5: If this module needs any updates in
% './chip-spec-settings/preamble-trm-module.sty'
% (include additional package, etc.), contact your module PM
% or create the file './chip-spec-settings/changelog.tex' make the
% necessary updates yourself and document them in this file <<<

\raggedright


\begin{document}


% Sets language into which pre-defined bits of text are translated
\selectlanguage{english}

% Do not delete this block
\ifglobal
% Add the following front matter if
% this module is outside of global project
\else
    \InputIfFileExists{readme}{}{}
    {\let\clearpage\relax \listoftodos}
    {\let\clearpage\relax \listofdonetodos}
    \tableofcontents
    %\listoftables
    %\listoffigures
\fi


%%%%%%%%%%%%%%%%%%%%%%%%%
%%% TRM Module Begins %%%
%%%%%%%%%%%%%%%%%%%%%%%%%

\hypertarget{ecc}{}
\chapter{ECC Accelerator (ECC)}
\label{mod:ecc}

\section{Overview}

Elliptic Curve Cryptography (ECC) is an approach to public-key cryptography based on the algebraic structure of elliptic curves. ECC uses smaller keys compared to RSA cryptography while providing equivalent security.

\chipname{}'s ECC accelerator can complete various calculations based on different elliptic curves, thus accelerating the ECC algorithm and ECC-derived algorithms such as ECDSA.

\section{Feature List}

\chipname{}'s ECC accelerator has the following features:

\begin{itemize}
    \item Three different elliptic curves, namely, P-192, P-256, and P-384 defined in \href{https://nvlpubs.nist.gov/nistpubs/FIPS/NIST.FIPS.186-5.pdf}{FIPS 186-5}
    \item 11 working modes
    \item Enhanced anti-attack performance
    \item Interrupt upon completion of calculation
\end{itemize}

\section{ECC Basics}

To better illustrate the functionality of the ECC accelerator, the basic knowledge and terminology used in this chapter are introduced in this section.

\subsection{Elliptic Curve and Points on the Curves}

The ECC algorithm is based on elliptic curves over prime fields, which can be represented as:
\[
{y}^{2} = {x}^{3} + {ax} + b \ \textrm{mod}\ p
\]
where,
\begin{itemize}
    \item $p$ is a prime number,
    \item $a$ and $b$ are two non-negative integers smaller than $p$,
    \item and $(x, y)$ is a point on the curve satisfying the representation.
\end{itemize}


\subsection{Affine Coordinates and Jacobian Coordinates}

An elliptic curve can be represented as below:

\begin{itemize}
    \item In affine coordinates:
    \[
    {y}^{2} = {x}^{3} + {ax} + b \ \textrm{mod}\ p
    \]
    \item In Jacobian coordinates:
    \[
    {Y}^{2} = {X}^{3} + {aXZ}^{4} + {bZ}^{6} \ \textrm{mod}\ p
    \]
\end{itemize}

To convert affine coordinates $(x,y)$ to/from Jacobian coordinates $(X,Y,Z)$:

\begin{itemize}
    \item From Jacobian to Affine coordinates:
    \[
    x = X / {Z}^2 \ \textrm{mod}\ p
    \]
    \[
    y = Y / {Z}^3 \ \textrm{mod}\ p
    \]
    \item From Affine to Jacobian coordinates:
    \[ X = x \]
    \[ Y = y \]
    \[ Z = 1\]
\end{itemize}

\subsection{Memory Blocks}

ECC's memory blocks store the input and output data of the ECC operation.

\begin{table}[H]
    \centering
    \caption{ECC Accelerator Memory Blocks} \label{tab:ecc-memory}
    \begin{threeparttable}
    \begin{tabular}{ | l | l | l | l | l | l | }
    \hline
        \rowcolor{lightgray}
        \textbf{Memory Block} & \textbf{Size (byte)}  &  \textbf{Starting Address}\tnote{*} &  \textbf{Ending Address} \tnote{*} & \textbf{Access}   \\ \hline
        ECC\_MULT\_Mem\_k    & 32  & 0x100 & 0x11F & R/W \\ \hline
        ECC\_MULT\_Mem\_Px   & 32  & 0x120 & 0x13F & R/W \\ \hline
        ECC\_MULT\_Mem\_Py   & 32  & 0x140 & 0x15F & R/W \\ \hline
        ECC\_MULT\_Mem\_Qx   & 32  & 0x160 & 0x17F & R/W \\ \hline
        ECC\_MULT\_Mem\_Qy   & 32  & 0x180 & 0x19F & R/W \\ \hline
        ECC\_MULT\_Mem\_Qz   & 32  & 0x1A0 & 0x1BF & R/W \\ \hline
    \end{tabular}
    \begin{tablenotes}
        \item[*] Address offset related to the ECC accelerator base address is provided in Table \ref{tab:sysmem-base-address} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.
    \end{tablenotes}
    \end{threeparttable}
\end{table}


\subsection{Data and Data Block}

\chipname{}'s ECC can operate on 192-bit, 256-bit, or 384-bit data, depending on the elliptic curves.

For example, the 256-bit data $D[255:0]$ can be divided into eight 32-bit data blocks $D[n][31:0] (n=0, 1, \cdots, 7)$. Data blocks with smaller indexes correspond to lower binary bits. To be specific:
\[
D[255:0] = {D[7][31:0],D[6][31:0],D[5][31:0],
D[4][31:0],D[3][31:0],D[2][31:0],D[1][31:0],D[0][31:0]}
\]

\subsection{Writing Data}

Writing data means writing data to an ECC memory block and using this data as the input to the ECC algorithm. To be specific, writing data to an ECC memory block means writing $D[n][31:0] (n=0, 1, \cdots, 7)$ to the ``starting address of this ECC memory block + $4 \times n$" sequentially:

\begin{itemize}
    \item write $D[0]$ to ``starting address"
    \item write $D[1]$ to ``starting address + 4"
    \item $\cdots$
    \item write $D[7]$ to ``starting address + 28"
\end{itemize}

\begin{tiplisting}
When storing data, write only the data block of the required length. Do not append a 0 to the most significant bit. For example, for 192-bit data, store only the 192 bits without appending 0.
\end{tiplisting}

\subsection{Reading Data}

Read data means reading data from the starting address of an ECC memory block and using this data as the output from the ECC algorithm. To be specific, read data from an ECC memory block means reading $D[n][31:0] (n=0, 1, \cdots, 7)$ from the ``starting address of this ECC memory block + $4 \times n$" successively:
\begin{itemize}
    \item read $D[0]$ from ``starting address"
    \item read $D[1]$ from ``starting address + 4"
    \item $\cdots$
    \item read $D[7]$ from ``starting address + 28"
\end{itemize}

\begin{tiplisting}
When reading data, only the data block of the required length needs to be read. For example, to read 192-bit data, simply read the lower 192 bits (i.e., 6 data blocks).
\end{tiplisting}


\subsection{Standard Calculation and Jacobian Calculation}

\chipname{}'s ECC performs Affine Point Calculation (including Affine Point Verification, Affine Point Add, and Affine Point Multiplication) using the affine coordinates and Jacobian Calculation (including Jacobian Point Verification, Jacobian Point Add, and Jacobian Point Multiplication) using the Jacobian coordinates.

\section{Function Description}
\label{sec:ecc-mode-config}
\subsection{Curve Mode}

\chipname{}'s ECC supports acceleration based on three curve modes, each corresponding to an elliptic curve. By configuring the \hyperref[fielddesc:ECCMULTCURVEMODE]{ECC\_MULT\_CURVE\_MODE} field, users can select the desired key size. For details, see Table \ref{tab:ecc-curve-mode} below.

\begin{table}[H]
    \centering
    \caption{ECC Accelerator Curve Mode Selection}
    \label{tab:ecc-curve-mode}
    \begin{threeparttable}
    \begin{tabular}{|p{4.2cm}|p{3.6cm}|}
    \hline
        \rowcolor{lightgray}
        \textbf{\hyperref[fielddesc:ECCMULTCURVEMODE]{ECC\_MULT\_CURVE\_MODE}} & \textbf{Elliptic Curves\tnote{*}} \\ \hline
        0 & FIPS P-192  \\\hline
        1 & FIPS P-256  \\\hline
        2 & FIPS P-384  \\\hline
    \end{tabular}
    \begin{tablenotes}
        \item[*] See definition of FIPS P-192, P-256, and P-384 defined in \href{https://nvlpubs.nist.gov/nistpubs/FIPS/NIST.FIPS.186-5.pdf}{FIPS 186-5}.
    \end{tablenotes}
    \end{threeparttable}
\end{table}

\subsection{Working Modes}
\label{sec:ecc-work-mode}

\chipname{}'s ECC accelerator supports 11 working modes based on two elliptic curves described in the above section. By configuring the \hyperref[fielddesc:ECCMULTWORKMODE]{ECC\_MULT\_WORK\_MODE} field, users can select the desired working mode. For details, see Table \ref{tab:ecc-work-mode}.

\begin{table}[H]
    \centering
    \caption{Working Modes of ECC Accelerator}
    \label{tab:ecc-work-mode}
    \begin{tabular}{|l|L{6cm}|}
    \hline
        \rowcolor{lightgray}
        \textbf{\hyperref[fielddesc:ECCMULTWORKMODE]{ECC\_MULT\_WORK\_MODE}} & \textbf{Working Modes}  \\ \hline
        0  & Affine Point Multi                   \\\hline
        1  & \textbf{Reserved}                    \\\hline
        2  & Affine Point Verif                   \\\hline
        3  & Affine Point Verif + Multi           \\\hline
        4  & Jacobian Point Multi                 \\\hline
        5  & Point Add                       \\\hline
        6  & Jacobian Point Verif                 \\\hline
        7  & Affine Point Verif + Jacobian Point Multi  \\\hline
        8  & Mod Add                         \\\hline
        9  & Mod Sub                     \\\hline
        10 & Mod Multi                   \\\hline
        11 & Mod Div                         \\\hline
    \end{tabular}
\end{table}

\begin{tiplisting}
Note that the calculation of Jacobian Point Multi mode is about 10\% faster than that of the Affine Point Multi mode.
\end{tiplisting}

Detailed descriptions about different working modes are provided in the following sections.

\subsubsection{Affine Point Multiplication (Affine Point Multi)}

Affine Point Multiplication can be represented as:
\[
Q = (Q_x,Q_y) = (J_x,J_y,J_z) = k \cdot (P_x,P_y)
\]
where,
\begin{itemize}
    \item $(Q_x,Q_y)$ is the affine expression of point Q.
    \item $(J_x,J_y,J_z)$ is the Jacobian expression of point Q.
    \item Input: $P_x$, $P_y$, and $k$  are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_k} respectively.
    \item Output: $Q_x$ and $Q_y$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py} respectively.
\end{itemize}

\subsubsection{Affine Point Verification (Affine Point Verif)}

Affine Point Verification can be used to verify if a point  $(P_x,P_y)$ is on a selected elliptic curve.

\begin{itemize}
    \item Input: $P_x$ and $P_y$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py} respectively.
    \item Output: The verification result is stored in the \hyperref[fielddesc:ECCMULTVERIFICATIONRESULT]{ECC\_MULT\_VERIFICATION\_RESULT} bit.
\end{itemize}

\subsubsection{Affine Point Verification + Affine Point Multiplication (Affine Point Verif + Multi)}

In this mode, ECC first verifies if point $(P_x,P_y)$ is on the selected elliptic curve. If so, the following multiplication is performed:
\[
Q = (Q_x,Q_y) = (J_x,J_y,J_z) = k \cdot (P_x,P_y)
\]
where,
\begin{itemize}
    \item $(Q_x,Q_y)$ is the affine expression of point Q.
    \item $(J_x,J_y,J_z)$ is the Jacobian expression of point Q.
    \item Input: $P_x$, $P_y$, and $k$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_k} respectively.
    \item Output:
    \begin{itemize}
        \item The verification result is stored in the \hyperref[fielddesc:ECCMULTVERIFICATIONRESULT]{ECC\_MULT\_VERIFICATION\_RESULT} bit.
        \item $Q_x$ and $Q_y$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py} respectively.
        \item $J_x$, $J_y$, and $J_z$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qx}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qy}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qz}.
    \end{itemize}
\end{itemize}

\subsubsection{Jacobian Point Multiplication (Jacobian Point Multi)}

Jacobian Point Multiplication can be represented as:
\[
Q = (Q_x,Q_y,Q_z) = k \cdot (P_x,P_y,1)
\]
where,
\begin{itemize}
    \item $(Q_x,Q_y,Q_z)$ is the Jacobian expression of point Q.
    \item 1 in the point's Jacobian coordinates is automatically completed by hardware.
    \item Input: $P_x$, $P_y$, and $k$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_k} respectively.
    \item Output: $Q_x$, $Q_y$, and $Q_z$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qx}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qy}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qz} respectively.
\end{itemize}

\subsubsection{Point Addition (Point Add)}

Point Addition can be represented as:
\[
R = (R_x,R_y) = (J_x,J_y,J_z) = (P_x,P_y,1) + (Q_x,Q_y,Q_z)
\]
where,
\begin{itemize}
    \item $(R_x,R_y)$ is the affine expression of point R.
    \item $(J_x,J_y,J_z)$ is the Jacobian expression of point R.
    \item 1 in the point's Jacobian coordinates is automatically completed by hardware.
    \item Input:
    \begin{itemize}
        \item $P_x$ and $P_y$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
        \item $Q_x$, $Q_y$, and $Q_z$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qx}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qy}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qz}.
    \end{itemize}
    \item Output:
    \begin{itemize}
        \item $R_x$ and $R_y$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
        \item $J_x$, $J_y$ and $J_z$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qx}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qy}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qz}.
    \end{itemize}
\end{itemize}

\subsubsection{Jacobian Point Verification (Jacobian Point Verif)}

Jacobian Point Verification can be used to verify if point $(Q_x,Q_y,Q_z)$ is on a selected elliptic curve.
\begin{itemize}
    \item $(Q_x,Q_y,Q_z)$ is the Jacobian expression of point Q.
    \item Input: $Q_x$, $Q_y$, and $Q_z$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qx}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qy}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qz} respectively.
    \item Output: The verification result is stored in the \hyperref[fielddesc:ECCMULTVERIFICATIONRESULT]{ECC\_MULT\_VERIFICATION\_RESULT} bit.
\end{itemize}

\subsubsection{Affine Point Verification + Jacobian Point Multiplication (Affine Point Verif + Jacobian Point Multi)}

In this mode, ECC first verifies if point $(P_x,P_y)$ is on the selected elliptic curve. If so, the following multiplication is performed:
\[
Q = (Q_x,Q_y,Q_z) = k \cdot (P_x,P_y,1)
\]
where,
\begin{itemize}
    \item $(Q_x,Q_y,Q_z)$ is the Jacobian expression of point Q.
    \item 1 in the point's Jacobian coordinates is automatically completed by hardware.
    \item Input: $P_x$, $P_y$, and $k$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_k}.
    \item Output:
    \begin{itemize}
        \item The verification result is stored in the \hyperref[fielddesc:ECCMULTVERIFICATIONRESULT]{ECC\_MULT\_VERIFICATION\_RESULT} bit.
        \item $Q_x$, $Q_y$, and $Q_z$ are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qx}, \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qy}, and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Qz}.
    \end{itemize}
\end{itemize}

\subsubsection{Mod Addition (Mod Add)}
Mod Addition can be represented as:
\[
R = A + B \ \textrm{mod}\ N
\]
where,
\begin{itemize}
    \item Input:
    \begin{itemize}
        \item A and B are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
        \item The value of N is related to the register fields below:
        \begin{itemize}
            \item \hyperref[fielddesc:ECCMULTCURVEMODE]{ECC\_MULT\_CURVE\_MODE} to select the related curve.
            \item \hyperref[fielddesc:ECCMULTMODBASE]{ECC\_MULT\_MOD\_BASE} to choose using mod base or order of the base point.
        \end{itemize}
    \end{itemize}
    \item Output: R is stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px}.
\end{itemize}

\subsubsection{Mod Subtraction (Mod Sub)}
Mod Subtraction can be represented as:
\[
R = A - B \ \textrm{mod}\ N
\]
where,
\begin{itemize}
    \item Input:
    \begin{itemize}
        \item A and B are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
        \item The value of N is related to the register fields below:
        \begin{itemize}
            \item \hyperref[fielddesc:ECCMULTCURVEMODE]{ECC\_MULT\_CURVE\_MODE} to select the related curve.
            \item \hyperref[fielddesc:ECCMULTMODBASE]{ECC\_MULT\_MOD\_BASE} to choose using mod base or order of the base point.
        \end{itemize}
    \end{itemize}
    \item Output: R is stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px}.
\end{itemize}

\subsubsection{Mod Multiplication (Mod Multi)}
Mod Multiplication can be represented as:
\[
R = A \cdot B \ \textrm{mod}\ N
\]
where,
\begin{itemize}
    \item Input:
    \begin{itemize}
        \item A and B are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
        \item The value of N is related to the register fields below:
        \begin{itemize}
            \item \hyperref[fielddesc:ECCMULTCURVEMODE]{ECC\_MULT\_CURVE\_MODE} to select the related curve.
            \item \hyperref[fielddesc:ECCMULTMODBASE]{ECC\_MULT\_MOD\_BASE} to choose using mod base or order of the base point.
        \end{itemize}
    \end{itemize}
    \item Output: R is stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
\end{itemize}

\subsubsection{Mod Division (Mod Div)}
Mod Division can be represented as:
\[
R = A \cdot {B}^{-1} \ \textrm{mod}\ N
\]
where,
\begin{itemize}
    \item Input:
    \begin{itemize}
        \item A and B are stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Px} and \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
        \item The value of N is related to the register fields below:
        \begin{itemize}
            \item \hyperref[fielddesc:ECCMULTCURVEMODE]{ECC\_MULT\_CURVE\_MODE} to select the related curve.
            \item \hyperref[fielddesc:ECCMULTMODBASE]{ECC\_MULT\_MOD\_BASE} to choose using mod base or order of the base point.
        \end{itemize}
    \end{itemize}
    \item Output: R is stored in \hyperref[tab:ecc-memory]{ECC\_MULT\_Mem\_Py}.
\end{itemize}

\subsection{Enhancing Anti-Attack Performance}
\label{sec:ecc-anti-attack}

When performing different types of point multiplication calculations, i.e., mode 0, 3, 4, and 7, \chipname{}'s ECC accelerator offers an additional option to further enhance its anti-attack performance by ensuring that all point multiplication calculations take the same maximum runtime.

To enable the enhanced anti-attack performance, set \hyperref[fielddesc:ECCMULTSECURITYMODE]{ECC\_MULT\_SECURITY\_MODE} to 1. If this setting is enabled, each time the ECC accelerator perform a point multiplication calculation:

\begin{itemize}
    \item The execution latency is constant for a given operation.
    \item Power consumption variations are minimized.
\end{itemize}

\section{Clock and Reset}
\label{sec:ecc-clkrst}

\chipname{}'s ECC only has one clock module (CRYPTO\_ECC\_CLK) and one reset module (CRYPTO\_ECC\_RST).

ECC's clock and reset is handled by the the Power/Clock/Reset (PCR) module (see Chapter \ref{mod:resclk} \textit{\nameref{mod:resclk}} for more information). Users should enable the ECC clock by setting \hyperref[fielddesc:HPSYSCLKRSTCRYPTOECCCLKEN]{HP\_SYS\_CLKRST\_CRYPTO\_ECC\_CLK\_EN} and release the ECC reset by clearing \hyperref[fielddesc:HPSYSCLKRSTRSTENECC]{HP\_SYS\_CLKRST\_RST\_EN\_ECC} before starting the ECC accelerator. Besides, due to resource reuse between cryptography accelerator modules, users also need to additionally clear the \hyperref[fielddesc:HPSYSCLKRSTRSTENECDSA]{HP\_SYS\_CLKRST\_RST\_EN\_ECDSA} bit.

%%%%%%%%%%%%%%%%%%%%%
%%% Interrupts %%%
%%%%%%%%%%%%%%%%%%%%%

\section{Interrupts}
\label{sec:ecc-interrupt}

\chipname{}'s ECC accelerator can generate the ECC\_INTR interrupt signal that will be sent to the \hyperref[mod:intmtrx]{Interrupt Matrix}.

ECC\_INTR has only one interrupt source to generate the ECC\_INTR interrupt signal, i.e., \label{int:ECCMULTCALCDONEINT}{ECC\_MULT\_CALC\_DONE\_INT}, which is triggered on the completion of an ECC calculation.

Each interrupt source can be configured by a common set of registers that are described in Section \hyperref[interrupt-config-registers]{\textit{Interrupt Configuration Registers}}. The ECC\_MULT\_CALC\_DONE\_INT interrupt source is configured by the following registers (refer to Section \ref{sec:ecc-reg-summ} \textit{\nameref{sec:ecc-reg-summ}} for more information):

\begin{itemize}
    \item \hyperref[fielddesc:ECCMULTCALCDONEINTRAW]{ECC\_MULT\_CALC\_DONE\_INT\_RAW}: Stores the raw interrupt status of ECC\_MULT\_CALC\_DONE\_INT.
    \item \hyperref[fielddesc:ECCMULTCALCDONEINTST]{ECC\_MULT\_CALC\_DONE\_INT\_ST}: Indicates the status of the ECC\_MULT\_CALC\_DONE\_INT interrupt. This bit is generated by enabling or disabling the \hyperref[fielddesc:ECCMULTCALCDONEINTRAW]{ECC\_MULT\_CALC\_DONE\_INT\_RAW} bit via \hyperref[fielddesc:ECCMULTCALCDONEINTENA]{ECC\_MULT\_CALC\_DONE\_INT\_ENA}.
    \item \hyperref[fielddesc:ECCMULTCALCDONEINTENA]{ECC\_MULT\_CALC\_DONE\_INT\_ENA}: Enables or disables the ECC\_MULT\_CALC\_DONE\_INT interrupt.
    \item \hyperref[fielddesc:ECCMULTCALCDONEINTCLR]{ECC\_MULT\_CALC\_DONE\_INT\_CLR}: Set this bit to clear the ECC\_MULT\_CALC\_DONE\_INT interrupt status. By setting this bit to 1, the \hyperref[fielddesc:ECCMULTCALCDONEINTRAW]{ECC\_MULT\_CALC\_DONE\_INT\_RAW} and  \hyperref[fielddesc:ECCMULTCALCDONEINTST]{ECC\_MULT\_CALC\_DONE\_INT\_ST} bits will be cleared.
\end{itemize}

\begin{tiplisting}
For definitions of \textit{interrupt}, \textit{interrupt signal}, \textit{interrupt source}, and their correlations, please refer to Chapter \ref{mod:intmtrx} \textit{\nameref{mod:intmtrx}} > Section \ref{sec:intmtx-int-terminology} \textit{\nameref{sec:intmtx-int-terminology}}.
\end{tiplisting}


\section{Programming Procedures}

The programming procedure for configuring ECC is described below:

\begin{enumerate}
    \item Configure the ECC clock and reset. Refer to Section \ref{sec:ecc-clkrst} for detailed information.
    \item Select the key size and working mode as described in Section \ref{sec:ecc-mode-config}.
    \item Enable the \hyperref[int:ECCMULTCALCDONEINT]{ECC\_MULT\_CALC\_DONE\_INT} interrupt as described in Section \ref{sec:ecc-interrupt}.
    \item Set the \hyperref[fielddesc:ECCMULTSTART]{ECC\_MULT\_START} field to start ECC calculation.
    \item Wait for the \hyperref[int:ECCMULTCALCDONEINT]{ECC\_MULT\_CALC\_DONE\_INT} interrupt, which indicates the completion of the ECC calculation.
    \item Check the result as described in Section \ref{sec:ecc-mode-config}.
\end{enumerate}

% >>> If your module has no registers, delete sample text
%       for Sections Register Summary and Registers below <<<

%%%%%%%%%%%%%%%%%%%%%%%%
%%% Register Summary %%%
%%%%%%%%%%%%%%%%%%%%%%%%

% >>> Update the sample text below and insert
%     the info on register summary into the subfile <<<

\newpage
\hypertarget{ecc-reg-summ}{}
\section{Register Summary}
\label{sec:ecc-reg-summ}

The addresses in this section are relative to ECC Accelerator base address provided in Table \ref{tab:sysmem-base-address} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.

The abbreviations given in Column \textbf{Access} are explained in Section \hyperref[glossary-access-types]{\textit{Access Types for Registers}}.

\subfile{\modulefiles/27-ECC-reg-summ__EN}


%%%%%%%%%%%%%%%%%
%%% Registers %%%
%%%%%%%%%%%%%%%%%

% >>> Update the sample text below and insert
%      the info on registers into the subfile <<<

\newpage
\section{Registers}

The addresses in this section are relative to ECC Accelerator base address provided in Table \ref{tab:sysmem-base-address} in Chapter \ref{mod:sysmem} \textit{\nameref{mod:sysmem}}.

For how to program reserved fields, please refer to Section \hyperref[programming-reserved-field]{\textit{Programming Reserved Register Field}}.

\subfile{\modulefiles/27-ECC-reg__EN}


\end{document}
